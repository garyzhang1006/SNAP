\documentclass{article}
\usepackage[T1]{fontenc}
\usepackage{iclr2027_conference,times}
\usepackage{amsmath,amssymb,booktabs,array,graphicx,hyperref,url,algorithm,algorithmic}
\usepackage{caption,needspace,flafter}
\captionsetup{labelsep=period,font=normalsize}
\brokenpenalty=10000
\newcolumntype{L}[1]{>{\raggedright\arraybackslash}p{#1}}
\newcommand{\ci}[2]{\ensuremath{[#1,\,#2]}}
\usepackage{xr}\externaldocument[S-]{supplement_extended}
\newcommand{\suptab}[1]{Table~\ref{S-#1} of the extended supplement}
\newcommand{\supfig}[1]{Figure~\ref{S-#1} of the extended supplement}
\hypersetup{pdfauthor={},pdftitle={Error bars on benchmark averages can miss run covariance}}
\title{Error Bars on Benchmark Averages\\Can Miss Run Covariance}
\author{Anonymous authors}
\newcommand{\Lam}{\Lambda}
\newcommand{\Sig}{\Sigma_E}
\usepackage{xcolor}
\newcommand{\pending}[1]{\textcolor{red}{[#1]}}
% compute2 outcomes, set from analysis/estimates.py verdicts once scoring ends.
% \Ronecase: 0 pending (prints every variant, labelled), 1 pass, 2 fail.
% \Rtwocase: 0 pending, 1 not supported, 2 supported, 3 undecided.
\newcommand{\Ronecase}{0}
\newcommand{\Rtwocase}{0}
\newcommand{\outcome}[4]{\ifnum#1=0 \pending{#3} #4\else\ifnum#1=#2 #4\fi\fi}
% PolyPythias values: \res{key}{label} prints the value from pythia_values.tex, written by
% work/finalize/fill_pending.py from the frozen analysis output, or the red placeholder until then.
\newcommand{\res}[2]{\ifcsname res@#1\endcsname\csname res@#1\endcsname\else\pending{#2}\fi}
\IfFileExists{pythia_values.tex}{\input{pythia_values.tex}}{}
\begin{document}
\maketitle
\begin{abstract}
Error bars on a language model's benchmark average usually sum per-benchmark variances and ignore the covariance between benchmarks across training runs. On 375 DataDecide runs, the ten-benchmark average's standard deviation in per-byte margin is 1.244 \ci{1.143}{1.338} times its value under independence. A margin comparison that assumes independence at a nominal 0.05 therefore has a true size near 0.11. That covariance sits within scoring formats, where BoolQ, alone in its format, carries 68\% of the margin covariance trace, and accuracy gives 1.078 with an interval that includes one. SNAP, a cross-half estimator, also gives per-benchmark run variances net of item noise, which the replicate standard deviation of the average can't separate when item noise is large. Correcting for the covariance changes few of DataDecide's recipe orderings. A test on four held-out tasks that we fixed before scoring fails at 1.217, with a lower limit below one. \outcome{\Ronecase}{1}{R1 pass}{A second rule fixed before scoring passes, with margin inflation of \res{r1lam}{R1 lambda} \ci{\res{r1lo}{lo}}{\res{r1hi}{hi}} across nine seeds at five PolyPythias sizes.}\outcome{\Ronecase}{2}{R1 fail}{A second rule fixed before scoring also fails, since nine seeds at five PolyPythias sizes give margin inflation of \res{r1lam}{R1 lambda} with an interval \ci{\res{r1lo}{lo}}{\res{r1hi}{hi}} that includes one.} \outcome{\Rtwocase}{1}{R2 not supported}{A disjoint item bank leaves \res{xlam}{cross lambda} there and gives no support to a shared item explanation.}\outcome{\Rtwocase}{2}{R2 supported}{A disjoint item bank supports a shared item component there.}\outcome{\Rtwocase}{3}{R2 undecided}{A disjoint item bank leaves the source of that inflation undecided.} All other analyses in the paper are exploratory. We recommend reporting the replicate standard deviation of the battery average together with a benchmark-removal check, and that standard deviation needs no item split and gives an inflation of 1.237 here.
\end{abstract}

\section{Introduction}
A benchmark average can change across training runs even when the evaluation items stay fixed. For an equally weighted average of $K$ benchmarks, independent run deviations give variance $K^{-2}\operatorname{tr}\Sig$, where $\Sig$ is their cross-benchmark covariance matrix. Assuming independence drops the off-diagonal terms and weakens a comparison's error control. When the true standard deviation is $\Lam$ times the independent one, a two-sided comparison at a nominal 0.05 has size $2\{1-\Phi(1.96/\Lam)\}$ with $\Phi$ the standard normal distribution function. That size is 0.115 at our margin estimate and 0.069 at the accuracy one, and our simulation of two independently trained configurations gives 0.113 (Appendix~\ref{app:supplementary}). Figure~\ref{fig:teaser} shows where that covariance sits in the DataDecide battery. Because the correction changes few calls on DataDecide's recipe comparisons, the understatement shows mainly in the error bar of one model's battery average and in planning replicate runs.

\begin{figure}[t]
\centering
\begin{minipage}[c]{0.56\linewidth}
\includegraphics[trim=0 0 86 0,clip,width=\linewidth]{figures/covariance.pdf}
\end{minipage}\hfill
\begin{minipage}[c]{0.41\linewidth}
\caption{Cross-half correlation estimates of run deviations over the 125 DataDecide configurations, per-byte margins left and accuracy right, with undefined values in grey. Margin correlations average 0.624 within scoring formats and 0.007 across them (Section~\ref{sec:composition}).}
\label{fig:teaser}
\end{minipage}
\end{figure}

We estimate item-general covariance from two disjoint item halves per benchmark, centring both half scores across each configuration's replicate runs and taking their cross-half products. Under zero-mean item noise and conditional independence across halves, they estimate run covariance, including its diagonal. We adapt the repeated-measurement construction of quantitative genetics and call it SNAP, Seed Noise Across Phenotypes (Appendix~\ref{app:derivations}).

Our empirical study uses DataDecide \citep{magnusson2025}, with 25 data recipes and five of its model sizes, each with three released replicates, and those 125 configurations supply 375 runs across ten benchmarks, scored at the largest checkpoint step each configuration's replicates share. Our primary analysis reads released item scores without model inference and compares per-byte log-likelihood margins with accuracy. Margins carry more seed signal, with per-benchmark reliability, the run variance's share of a benchmark half's replicate variance, averaging 0.652 against 0.324 for accuracy (Section~\ref{sec:composition}). We therefore treat margins as the primary scale and accuracy as a bound.

Margin inflation $\Lam$, the ratio of the average's standard deviation to its independent value, is 1.244 \ci{1.143}{1.338}. Accuracy gives 1.078 \ci{0.993}{1.157} at the scored checkpoint, an interval that includes one (Table~\ref{tab:primary}).

Because we built our estimator on these ten benchmarks, we pre-specified a test on SciQ \citep{welbl2017sciq}, MedMCQA \citep{pal2022medmcqa}, and DROP \citep{dua2019drop} and CoQA \citep{reddy2019coqa} in multiple-choice form, on the same checkpoints at 530M, 750M, and 1B. Its rule requires the lower interval limit of held-out margin inflation to exceed one. The test fails at its full scope of 225 runs, where held-out inflation is 1.217 \ci{0.872}{1.498} against 1.240 \ci{1.054}{1.397} for the original ten on the same runs (Section~\ref{sec:heldout}).

A single model family whose replicates mix default and auxiliary runs on different schedules leaves open whether the inflation belongs to DataDecide's design. We therefore score all 45 PolyPythias runs at five sizes, where each of nine seeds reruns one recipe, and pair that battery with a disjoint bank of 6,808 items built with the OLMES harness \citep{gu2025olmes} at a pinned commit. Two rules fixed before scoring read the results, and in simulation the first passes in 0.259 of replicates at a true 1.244 and the second returns undecided in 0.761. Section~\ref{sec:family} reports that margin inflation there is \res{r1lam}{R1 lambda} with interval \ci{\res{r1lo}{lo}}{\res{r1hi}{hi}}\outcome{\Ronecase}{1}{R1 pass}{, which passes the first rule,}\outcome{\Ronecase}{2}{R1 fail}{, which fails the first rule,} and that the shared-item explanation \outcome{\Rtwocase}{1}{R2 not supported}{isn't supported}\outcome{\Rtwocase}{2}{R2 supported}{is supported}\outcome{\Rtwocase}{3}{R2 undecided}{is undecided}.

This paper makes four contributions to measuring run covariance. (i) We define the item-general run covariance that cross-half products estimate, identified up to an item component shared across benchmarks and halves. (ii) The replicate standard deviation of the battery average gives the uncertainty of rerunning the released items and is what we recommend reporting. The split adds per-benchmark run variances net of item noise and a redrawn-bank target, untested on DataDecide, and the two part ways when item noise is large, as on DataDecide accuracy without BoolQ, where the replicate standard deviation gives 1.285 against the split's 1.558 (Section~\ref{sec:estimator}). (iii) Algorithm~\ref{alg:snap} gives the measurement recipe, with near-nominal intervals in simulation unless a run effect follows the size band or item noise reaches the top of our swept range. (iv) We measure run covariance across the 375 released runs and find it concentrated within scoring formats, where every recipe, recipe-pair and benchmark deletion and four of five size-band deletions leave a margin interval that excludes one.
\section{Estimand and estimator}
\label{sec:estimator}
\subsection{Runs and score scales}
Let $c$ index configurations, $r$ index their $R$ replicate runs, and $j$ index benchmarks, where a configuration fixes a data recipe, model size, and nominal hyperparameters, and Table~\ref{tab:notation} collects the symbols. We call the set of benchmarks the battery and the set of items the bank. DataDecide's seed controls initialisation and data order together. Each configuration has one default and two auxiliary runs, and below 1B the auxiliary runs stop at 25 percent of the 1B compute budget. That schedule difference adds a batch component beyond the seed. For item $i$, define the per-byte margin and corresponding correctness indicator by
\begin{equation}
m_{crji}=\frac{\ell(\mathrm{gold}_i)}{\mathrm{bytes}(\mathrm{gold}_i)}-
\max_{v\ne\mathrm{gold}_i}\frac{\ell(v_i)}{\mathrm{bytes}(v_i)},
\qquad a_{crji}=\mathbf{1}\{m_{crji}>0\}.
\label{eq:scores}
\end{equation}
Here $\ell$ is the summed answer log-likelihood and $v$ ranges over alternatives, with a zero margin treated as incorrect. Items are averaged within each benchmark, with MMLU macro-averaged over its 57 subjects, and we denote the margin and accuracy scores by $y^{\mathrm{marg}}$ and $y^{\mathrm{acc}}$, calling each a trait.

For each half $H\in\{A,B\}$, our measurement model is
\begin{equation}
y^H_{crj}=\mu^H_{cj}+E_{crj}+\varepsilon^H_{crj}.
\label{eq:model}
\end{equation}
A half-specific mean lets the two item sets differ in difficulty, the component $E_{crj}$ is the run deviation shared by the two item populations, and $\varepsilon^H_{crj}$ carries the remaining item-dependent variation. When deriving unbiasedness we assume independent, identically distributed run effects within a configuration, an assumption the released runs only approximate (Section~\ref{sec:data}), and we write their covariance as $\Sigma_{E,c}$. This target is defined for a redrawn or extended item bank. A run's item-specific deviations on the released bank fall into $\varepsilon$, and reproducibility on that fixed bank is a separate question.

\subsection{Cross-half covariance}
We partition each benchmark's items into disjoint halves with a frozen random seed and reuse that assignment for its replicate runs. We centre each half score across replicate runs, giving $d^H_{crj}=y^H_{crj}-R^{-1}\sum_s y^H_{csj}$. If the item errors have conditional mean zero and zero covariance between the two halves across the relevant runs and benchmarks, then
\begin{equation}
\mathbb{E}[d^A_{crj}d^B_{crk}]=(1-1/R)\Sigma_{E,c}(j,k).
\label{eq:identity}
\end{equation}
Equation~\ref{eq:identity} covers diagonal entries without requiring Gaussian run effects. Disjoint item identifiers alone don't establish the required independence, since shared passages and duplicate content can connect the halves, and we check the one benchmark with documented shared passages by keeping each passage in one half. Centring removes any item effect that stays constant across runs. A run's deviation on every item of one prompt template recurs on any redrawn bank in that format and belongs to run covariance of the fixed battery. We identify the estimate only up to a deviation tied to items that both halves share. Cross-format inflation keeps only the diagonal and the covariances between benchmarks of different scoring formats, and it is 0.921 \ci{0.801}{1.035}. A uniform component shared across benchmarks that carried the whole margin excess of 0.244 above one would lift it to 1.113, and without cross-format run covariance the upper limit admits at most 0.319 of that excess, although negative cross-format run covariance, as the 0.921 point estimate suggests, could offset a larger component (Appendix~\ref{app:calibration}). A format-following component is untested in DataDecide, and a cross-bank rule tests one tied to the item set in PolyPythias. The fixed-bank inflation reported below needs no halves and no cross-half assumption. Only the split's redrawn-bank target and the per-benchmark run variances rest on it.

We write the average centred score as $g^H_{cr}=K^{-1}\sum_j d^H_{crj}$ and compute
\begin{equation}
T_c=\frac{1}{R-1}\sum_r g^A_{cr}g^B_{cr},\qquad
U_c=\frac{1}{K^2(R-1)}\sum_{j,r}d^A_{crj}d^B_{crj}.
\label{eq:tu}
\end{equation}
The two statistics in Equation~\ref{eq:tu} have expectations $K^{-2}\mathbf{1}^{\mathsf T}\Sigma_{E,c}\mathbf{1}$ and $K^{-2}\operatorname{tr}\Sigma_{E,c}$. We sum each across configurations before dividing to obtain
\begin{equation}
\widehat\Lam=\sqrt{\frac{\sum_c T_c}{\sum_c U_c}},\qquad
\Lam^2=\frac{\mathbf{1}^{\mathsf T}\Sig\mathbf{1}}{\operatorname{tr}\Sig}
=1+(K-1)\bar r_E,\qquad K_{\mathrm{eff}}=\frac{K}{\Lam^2},
\label{eq:lambda}
\end{equation}
where $\Sig=N^{-1}\sum_c\Sigma_{E,c}$ is the covariance matrix averaged over the $N$ configurations, whose off-diagonal contribution we express through the effective coefficient
\begin{equation}
\bar r_E=\frac{\sum_{j\ne k}\Sigma_E(j,k)}{(K-1)\operatorname{tr}\Sig}.
\label{eq:effective}
\end{equation}
This coefficient equals the mean off-diagonal correlation only when the marginal variances are equal (Appendix~\ref{app:derivations}). Our $K_{\mathrm{eff}}$ compares this battery with independent benchmarks of the same average variance and doesn't count independent information sources. Because $\Lam$ and $\bar r_E$ normalise by the battery's own trace, they can rise when removing a high-variance benchmark lowers $\sigma_{\mathrm{agg}}$, and we compare batteries through $\sigma_{\mathrm{agg}}$ (Table~\ref{tab:primary}).

Either pooled sum can be nonpositive in a finite sample, and our calibration simulations check the bias of the ratio. Subtracting item-sampling variance from the full-data diagonal instead needs a model of item-by-run noise, and on the released runs it gives 1.246 \ci{1.144}{1.340} against 1.244 on margins (Appendix~\ref{app:implementation}). On the released bank, the replicate standard deviation of the full-bank average gives inflation 1.237 \ci{1.137}{1.331} on margins and 1.067 \ci{0.994}{1.134} on accuracy, against 1.244 and 1.078 from the split. Its denominator sums per-benchmark replicate variances, and any item component the halves share counts as fixed-bank run variance. Under that item-by-run model, item noise is 0.037 of the full-bank margin diagonal and 0.186 of the accuracy one, leaving the two targets nearly equal. Without BoolQ, however, it is 0.578 of the accuracy diagonal and fixed-bank accuracy inflation of 1.285 falls below the split's 1.558.

\begin{algorithm}[t]
\caption{SNAP measurement recipe for a fixed battery of $K$ benchmarks scored on $R$ replicate runs per configuration.}
\label{alg:snap}
\begin{algorithmic}[1]
\STATE Score every replicate run $r$ of every configuration $c$ on every item of the $K$ benchmarks.
\STATE Assign each benchmark's items to halves $A$ and $B$ once, with a frozen seed, and reuse that assignment for every run.
\STATE Average items within each half to obtain $y^A_{crj}$ and $y^B_{crj}$, then centre across the $R$ runs within each configuration to obtain $d^H_{crj}$.
\STATE Form the battery averages $g^H_{cr}$ and the statistics $T_c$ and $U_c$ of Equation~\ref{eq:tu}.
\STATE Pool over configurations and report $\widehat\Lam=\sqrt{\sum_c T_c/\sum_c U_c}$ and $K_{\mathrm{eff}}=K/\widehat\Lam^2$.
\STATE Report a wild cluster bootstrap-$t$ interval over the recipe clusters (Appendix~\ref{app:implementation}) and repeat the split to measure re-split variation.
\STATE Recompute $\widehat\Lam$ after removing each benchmark and each cluster in turn, and report that spread beside the interval, since the factor belongs to the battery.
\end{algorithmic}
\end{algorithm}


\subsection{Interval inference}
We report nominal 95\% wild cluster bootstrap-$t$ intervals over the 25 recipe clusters, using 4,999 Rademacher draws and a linearisation of the squared ratio, with the observed denominator held fixed during resampling (Appendix~\ref{app:implementation}).

The wild interval's coverage across the twelve populations of Table~\ref{tab:coverage}, at 2,000 replicates each, runs from 0.928 to 0.954, while a configuration percentile bootstrap that ignores recipe dependence drops to 0.872 under recipe-shared effects. Within-benchmark cross-half error correlation of 0.1 and the accuracy-like null population bias the estimate down by 0.011 to 0.016. In these populations finite-sample bias works against finding an excess.

\section{Data and analysis scope}
\label{sec:data}
DataDecide supplies the ten benchmarks ARC-Challenge and ARC-Easy \citep{clark2018arc}, BoolQ \citep{clark2019boolq}, CommonsenseQA \citep{talmor2019commonsenseqa}, HellaSwag \citep{zellers2019hellaswag}, MMLU \citep{hendrycks2021mmlu}, OpenBookQA \citep{mihaylov2018openbookqa}, PIQA \citep{bisk2020piqa}, Social IQa \citep{sap2019socialiqa}, and WinoGrande \citep{sakaguchi2020winogrande}. We analyse 37,682 items per run across 25 recipes at 150M, 300M, 530M, 750M, and 1B parameters, since smaller models lack per-item replicate coverage. Replicate seed labels are $\{2,14,15\}$ below 1B and $\{2,4,5\}$ at 1B.

In each configuration we select the largest checkpoint step that all three runs share. The release's checkpoint index puts this step at 41.5\% of the 750M default runs' final step on average and 87\% at 530M. In 26 configurations, one at 530M and 25 at 750M, the largest final step exceeds the smallest by more than a factor of 1.5. At 750M the default runs stop short of their released final models, but the auxiliary runs sit near their final steps in all but one configuration.

We interpret the full estimate as covariance among the released runs at the selected steps. The auxiliary-run contrast, at 1.256, removes the batch component. Dropping the 26 severely truncated configurations removes the 750M band and one 530M configuration, giving margin inflation 1.276 (\ci{1.114}{1.420}) and accuracy inflation 1.120 (\ci{1.020}{1.208}), a subset we chose after seeing the full-sample estimates. On the 33 configurations that share a final step, margin inflation is 1.082 (\ci{0.121}{1.530}) and accuracy 1.129 (\ci{0.769}{1.397}), intervals too wide to separate the value one from the full-sample estimates, although the full-sample margin auxiliary contrast, whose runs sit near their final steps, still excludes one. A configuration's margin influence has Spearman correlation $-0.036$ with its selected step's share of the default final step (Appendix~\ref{app:schedule}). At the adjacent earlier shared step the accuracy interval is \ci{1.044}{1.210} and excludes one, and across the 123 configurations with such a step the paired change in accuracy inflation is 0.048 (\ci{-0.022}{0.130}, Appendix~\ref{app:checkpoint}). That check can't see an offset shared by every configuration. The auxiliary contrast doesn't remove differences tied to checkpoint schedules either, and the PolyPythias seeds avoid them by sharing one final step. Our accuracy bound thus holds at the scored checkpoint.

We never drew the analysis plan's split of screening and estimation recipes. Every choice in the primary analysis could therefore see all 125 configurations, and neither the plan's predicted inflation thresholds nor its check G5 held (Appendix~\ref{app:design}).

We retain the full battery for our primary estimand, including benchmarks with negative estimated diagonal covariance, since an unbiased moment estimate can be negative near zero and deleting such a benchmark would change the estimand.

We add a second model family whose replicate design DataDecide lacks. PolyPythias \citep{vanderwal2025} trains nine seeds at each of 14M, 31M, 70M, 160M, and 410M parameters with shared code, data, and hyperparameters and a seed that sets initialisation and data order together, and we score all 45 runs at the shared final step 143,000 on all 37,682 items. The request text was taken from the DataDecide release, and every prompt matches its scoring byte for byte. Appendix~\ref{app:transport} reports an earlier transfer check on 4,755 items.

For the identification check we built a second bank of 6,808 items from the same ten benchmarks with OLMES at a pinned commit, taking the train split for eight benchmarks and the validation split for OpenBookQA and MMLU. Any item whose context matches a bank-one context or whose question appears among its own few-shot exemplars was dropped, and each benchmark apart from MMLU was capped at 600 items. Both PolyPythias rules were committed before either bank was scored.

\section{Results}
\subsection{Full-battery estimates and composition}
\label{sec:composition}
Full-battery margins exclude independence, while accuracy gives only a bound. At the accuracy upper limit of 1.157 the battery has $\bar r_E$ of 0.038 and $K_{\mathrm{eff}}$ of 7.47 under our model.

\begin{table}[!htb]
\centering
\caption{Inflation estimates across 125 configurations with nominal 95\% recipe-cluster intervals, or cluster-robust $t(4)$ intervals over the five size bands where marked. The auxiliary contrast compares the two auxiliary runs and removes the default-versus-auxiliary batch component. The aggregate standard deviation $\sigma_{\mathrm{agg}}$ of the battery mean under the fitted covariance (Appendix~\ref{app:derivations}) uses margin units for margins and fractions for accuracy.}
\label{tab:primary}
\small\setlength{\tabcolsep}{4pt}\renewcommand{\arraystretch}{1.12}
\begin{tabular}{llrrrr}
\toprule
Score & Estimate & $\widehat\Lam$ & Interval & $K_{\mathrm{eff}}$ & $\sigma_{\mathrm{agg}}$\\
\midrule
Margin & Raw & 1.244 & \ci{1.143}{1.338} & 6.46 & 0.00572\\
Margin & Auxiliary contrast & 1.256 & \ci{1.090}{1.400} & 6.34 & 0.00562\\
Margin & Size-band clusters & 1.244 & \ci{1.030}{1.426} & 6.46 & 0.00572\\
Accuracy & Raw & 1.078 & \ci{0.993}{1.157} & 8.60 & 0.00514\\
Accuracy & Auxiliary contrast & 1.084 & \ci{0.939}{1.207} & 8.51 & 0.00516\\
Accuracy & Size-band clusters & 1.078 & \ci{0.961}{1.184} & 8.60 & 0.00514\\
\bottomrule
\end{tabular}
\end{table}

Accuracy carries little seed signal at the benchmark level, where reliability averages 0.324 against 0.652 on margins, and Table~\ref{tab:original3} and Appendix~\ref{app:supplementary} give the per-benchmark diagnostics, none of which enter the headline ratio because it uses covariance products directly.

The factor belongs to this battery, since margin correlations track scoring format (cloze for HellaSwag, PIQA and WinoGrande, yes/no for BoolQ, and multiple choice for the other six), averaging 0.624 within formats and 0.007 across them, excluding WinoGrande's undefined correlations (Figure~\ref{fig:covariance} in Appendix~\ref{app:supplementary}). BoolQ, the only benchmark in its format, carries 68\% of the margin covariance trace and 84\% of the accuracy trace. Removing it raises margin inflation to 1.786 \ci{1.689}{1.877}, and accuracy inflation to 1.558 \ci{1.412}{1.687}. Removing CommonsenseQA instead gives inflation estimates of 1.096 and 1.014. The removal estimates imply off-diagonal terms involving BoolQ summing to about $-0.15$ of the margin trace and $-0.06$ of the accuracy trace. Its removal lowers the denominator of the ratio more than the numerator and so raises the factor, while the margin aggregate standard deviation falls from 0.00572 to 0.00516. Lowering BoolQ's weight from a tenth to 0.0867 already stops the accuracy interval including one (Appendix~\ref{app:supplementary}).

Shared passages don't account for BoolQ's trace share, although that check can't reach other shared item effects. We recovered the 2,938 passages behind BoolQ's 3,270 questions from the release's request files, and assigning each passage whole to one half changes the two estimates by $-0.0002$ and $-0.0001$ on average across fifty re-splits. The BoolQ diagonal moves by 0.03\% and 0.15\% under the same reassignment. A permutation that reshuffles each run's item deviations inside each of the 66 item groups, MMLU's 57 subjects and the other nine benchmarks, leaves all trait scores fixed and breaks the tie between particular items and runs. Across 2,000 such permutations the permuted estimates sit about 0.001 below the observed margin value and 0.002 below accuracy. Item-level sharing thus accounts for little of either, although an effect carried by every item of a benchmark survives it.

Benchmark removal moves our estimates more than recipe or size removal. Recipe removal moves margin inflation between 1.207 and 1.256, and all 300 recipe-pair deletions keep the interval above one, with estimates from 1.177 to 1.263 and no lower limit below 1.037 (Appendix~\ref{app:supplementary}). Size removal moves it between 1.178 and 1.289, where the deletion of 530M gives \ci{0.993}{1.338} and stops excluding one. Figure~\ref{fig:composition} shows every single-benchmark removal with its interval on both scales, and Appendix~\ref{app:composition} separates benchmark count, identity, and weights. Our design treats this battery as fixed, and that spread isn't a distribution over benchmark collections.

\begin{figure}[t]
\centering
\includegraphics[width=0.95\linewidth]{figures/composition.pdf}
\caption{Inflation after removing each benchmark in turn, with nominal 95\% recipe-cluster intervals. The solid line marks one and the dashed line the full ten-benchmark estimate. On margins every removal leaves an interval that excludes one, with estimates from 1.096 without CommonsenseQA to 1.786 without BoolQ.}
\label{fig:composition}
\end{figure}


\subsection{A pre-specified test on held-out tasks}
\label{sec:heldout}
The pre-specified rule fails on the four held-out tasks. Held-out margin inflation on the 225 runs is 1.217 \ci{0.872}{1.498}, while the original ten give 1.240 \ci{1.054}{1.397} and pass it, a comparison specified before any held-out score existed. Because inflation belongs to its battery, the four-task and ten-task values aren't directly comparable. The held-out interval is also 0.626 wide against 0.343 for the original.

The protocol named SciQ, MedMCQA, and two AGIEval \citep{zhong2024agieval} reasoning tasks, with a pre-specified rule replacing any task whose 1B pilot accuracy fell below chance plus 0.02, a bar both AGIEval tasks narrowly missed (Appendix~\ref{app:heldout}). The battery became SciQ, MedMCQA, and multiple-choice versions of DROP and CoQA, with 1,000 items in each of the four tasks and items sharing a passage or story kept in the same half. Our GPU budget couldn't cover all 375 released checkpoints, and our protocol's scope rule limited the test to those runs at the steps of the primary analysis. The test passes only if the lower 95\% limit exceeds one, and Table~\ref{tab:heldout} reports the test at its pre-specified scope. Every partial scope read before the full one also failed the rule, with held-out margin estimates of 1.152 to 1.203, and Appendix~\ref{app:heldout} lists all five, with simulated power by number of sizes.

\begin{table}[!htb]
\centering
\caption{The pre-specified test at 530M, 750M, and 1B, with nominal 95\% recipe-cluster intervals over 75 configurations. The auxiliary contrast compares the two auxiliary runs, the original rows read the released runs at the same sizes as a comparison specified before any held-out score existed, and the last row gives the median over 60 four-benchmark subsets of the original battery across all five sizes with subset 5th to 95th percentiles, not an interval, from the committed battery-size curve.}
\label{tab:heldout}
\small\setlength{\tabcolsep}{4pt}\renewcommand{\arraystretch}{1.12}
\begin{tabular}{llrr}
\toprule
Battery & Score & $\widehat\Lam$ & Interval\\
\midrule
Held-out, four tasks & Margin & 1.217 & \ci{0.872}{1.498}\\
Held-out, four tasks & Accuracy & 1.220 & \ci{1.066}{1.359}\\
Held-out, auxiliary contrast & Margin & 1.181 & \ci{0.906}{1.410}\\
Original, ten benchmarks & Margin & 1.240 & \ci{1.054}{1.397}\\
Original, ten benchmarks & Accuracy & 1.097 & \ci{0.922}{1.246}\\
Original, auxiliary contrast & Margin & 1.273 & \ci{0.738}{1.629}\\
Original, four-benchmark subsets, all sizes (percentiles) & Margin & 1.176 & 0.982 to 1.476\\
\bottomrule
\end{tabular}
\end{table}

As an exploratory result, the held-out accuracy interval excludes one while the margin interval doesn't, the reverse of the original battery's pattern on the same runs. Accuracy inflation is 1.220 \ci{1.066}{1.359} against 1.097 for the original ten on the same runs. Removing CoQA lowers held-out margin inflation to 1.090, and removing any other task leaves it between 1.120 and 1.213. Only the removal of DROP, at 1.213 \ci{1.035}{1.377}, excludes one. The held-out estimate thus depends on battery composition as the original does.

\subsection{A second family and a disjoint item bank}
\label{sec:family}
Two rules fixed before scoring decide what the runs show. The first reads margin inflation across the 45 PolyPythias runs from a delete-one-seed jackknife with eight degrees of freedom and passes when the lower endpoint exceeds one, since five size clusters are too few for the wild bootstrap. The second puts every bank-one item in half $A$ and every bank-two item in half $B$ on the same runs, and that assignment removes any component the halves share only through a common item set. It returns not supported when that cross-bank interval sits above one and the jackknife interval for the within-minus-cross difference in log squared inflation includes zero. It supports the explanation when that difference interval lies above zero and the cross-bank interval covers one, and we report any other pattern as undecided. The banks also differ in item count and exemplar overlap, and bank two draws most of its items from train splits, which pretraining data are more likely to contain. Any covariance these differences carry also enters the cross-bank estimate.

We simulated both rules after fixing them, drawing seed effects and item noise from the DataDecide margin fit (Appendix~\ref{app:transport}). Over 4,000 replicates the first rule passes in 0.259 at a true 1.244 and in 0.016 at independence, with the middle 90\% of estimates between 1.026 and 1.456. At a true 1.244 with no shared item component the second returns not supported in 0.226, supported in 0.013 and undecided in 0.761, while under independence a uniform item share of 0.124, which reproduces 1.244 only in the population of Appendix~\ref{app:calibration} and lifts the median within-bank estimate to 1.046, gives supported in 0.033 and undecided in 0.957. At a share of 0.755, enough alone to give 1.244 on this noise, the second returns supported in 0.067, not supported in 0.015 and undecided in 0.919. An undecided verdict from the second rule can't exclude one.

\outcome{\Ronecase}{1}{R1 pass}{The second family also shows margin inflation above one. Margin inflation across the 45 runs is \res{r1lam}{R1 lambda} with jackknife interval \ci{\res{r1lo}{lo}}{\res{r1hi}{hi}}, against 1.244 on DataDecide, and accuracy gives \res{acclam}{acc lambda} with interval \ci{\res{acclo}{lo}}{\res{acchi}{hi}}. Each PolyPythias seed reruns one recipe, and no counterpart arises to the batch component behind our auxiliary contrast. The per-size estimates run from \res{szmin}{min} at \res{szminat}{size} to \res{szmax}{max} at \res{szmaxat}{size} (Appendix~\ref{app:transport}). Without BoolQ margin inflation is \res{nbqlam}{noBoolQ lambda} with interval \ci{\res{nbqlo}{lo}}{\res{nbqhi}{hi}}, \res{nbqdir}{which moves the same way as on DataDecide or not}.}

\outcome{\Ronecase}{2}{R1 fail}{The second family doesn't pass the margin rule's threshold. Margin inflation across the 45 runs is \res{r1lam}{R1 lambda} with a jackknife interval \ci{\res{r1lo}{lo}}{\res{r1hi}{hi}} that includes one, and accuracy gives \res{acclam}{acc lambda} with interval \ci{\res{acclo}{lo}}{\res{acchi}{hi}}. Five sizes with nine seeds each give 40 within-configuration contrasts against 250 in DataDecide, too few for the interval to separate this family's inflation from one or from DataDecide's 1.244. The per-size estimates run from \res{szmin}{min} at \res{szminat}{size} to \res{szmax}{max} at \res{szmaxat}{size} (Appendix~\ref{app:transport}).}

\outcome{\Rtwocase}{1}{R2 not supported}{The shared-item explanation fails its rule in this family. With bank one as half $A$ and bank two as half $B$ on the same 45 runs, margin inflation is \res{xlam}{cross lambda} with interval \ci{\res{xlo}{lo}}{\res{xhi}{hi}}. The within-minus-cross difference in log squared inflation has an interval \ci{\res{dlo}{lo}}{\res{dhi}{hi}} that includes zero, and its upper limit then bounds any component the halves of one item set share.}

\outcome{\Rtwocase}{2}{R2 supported}{The shared-item explanation passes its rule in this family. With bank one as half $A$ and bank two as half $B$ on the same 45 runs, margin inflation is \res{xlam}{cross lambda} with interval \ci{\res{xlo}{lo}}{\res{xhi}{hi}} covering one. The within-minus-cross difference in log squared inflation has an interval \ci{\res{dlo}{lo}}{\res{dhi}{hi}} lying above zero. At least part of the within-bank inflation in PolyPythias comes from a component the two halves of one item set share or from differences between the banks. The cross-bank value of \res{xlam}{cross lambda} is the one we'd report for its run covariance. We can't say whether DataDecide carries the same component, because its cross-bank run needs GPU time we didn't have.}

\outcome{\Rtwocase}{3}{R2 undecided}{The identification check is undecided under its rule. With bank one as half $A$ and bank two as half $B$ on the same 45 runs, margin inflation is \res{xlam}{cross lambda} with interval \ci{\res{xlo}{lo}}{\res{xhi}{hi}}, and the within-minus-cross difference in log squared inflation has interval \ci{\res{dlo}{lo}}{\res{dhi}{hi}}. Bank two holds under a fifth of bank one's items, and that smaller bank widens both intervals.}

\subsection{Concentration by scoring format}
Margin correlations track scoring format, and cross-format inflation is 0.921 against 1.244 for the full matrix. Format and task content aren't separable in this battery. Setting within-format correlations to their mean of 0.624 and cross-format ones to 0.007, keeping benchmark seed variances, gives margin inflation of 1.285 against the observed 1.244 and 1.728 against 1.786 without BoolQ. The fit is in sample, and Appendix~\ref{app:formatfit} checks it with each benchmark left out.

\subsection{Predicting aggregate uncertainty}
\label{sec:prediction}
Covariance models beat independence on margins only when given held-out marginal seed standard deviations. We compared five covariance models and an exploratory rescaled plug-in on five recipe folds, fitting the run-covariance models off fold (Table~\ref{tab:prediction} in Appendix~\ref{app:supplementary}). On margins the rank-one, rank-one plus gain, and full models lower mean squared error of log aggregate standard deviation by about 0.03 from the 0.0358 of independence, and their paired recipe-bootstrap intervals exclude zero. The rank-one interval reaches zero when nonpositive held-out variances are filled from training folds. On accuracy every such interval includes zero, and an operational comparison refitting without held-out marginals separates no model from independence on either scale.

On real comparisons at each size below 1B, we called a pair of recipes when their aggregate gap exceeded two standard errors, computed either under independence or with the estimated covariance, leaving the pair out of both estimates. We scored each call against the pair's ordering at 1B whenever that ordering itself cleared two corrected standard errors. Across the four sizes and both scales, the correction removes between 1 and 12 of the 173 to 223 calls on such pairs and changes the share of wrong calls by at most 0.009, from rates between 0.029 and 0.070, and every recipe-bootstrap interval for that change reaches zero (Table~\ref{tab:decision} in Appendix~\ref{app:decision}). A wrong call here, however, also counts genuine rank changes between sizes, and these rates aren't false-positive rates in the usual sense. On PolyPythias null seed pairs, with deviations from the other seven seeds of one configuration, both rules call 0.133 of margin pairs at two standard errors against a nominal 0.046, or 0.092 for $t$ with six degrees of freedom (Appendix~\ref{app:nullpairs}). In single-run comparisons the correction changes the wrong-call rate by at most 0.019 for runs sharing a seed label and 0.016 for runs that don't, every interval reaching zero. The ratio of seed-matched to seed-unmatched paired-difference standard deviations runs from 0.928 to 1.025 and gives no sign that shared seed labels change the correction's effect.


\section{Related work}
\citet{bouthillier2021} derive variance inflation from correlated trainings on one task, whereas we account for covariance across benchmarks within a replicate run. \citet{reimers2017}, \citet{zhou2020}, \citet{summers2021}, \citet{madaan2024}, and \citet{fehlauer2025} study seed-related variability, \citet{sellam2022} release 25 pretraining seeds of one BERT configuration, and \citet{dodge2020} test whether good initialisations transfer across tasks. \citet{jordan2024} finds run deviations in accuracy nearly independent across shifted ImageNet test sets, a pattern our accuracy interval allows. Our margin covariance, by contrast, sits within scoring formats. \citet{miller2024} gives clustered and paired item-sampling errors with each model treated as fixed, and \citet{henderson2018} and \citet{agarwal2021} resample runs within a task and discard the paired run effects we keep. Apart from such shifted test sets, we know of no study that estimates the covariance of run noise between distinct benchmarks or an effective benchmark count from that covariance.

Our cross-half estimator adapts the split-half construction of \citet{spearman1910,brown1910}, the attenuation correction of \citet{spearman1904}, and the variance components of \citet{searle1992} and generalizability theory \citep{brennan2001}, and we add the estimand, cross-benchmark run covariance at a fixed configuration, which neither \citet{miller2024} nor \citet{heineman2025} estimates, and its measurement on released runs. \citet{cheverud1988} motivates our phenotypic plug-in (Table~\ref{tab:prediction}), and we take the covariance analogy of \citet{martin1977} without its variance components.

Our few-cluster inference follows \citet{cameron2008} and \citet{mackinnon2017}. \citet{heineman2025} relate per-benchmark signal and noise on DataDecide and OLMo to decision accuracy, and their checkpoint noise gives DataDecide's accuracy average an inflation of 1.104, near our 1.078, and OLMo~2's 0.992 against a median seed value of 0.986 over its last 30 checkpoints (Appendix~\ref{app:heineman}). Across models, benchmark scores share low-dimensional structure \citep{ruan2024,burnell2023revealing}, whereas we measure covariance of run deviations within a configuration.

\section{Discussion and limitations}
We read the accuracy result as an upper bound of 1.157 on inflation at the scored checkpoint that leaves independence open, and at accuracy-like item noise this design detects a true 1.10 in 0.135 of replicates. Exploratory cuts excluding one, without BoolQ at 1.558, without the 26 truncated configurations at 1.120, at the adjacent checkpoint and on the held-out tasks, don't settle it.

Apart from the held-out test and the two PolyPythias rules, all fixed before scoring, our study remains exploratory. We didn't score the 375 DataDecide runs on the second bank. The 1.244 thus stays untested on fresh items, and the shared-item check is unrun in the family that supplies our headline. The default 750M runs are scored at 41.5\% of their final step on average. The 33 configurations that share a final step give a margin interval too wide to test the full estimate. Our populations stop at 1B and at likelihood-scored multiple choice, since no larger public multi-seed population shares replicate item scores (Appendix~\ref{app:design}) and generative tasks lack our margin. With no cross-format run covariance, a uniform item effect shared across benchmarks could carry at most 0.319 of the margin excess before implied cross-format inflation passes 1.035. On accuracy, however, such an effect could reproduce 1.078 with a cross-format value of 1.047 inside \ci{0.848}{1.063}, and a format-following component remains unmeasured in DataDecide (Appendix~\ref{app:calibration}).\outcome{\Rtwocase}{1}{R2 not supported}{ The PolyPythias cross-bank check can't reach an item effect that both banks share.}\outcome{\Rtwocase}{2}{R2 supported}{ The cross-bank check in PolyPythias finds such a component in one family, where the within-bank value can overstate run covariance.}\outcome{\Rtwocase}{3}{R2 undecided}{ The cross-bank check in PolyPythias leaves that item effect undecided in one family.}

We cluster on recipes, but the release shares one seed label across every recipe inside a size band, and a run effect following the band would take simulated margin coverage to 0.821 at a quarter of latent variance and 0.580 at a half, although the size-band-clustered margin interval \ci{1.030}{1.426} of Table~\ref{tab:primary} still excludes one. A permutation test that aligns default and auxiliary runs across recipes gives that effect an anti-conservative upper bound of 0.044 of margin run covariance, where simulated coverage is 0.941 at a share of 0.05, and its power is 0.490 at the bound.

Against one configuration's six held-out seeds, a noisy target, independence predicts margin inflation better than either three-seed fit. On the 4,755-item PolyPythias transfer runs at three sizes (Appendix~\ref{app:transport}), its mean absolute log error is 0.339, against 0.536 for the phenotypic plug-in and 0.512 for SNAP with item noise added to both sides. SNAP beats independence against a target pooled across configurations, 0.275 against 0.319 on three-size margins and 0.367 against 0.523 on accuracy, though not on five-size margins, and only that target resembles Section~\ref{sec:prediction}'s recipe folds.

For a fixed released bank the replicate standard deviation of the battery average gives the aggregate uncertainty directly, and we recommend reporting it with a benchmark-removal check. The split adds the item-general value for a redrawn or extended bank, the per-benchmark run variances, and $K_{\mathrm{eff}}$ for planning run counts, and it departs most from the replicate value when item noise fills much of each benchmark's replicate variance, as on accuracy. Without replicates, the format fit of Appendix~\ref{app:formatfit} gives a fallback untested outside this battery. In an equicorrelated population at the observed $\bar r_E$ of 0.061 on margins and 0.018 on accuracy, with generic item noise, three runs each let the margin interval exclude one in 0.997 of replicates and accuracy needs eight to reach 0.803, but at the accuracy battery's own noise three runs detect a true 1.078 in 0.111 of replicates (Appendix~\ref{app:supplementary}).

\label{maintext:end}
\section*{AI use statement}
We used generative AI tools to propose and refine hypotheses, critique the estimator and simulation design, assist with analysis code, revise manuscript text, survey literature, and contribute to the technical and editorial review of this version. The study includes synthetic calibration and sensitivity simulations, whose role differs from the released model outputs used for the empirical analysis. The authors remain responsible for verifying AI-assisted claims, code, citations, and the final submission, including an accurate disclosure of AI involvement in generating simulation data.

\section*{Ethics statement}
This study analyses released model outputs and introduces no new participant recruitment or data collection. Its intended use is to improve uncertainty reporting in model evaluation. An independence approximation used outside the studied battery could understate uncertainty, which keeps the recommendations conditional on the measured population and score definition. Use of derived artifacts must retain the applicable source attribution and licensing requirements.

\section*{Reproducibility statement}
Section~\ref{sec:estimator} and Appendix~\ref{app:derivations} define the estimator and its assumptions. Appendix~\ref{app:implementation} specifies the interval calculation and data reduction. Appendix~\ref{app:design} records the populations and planned checks. Appendix~\ref{app:supplementary} reports later sensitivity analyses alongside the primary results. The released archive also holds full-length versions of the compressed appendix sections. The primary calculation uses 4,999 bootstrap draws, 2,000 calibration replicates, 2,000 replicates for each of the item-group and batch-slot permutation checks and 500 for the run-label permutation bias check, 50 item re-splits, and master seed 20260101, and reproduction requires the per-item reductions, item ordering and half assignments, selected checkpoint identifiers, and the corresponding analysis scripts. We release all four of them alongside the paper. Neither the analysis plan nor the protocol and rule commits carry an independently corroborated timestamp. A separate implementation run on Kaggle reproduces the primary estimates, the BoolQ-removal estimates, and their intervals to three decimals from the released per-item scores, a run that also produced the passage-aware split, the coverage simulation, the operational prediction comparison, and the seed-matched pair analysis. The earlier 4,755-item PolyPythias transfer check, its five-size extension, and the checkpoint results come from execution records that we didn't rerun for the final analysis. The pre-specified test ships with its protocol, the dated amendments that record each change to it, the frozen hash of its request file, and the kernels that scored and analysed it, and the last twenty runs at 1B were scored off Kaggle on three RTX 3090 cards. We score the PolyPythias battery on both banks in float32 with transformers 4.57.1 on Kaggle T4 cards, each run checking its cached scores against an uncached reference on 24 items to within 0.001 nats, and the release will include the hashes of both request files, the bank-two file as a deterministic rebuild from its manifest, with the scoring kernels. Separate CPU kernels on the released runs produced the recipe-decision, seed-count, battery-size, and item-count analyses. Fifteen further CPU kernels behind later sensitivity analyses, among them the null seed-pair, split-against-replicate, rule-power and weighting checks, ship with their scripts, logs and outputs in the archive's rebuttal\_checks folder.

\input{references.tex}
\clearpage
\appendix
\input{appendix_a.tex}
\input{appendices_bcd.tex}
\end{document}
