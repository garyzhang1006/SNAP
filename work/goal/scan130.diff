diff --git a/deliverables/main.tex b/deliverables/main.tex
index 264150a..e37d0ba 100644
--- a/deliverables/main.tex
+++ b/deliverables/main.tex
@@ -13 +13 @@
-\hypersetup{pdfauthor={},pdftitle={Error bars on benchmark averages can miss run covariance}}
+\hypersetup{pdfauthor={},pdftitle={SNAP: Error bars on benchmark averages can miss run covariance}}
@@ -37 +37,3 @@ Error bars on a language model's benchmark average usually sum per-benchmark var
-A benchmark average can change across training runs even when the evaluation items stay fixed. If two runs of one configuration differ on that average, how much of the gap should an error bar expect? For an equally weighted average of $K$ benchmarks, independent run deviations give variance $K^{-2}\operatorname{tr}\Sig$, where $\Sig$ is their cross-benchmark covariance matrix. In other words, the naive independence assumption drops the off-diagonal terms and weakens a comparison's error control. When the true standard deviation is $\Lam$ times the independent one, a two-sided comparison at a nominal 0.05 has size $2\{1-\Phi(1.96/\Lam)\}$ with $\Phi$ the standard normal distribution function. That size is 0.115 at the margin estimate and 0.069 at the accuracy one, and our simulation of two independently trained configurations gives 0.113 (Appendix~\ref{app:supplementary}). Since the correction changes few calls on DataDecide's recipe comparisons, the understatement shows mainly in the error bar of one model's battery average and in planning replicate runs.
+A benchmark average can change across training runs even when the evaluation items stay fixed. If two runs of one configuration differ on that average, how much of the gap should an error bar expect? For an equally weighted average of $K$ benchmarks, independent run deviations give variance $K^{-2}\operatorname{tr}\Sig$, where $\Sig$ is their cross-benchmark covariance matrix. In other words, the naive independence assumption drops the off-diagonal terms and weakens a comparison's error control.
+
+When the true standard deviation is $\Lam$ times the independent one, a two-sided comparison at a nominal 0.05 has size $2\{1-\Phi(1.96/\Lam)\}$ with $\Phi$ the standard normal distribution function. That size is 0.115 at the margin estimate and 0.069 at the accuracy one, and our simulation of two independently trained configurations gives 0.113 (Appendix~\ref{app:supplementary}). Since the correction changes few calls on DataDecide's recipe comparisons, the understatement shows mainly in the error bar of one model's battery average and in planning replicate runs.
@@ -50 +52 @@ A benchmark average can change across training runs even when the evaluation ite
-Figure~\ref{fig:teaser} shows where that covariance sits in the DataDecide battery. Item-general covariance is estimated from two disjoint item halves per benchmark, where both half scores are centred across each configuration's replicate runs and their cross-half products are taken. Under zero-mean item noise and conditional independence across halves, these products estimate run covariance with its diagonal. We adapt the repeated-measurement construction of quantitative genetics and call it SNAP, Seed Noise Across Phenotypes (Appendix~\ref{app:derivations}).
+Figure~\ref{fig:teaser} shows where that covariance sits in the DataDecide battery. We estimate item-general covariance from two disjoint item halves per benchmark, where we centre both half scores across each configuration's replicate runs and take their cross-half products. Under zero-mean item noise and conditional independence across halves, these products estimate run covariance with its diagonal. We adapt the repeated-measurement construction of quantitative genetics and call it SNAP, Seed Noise Across Phenotypes (Appendix~\ref{app:derivations}).
@@ -60 +62 @@ A single model family whose replicates mix default and auxiliary runs on differe
-This paper makes four contributions to the measurement of run covariance. (i) We define the item-general run covariance that cross-half products estimate, identified up to an item component shared across benchmarks and halves. (ii) The replicate standard deviation of the battery average gives the uncertainty of a rerun on the released items and is what we recommend that authors report. The split adds per-benchmark run variances net of item noise and a redrawn-bank target, untested on DataDecide, and its value departs from the replicate one when item noise is large, at 1.558 against 1.285 for accuracy without BoolQ (Section~\ref{sec:estimator}). (iii) Algorithm~\ref{alg:snap} gives the measurement recipe, with near-nominal intervals in simulation unless a run effect is shared within a model size or item noise reaches the top of our swept range, and it tells a planner that, with generic item noise, three runs per configuration resolve margin inflation while accuracy needs eight. (iv) We measure run covariance across the 375 released runs and find it concentrated within scoring formats, where every recipe, recipe-pair and benchmark deletion and four of five size-band deletions leave a margin interval that excludes one.
+This paper makes four contributions to the measurement of run covariance. (i) We define the item-general run covariance that cross-half products estimate, identified up to an item component shared across benchmarks and halves. (ii) The replicate standard deviation of the battery average gives the uncertainty of a rerun on the released items and is what we recommend that authors report. The split adds per-benchmark run variances net of item noise and a redrawn-bank target, untested on DataDecide, and its value departs from the replicate one when item noise is large, at 1.558 against 1.285 for accuracy without BoolQ (Section~\ref{sec:estimator}). (iii) Algorithm~\ref{alg:snap} gives the measurement recipe, with near-nominal intervals in simulation unless the runs of one model size share a run effect or item noise reaches the top of our swept range, and it tells a planner that, with generic item noise, three runs per configuration resolve margin inflation while accuracy needs eight. (iv) We measure run covariance across the 375 released runs and find it concentrated within scoring formats, where every recipe, recipe-pair and benchmark deletion and four of five size-band deletions leave a margin interval that excludes one.
@@ -71 +73 @@ m_{crji}=\frac{\ell(\mathrm{gold}_i)}{\mathrm{bytes}(\mathrm{gold}_i)}-
-Here $\ell$ is the summed answer log-likelihood and $v$ ranges over alternatives, with a zero margin treated as incorrect. Items are averaged per benchmark, with MMLU macro-averaged over its 57 subjects, and we denote the margin and accuracy scores by $y^{\mathrm{marg}}$ and $y^{\mathrm{acc}}$ and call each a trait.
+Here $\ell$ is the summed answer log-likelihood and $v$ ranges over alternatives, with a zero margin treated as incorrect. We average items per benchmark, with MMLU macro-averaged over its 57 subjects, and we denote the margin and accuracy scores by $y^{\mathrm{marg}}$ and $y^{\mathrm{acc}}$ and call each a trait.
@@ -78 +80 @@ y^H_{crj}=\mu^H_{cj}+E_{crj}+\varepsilon^H_{crj}.
-A half-specific mean lets the two item sets differ in difficulty, the component $E_{crj}$ is the run deviation shared by the two item populations, and $\varepsilon^H_{crj}$ carries the remaining item-dependent variation. For the unbiasedness derivation we assume independent, identically distributed run effects within a configuration, an assumption the released runs only approximate (Section~\ref{sec:data}), and we write their covariance as $\Sigma_{E,c}$. This target is defined for a redrawn or extended item bank. A run's item-specific deviations on the released bank fall into $\varepsilon$, and fixed-bank reproducibility is a separate question.
+A half-specific mean lets the two item sets differ in difficulty, the component $E_{crj}$ is the run deviation shared by the two item populations, and $\varepsilon^H_{crj}$ carries the remaining item-dependent variation. For the unbiasedness derivation we assume independent, identically distributed run effects within a configuration, an assumption the released runs only approximate (Section~\ref{sec:data}), and we write their covariance as $\Sigma_{E,c}$. This target applies to a redrawn or extended item bank. A run's item-specific deviations on the released bank fall into $\varepsilon$, and fixed-bank reproducibility is a separate question.
@@ -81 +83 @@ A half-specific mean lets the two item sets differ in difficulty, the component
-Each benchmark's items are partitioned into disjoint halves with a frozen random seed, and that assignment is reused for its replicate runs. Each half score is centred across replicate runs, and we write $d^H_{crj}=y^H_{crj}-R^{-1}\sum_s y^H_{csj}$. If the item errors have conditional mean zero and zero covariance between the two halves across the relevant runs and benchmarks, then
+We partition each benchmark's items into disjoint halves with a frozen random seed, and we reuse that assignment for its replicate runs. We centre each half score across replicate runs, and we write $d^H_{crj}=y^H_{crj}-R^{-1}\sum_s y^H_{csj}$. If the item errors have conditional mean zero and zero covariance between the two halves across the relevant runs and benchmarks, then
@@ -88 +90 @@ Note that Equation~\ref{eq:identity} covers diagonal entries and doesn't require
-Cross-format inflation keeps only the diagonal and the covariances between benchmarks of different scoring formats, and it is 0.921 \ci{0.801}{1.035}. A uniform component shared across benchmarks that carried the whole margin excess of 0.244 above one would lift it to 1.113, and without cross-format run covariance the upper limit admits at most 0.319 of that excess, although negative cross-format run covariance, as the 0.921 point estimate suggests, could offset a larger component (Appendix~\ref{app:calibration}). A format-following component is untested in DataDecide, and a cross-bank rule tests one tied to the item set in PolyPythias. The fixed-bank inflation reported below needs no halves and no cross-half assumption. Only the split's redrawn-bank target and the per-benchmark run variances rest on it.
+Cross-format inflation keeps only the diagonal and the covariances between benchmarks of different scoring formats, and it is 0.921 \ci{0.801}{1.035}. A uniform component shared across benchmarks that carried the whole margin excess of 0.244 above one would lift it to 1.113, and without cross-format run covariance the upper limit admits at most 0.319 of that excess, although negative cross-format run covariance, as the 0.921 point estimate suggests, could offset a larger component (Appendix~\ref{app:calibration}). DataDecide can't test a format-following component, and a cross-bank rule tests one tied to the item set in PolyPythias. The fixed-bank inflation reported below needs no halves and no cross-half assumption. Only the split's redrawn-bank target and the per-benchmark run variances rest on it.
@@ -96 +98 @@ U_c=\frac{1}{K^2(R-1)}\sum_{j,r}d^A_{crj}d^B_{crj}.
-The two statistics in Equation~\ref{eq:tu} have expectations $K^{-2}\mathbf{1}^{\mathsf T}\Sigma_{E,c}\mathbf{1}$ and $K^{-2}\operatorname{tr}\Sigma_{E,c}$. Each is summed across configurations, and one sum is divided by the other to obtain
+The two statistics in Equation~\ref{eq:tu} have expectations $K^{-2}\mathbf{1}^{\mathsf T}\Sigma_{E,c}\mathbf{1}$ and $K^{-2}\operatorname{tr}\Sigma_{E,c}$. We sum each across configurations, and we divide one sum by the other to obtain
@@ -128 +130 @@ Either pooled sum can be nonpositive in a finite sample, and calibration simulat
-Nominal 95\% wild cluster bootstrap-$t$ intervals are computed over the 25 recipe clusters, using 4,999 Rademacher draws and a linearisation of the squared ratio, with the observed denominator held fixed during resampling (Appendix~\ref{app:implementation}).
+We compute nominal 95\% wild cluster bootstrap-$t$ intervals over the 25 recipe clusters, using 4,999 Rademacher draws and a linearisation of the squared ratio, with the observed denominator held fixed during resampling (Appendix~\ref{app:implementation}).
@@ -136 +138 @@ From the ten DataDecide benchmarks ARC-Challenge and ARC-Easy \citep{clark2018ar
-In each configuration the largest checkpoint step that all three runs share is selected. The release's checkpoint index puts this step at 41.5\% of the 750M default runs' final step on average and 87\% at 530M. In 26 configurations, one at 530M and 25 at 750M, the largest final step exceeds the smallest by more than a factor of 1.5. At 750M the default runs stop short of their released final models. The auxiliary runs, however, sit near their final steps in all but one configuration.
+In each configuration we select the largest checkpoint step that all three runs share. The release's checkpoint index puts this step at 41.5\% of the 750M default runs' final step on average and 87\% at 530M. In 26 configurations, one at 530M and 25 at 750M, the largest final step exceeds the smallest by more than a factor of 1.5. At 750M the default runs stop short of their released final models. The auxiliary runs, however, sit near their final steps in all but one configuration.
@@ -138 +140 @@ In each configuration the largest checkpoint step that all three runs share is s
-The full estimate is interpreted as covariance among the released runs at the selected steps. The auxiliary-run contrast, at 1.256, removes the batch component. The 26 severely truncated configurations cover the 750M band and one 530M configuration, and without them margin inflation is 1.276 (\ci{1.114}{1.420}) and accuracy inflation 1.120 (\ci{1.020}{1.208}), a subset we chose after we saw the full-sample estimates. On the 33 configurations that share a final step, margin inflation is 1.082 (\ci{0.121}{1.530}) and accuracy 1.129 (\ci{0.769}{1.397}), intervals too wide to separate the value one from the full-sample estimates, although the auxiliary contrast on full-sample margins, whose runs sit near their final steps, still excludes one. A configuration's margin influence has Spearman correlation $-0.036$ with its selected step's share of the default final step (Appendix~\ref{app:schedule}).
+We interpret the full estimate as covariance among the released runs at the selected steps. The auxiliary-run contrast, at 1.256, removes the batch component. The 26 severely truncated configurations cover the 750M band and one 530M configuration, and without them margin inflation is 1.276 (\ci{1.114}{1.420}) and accuracy inflation 1.120 (\ci{1.020}{1.208}), a subset we chose after we saw the full-sample estimates. On the 33 configurations that share a final step, margin inflation is 1.082 (\ci{0.121}{1.530}) and accuracy 1.129 (\ci{0.769}{1.397}), intervals too wide to separate the value one from the full-sample estimates, although the auxiliary contrast on full-sample margins, whose runs sit near their final steps, still excludes one. A configuration's margin influence has Spearman correlation $-0.036$ with its selected step's share of the default final step (Appendix~\ref{app:schedule}).
@@ -144 +146 @@ We didn't draw the analysis plan's split of screening and estimation recipes. Co
-The full battery is retained for the primary estimand with the benchmarks whose estimated diagonal covariance is negative, since an unbiased moment estimate can be negative near zero and the deletion of such a benchmark would change the estimand.
+We retain the full battery for the primary estimand with the benchmarks whose estimated diagonal covariance is negative, since an unbiased moment estimate can be negative near zero and the deletion of such a benchmark would change the estimand.
@@ -146 +148 @@ The full battery is retained for the primary estimand with the benchmarks whose
-We add a second model family whose replicate design DataDecide lacks. PolyPythias \citep{vanderwal2025} trains nine seeds at each of 14M, 31M, 70M, 160M, and 410M parameters with shared code, data, and hyperparameters and a seed that sets initialisation and data order together, and all 45 runs are scored at the shared final step 143,000 on all 37,682 items. The request text was taken from the DataDecide release, and every prompt matches its scoring byte for byte. Appendix~\ref{app:transport} reports an earlier transfer check on 4,755 items.
+We add a second model family whose replicate design DataDecide lacks. PolyPythias \citep{vanderwal2025} trains nine seeds at each of 14M, 31M, 70M, 160M, and 410M parameters with shared code, data, and hyperparameters and a seed that sets initialisation and data order together, and we score all 45 runs at the shared final step 143,000 on all 37,682 items. We took the request text from the DataDecide release, and every prompt matches its scoring byte for byte. Appendix~\ref{app:transport} reports an earlier transfer check on 4,755 items.
@@ -148 +150 @@ We add a second model family whose replicate design DataDecide lacks. PolyPythia
-For the identification check we built a second bank of 6,808 items from the same ten benchmarks with OLMES at a pinned commit, with the train split for eight benchmarks and the validation split for OpenBookQA and MMLU. Any item whose context matches a bank-one context or whose question appears among its own few-shot exemplars was dropped, and each benchmark apart from MMLU was capped at 600 items. Both PolyPythias rules were committed before either bank was scored.
+For the identification check we built a second bank of 6,808 items from the same ten benchmarks with OLMES at a pinned commit, with the train split for eight benchmarks and the validation split for OpenBookQA and MMLU. We dropped any item whose context matches a bank-one context or whose question appears among its own few-shot exemplars, and we capped each benchmark apart from MMLU at 600 items. We committed both PolyPythias rules before we scored either bank.
@@ -178 +180 @@ The factor belongs to this battery, since margin correlations track scoring form
-Shared passages don't account for BoolQ's trace share, but that check can't reach other shared item effects. The 2,938 passages behind BoolQ's 3,270 questions were recovered from the release's request files, and when we assign each passage whole to one half, the two estimates change by $-0.0002$ and $-0.0001$ on average across fifty re-splits. The BoolQ diagonal moves by 0.03\% and 0.15\% under the same reassignment. A permutation that reshuffles each run's item deviations inside each of the 66 item groups, MMLU's 57 subjects and the other nine benchmarks, leaves all trait scores fixed and breaks the tie between particular items and runs. Across 2,000 such permutations the permuted estimates sit about 0.001 below the observed margin value and 0.002 below accuracy. Item-level sharing thus seems to account for little of either, but an effect carried by every item of a benchmark survives it.
+Shared passages don't account for BoolQ's trace share, but that check can't reach other shared item effects. We recovered the 2,938 passages behind BoolQ's 3,270 questions from the release's request files, and when we assign each passage whole to one half, the two estimates change by $-0.0002$ and $-0.0001$ on average across fifty re-splits. The BoolQ diagonal moves by 0.03\% and 0.15\% under the same reassignment. A permutation that reshuffles each run's item deviations inside each of the 66 item groups, MMLU's 57 subjects and the other nine benchmarks, leaves all trait scores fixed and breaks the tie between particular items and runs. Across 2,000 such permutations the permuted estimates sit about 0.001 below the observed margin value and 0.002 below accuracy. Item-level sharing thus seems to account for little of either, but an effect carried by every item of a benchmark survives it.
@@ -194 +196 @@ The pre-specified rule fails on the four held-out tasks. Held-out margin inflati
-The protocol named SciQ, MedMCQA, and two AGIEval \citep{zhong2024agieval} reasoning tasks, with a pre-specified rule that replaced any task whose 1B pilot accuracy fell below chance plus 0.02, a bar both AGIEval tasks narrowly missed (Appendix~\ref{app:heldout}). The battery became SciQ, MedMCQA, and multiple-choice versions of DROP and CoQA, with 1,000 items in each of the four tasks and items sharing a passage or story kept in the same half. Unfortunately, our GPU budget couldn't cover all 375 released checkpoints, and our protocol's scope rule limited the test to those runs at the steps of the primary analysis. The test passes only if the lower 95\% limit exceeds one, and Table~\ref{tab:heldout} reports the test at its pre-specified scope. Every partial scope read before the full one also failed the rule, with held-out margin estimates of 1.152 to 1.203, and Appendix~\ref{app:heldout} lists all five, with simulated power by number of sizes.
+The protocol named SciQ, MedMCQA, and two AGIEval \citep{zhong2024agieval} reasoning tasks, with a pre-specified rule that replaced any task whose 1B pilot accuracy fell below chance plus 0.02, a bar both AGIEval tasks narrowly missed (Appendix~\ref{app:heldout}). The battery became SciQ, MedMCQA, and multiple-choice versions of DROP and CoQA, with 1,000 items in each of the four tasks and items sharing a passage or story kept in the same half. Our GPU budget couldn't cover all 375 released checkpoints, and our protocol's scope rule limited the test to those runs at the steps of the primary analysis. The test passes only if the lower 95\% limit exceeds one, and Table~\ref{tab:heldout} reports the test at its pre-specified scope. Every partial scope read before the full one also failed the rule, with held-out margin estimates of 1.152 to 1.203, and Appendix~\ref{app:heldout} lists all five, with simulated power by number of sizes.
@@ -232 +234 @@ Over 4,000 replicates that we simulated after we fixed both rules, with seed eff
-\outcome{\Rtwocase}{3}{R2 undecided}{The identification check is undecided under its rule. With bank one as half $A$ and bank two as half $B$ on the same 45 runs, margin inflation is \res{xlam}{cross lambda} with interval \ci{\res{xlo}{lo}}{\res{xhi}{hi}}, and the within-minus-cross difference in log squared inflation has interval \ci{\res{dlo}{lo}}{\res{dhi}{hi}}. Bank two holds under a fifth of bank one's items, and that smaller bank widens both intervals.}
+\outcome{\Rtwocase}{3}{R2 undecided}{The identification check returns undecided under its rule. With bank one as half $A$ and bank two as half $B$ on the same 45 runs, margin inflation is \res{xlam}{cross lambda} with interval \ci{\res{xlo}{lo}}{\res{xhi}{hi}}, and the within-minus-cross difference in log squared inflation has interval \ci{\res{dlo}{lo}}{\res{dhi}{hi}}. Bank two holds under a fifth of bank one's items, and that smaller bank widens both intervals.}
@@ -239 +241 @@ Format and task content aren't separable in this battery. If we set within-forma
-Covariance models beat independence on margins only when given held-out marginal seed standard deviations. Five covariance models and an exploratory rescaled plug-in of observed score correlations were compared on five recipe folds, with the run-covariance models fitted off fold (Table~\ref{tab:prediction} in Appendix~\ref{app:supplementary}). On margins the rank-one, rank-one plus gain, and full models lower mean squared error of log aggregate standard deviation by about 0.03 from the 0.0358 of independence, and their paired recipe-bootstrap intervals exclude zero. The rank-one interval, however, reaches zero when nonpositive held-out variances are filled from training folds. On accuracy every such interval includes zero, and an operational comparison that refits without held-out marginals separates no model from independence on either scale.
+Covariance models beat independence on margins only when given held-out marginal seed standard deviations. We compared five covariance models and an exploratory rescaled plug-in of observed score correlations on five recipe folds, with the run-covariance models fitted off fold (Table~\ref{tab:prediction} in Appendix~\ref{app:supplementary}). On margins the rank-one, rank-one plus gain, and full models lower mean squared error of log aggregate standard deviation by about 0.03 from the 0.0358 of independence, and their paired recipe-bootstrap intervals exclude zero. The rank-one interval, however, reaches zero when we fill nonpositive held-out variances from training folds. On accuracy every such interval includes zero, and an operational comparison that refits without held-out marginals separates no model from independence on either scale.
@@ -241 +243 @@ Covariance models beat independence on margins only when given held-out marginal
-On real comparisons at each size below 1B, a pair of recipes was called when their aggregate gap exceeded two standard errors, computed either under independence or with the estimated covariance, and neither estimate uses the pair. Each call was scored against the pair's ordering at 1B whenever that ordering itself cleared two corrected standard errors. Across the four sizes and both scales, the correction removes between 1 and 12 of the 173 to 223 calls on such pairs and changes the share of wrong calls by at most 0.009, from rates between 0.029 and 0.070, and every recipe-bootstrap interval for that change reaches zero (Table~\ref{tab:decision} in Appendix~\ref{app:decision}). Note that a wrong call also counts genuine rank changes between sizes, and these rates aren't false-positive rates in the usual sense.
+On real comparisons at each size below 1B, we called a pair of recipes when their aggregate gap exceeded two standard errors, computed either under independence or with the estimated covariance, and neither estimate uses the pair. We scored each call against the pair's ordering at 1B whenever that ordering itself cleared two corrected standard errors. Across the four sizes and both scales, the correction removes between 1 and 12 of the 173 to 223 calls on such pairs and changes the share of wrong calls by at most 0.009, from rates between 0.029 and 0.070, and every recipe-bootstrap interval for that change reaches zero (Table~\ref{tab:decision} in Appendix~\ref{app:decision}). Note that a wrong call also counts genuine rank changes between sizes, and these rates aren't false-positive rates in the usual sense.
@@ -256 +258 @@ We read the accuracy result, which leaves independence open, as an upper bound o
-The study is exploratory apart from the held-out test and the two PolyPythias rules, which we fixed before scoring\outcome{\Ronecase}{1}{R1 pass}{, and the held-out test failed}\outcome{\Ronecase}{2}{R1 fail}{, and the held-out test and the first rule both failed}. The shared-item check is unrun in the family that supplies our headline, whose 1.244 thus stays untested on fresh items. The default 750M runs are scored at 41.5\% of their final step on average. The 33 configurations that share a final step give a margin interval too wide to test the full estimate.
+The study is exploratory apart from the held-out test and the two PolyPythias rules, which we fixed before scoring\outcome{\Ronecase}{1}{R1 pass}{, and the held-out test failed}\outcome{\Ronecase}{2}{R1 fail}{, and the held-out test and the first rule both failed}. The shared-item check is unrun in the family that supplies our headline, whose 1.244 thus stays untested on fresh items. We score the default 750M runs at 41.5\% of their final step on average. The 33 configurations that share a final step give a margin interval too wide to test the full estimate.
@@ -260 +262 @@ The populations stop at 1B and at likelihood-scored multiple choice, since no la
-Intervals are clustered on recipes, but the release shares one seed label across every recipe inside a size band, and a run effect following the band would take simulated margin coverage to 0.821 at a quarter of latent variance and 0.580 at a half, although the size-band-clustered margin interval \ci{1.030}{1.426} of Table~\ref{tab:primary} still excludes one. A permutation test that aligns default and auxiliary runs across recipes gives that effect an anti-conservative upper bound of 0.044 of margin run covariance, where simulated coverage is 0.941 at a share of 0.05, and its power is 0.490 at the bound.
+We cluster intervals on recipes, but the release shares one seed label across every recipe inside a size band, and a run effect following the band would take simulated margin coverage to 0.821 at a quarter of latent variance and 0.580 at a half, although the size-band-clustered margin interval \ci{1.030}{1.426} of Table~\ref{tab:primary} still excludes one. A permutation test that aligns default and auxiliary runs across recipes gives that effect an anti-conservative upper bound of 0.044 of margin run covariance, where simulated coverage is 0.941 at a share of 0.05, and its power is 0.490 at the bound.
