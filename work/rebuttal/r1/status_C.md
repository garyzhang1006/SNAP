# Status, section C (round 1)

Slice length: 9,475 characters in slice_C.orig.tex, 9,040 in slice_C.new.tex.

- R1-11 FIXED: the noise-2.00 simulation and earlier-scope sentences left the main text (app:heldout already reports 530M, 530M+750M, the 68-configuration interim, 1B alone at 1.152 and 750M alone at 1.203, plus the 2.00 widths and pass rate), replaced by one sentence giving the 1.152 to 1.203 range across all five partial scopes (compute extra/heldout/outputs/k04-interim-1b 1.1521, k04-heldout-750m 1.2029), and the width sentence and "can't rescue" sentence stay. Nothing was moved, so APP has no work here.
- R1-13 FIXED: the BoolQ paragraph opens by tying the factor to this battery through the format correlations (0.624 within, 0.007 across, main.tex l.226, cited via Figure~\ref{fig:covariance} because 4.4 has no label), and "A larger factor accompanies a less variable average" became the margin aggregate SD fall from 0.00572 to 0.00516 (research/outputs/snap-r2-grouped/snap-r2-grouped.log:64, aggregate_sd 0.0051647 at lambda 1.7856), merged with the old closing sentence on marginal variances.
- R1-29 FIXED: strict inequalities replaced by "At the accuracy upper limit of 1.157 this fixed battery has r_E of 0.038 and K_eff of 7.47". The old sentence said "That upper endpoint" right after the accuracy value, and 10/1.157^2 = 7.47 confirms it is the accuracy limit, so the text now names it.
- R1-39 FIXED: Table 1 gains "Size-band clusters" rows for margin (1.244, 1.030 to 1.426) and accuracy (1.078, 0.961 to 1.184), with the caption noting the marked rows (numbers from main.tex l.126 and appendices_bcd.tex l.379). Section B's owner can now drop the last sentence of l.126.
- R1-44 FIXED: the reliability paragraph shrank to one sentence that keeps the 0.652/0.324 means and points to Table~\ref{tab:original3} and Appendix~\ref{app:supplementary}, which already hold the per-benchmark reliabilities and the 1.296 / -0.55 diagnostic (appendices_bcd.tex l.53, l.84). No move needed.
- R1-46 FIXED: the sentence carrying 0.926 and 0.524 left the main text under R1-11. APP note: the supplement's tab:rulepower (supplement_extended_body.tex l.716) still prints 0.926, while k12-rule-power-n073 gives 0.9265.
- R1-52 FIXED, with a different fix from the one proposed: no removal table exists in app:composition (appendices_bcd.tex has no single-benchmark removal table), so the caption drops the source phrase instead of pointing there. The figure already shows every removal with its interval.
- R1-63 REJECTED: extending the failed registered test to all 375 runs after seeing its result is the forking path that registration exists to prevent, and it would need GPU.
