R1-02 FIXED: abstract now says cross-half products remove item noise the halves don't share, that an item effect shared across benchmarks and halves would enter, and that a 0.124 share reproduces the margin value (main.tex:75, 271); contributions say "identified up to an item component shared across benchmarks and halves".
R1-03 FIXED: intro P1 adds the paired-comparison sentence (median ratio 1.024, main.tex:234; wrong-call change at most 0.009) with the seed-label point worded as context, and says the covariance matters more for a single battery average's error bar than for seed-matched comparisons.
R1-07 FIXED: abstract cut from about 289 to about 214 words (one outcome branch), dropping the test-inversion parenthetical, the 6.5-benchmark clause, the 1.786 clause, the 1.240 comparison, the timestamp clause and the 0.009 clause.
R1-09 FIXED: the deletion claim left the abstract, and the contributions now read "every recipe, recipe-pair and benchmark deletion, along with four of the five size-band deletions, leaves an interval that excludes one (dropping 530M gives a lower limit of 0.993)" (main.tex:171).
R1-10 FIXED: abstract and final intro paragraph read "Apart from the registered test and the two PolyPythias rules, every analysis is exploratory"; the timestamp and screening-split sentences were removed from both (facts stay with section E's limitations).
R1-12 FIXED: 6.5-benchmark clause removed from the abstract; contributions call it a within-battery count that doesn't rank batteries, since removing BoolQ raises the factor to 1.786 while the aggregate SD falls from 0.00572 to 0.00516 (research/outputs/snap-r11-removal/r11_removal.json, margin removed=null and removed=boolq).
R1-15 FIXED: abstract names the estimand as released runs at the largest checkpoint step shared within each of 125 configurations, whose replicates differ in seed and partly in checkpoint schedule; intro P3 carries the same scoring step.
R1-22 FIXED: intro P3 adds that margins carry more seed signal (reliability 0.652 against 0.324, main.tex:165, Section~\ref{sec:composition}), so margins are primary and accuracy is read as a bound; no magnusson2025 citation added for it.
R1-28 FIXED: "bounds accuracy inflation at 1.157" replaced by "which includes one at the scored checkpoint" (shorter form of the suggested wording, since 1.157 is already the interval's upper end in the same sentence).
R1-35 FIXED: deleted "We keep the name though..." and the phenotype/trait gloss; SNAP now introduced as an adaptation of the quantitative-genetics repeated-measurement construction with a pointer to Appendix~\ref{app:derivations}; the factor is introduced once as "margin inflation $\Lam$", defined as the ratio of the average's SD to its independent value.
R1-40 FIXED: final paragraph now says the 0.101 comes from "a separate transfer simulation".
R1-45 FIXED: the R1-fail abstract branch now appends a nested \outcome{\Rtwocase} clause that reports the disjoint-bank result under each R2 case.
R1-60 REJECTED: the author block is anonymous and pdfauthor is empty (main.tex:13, 15), outside slice A anyway.

Other cuts to hold length: dropped the duplicated "We read the full-battery accuracy estimate as a bound..." sentence, the 0.111 simulation rate, the duplicated 1.10/0.135 contribution sentence, and the "every interval for the wrong-call change reaches zero" clause (still in Sec 4.5).
Length: source 8246 -> 8277 chars (the extra is the nested R1-fail/R2 branch, which never renders alongside the R1-pass branch); rendered text for the R1-pass/R2-not-supported case 7228 -> about 6990 chars.
