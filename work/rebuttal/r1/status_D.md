# Section D status, round 1

Rule check: R2 wording now matches SNAP commit 6cfedee (compute2/README.md and analysis/estimates.py verdicts()). "Supported" fires when the difference interval's lower limit exceeds zero and the cross-bank interval covers one (lo <= 1 <= hi); "not supported" when the cross-bank lower limit exceeds one and the difference interval includes zero. The difference is computed on log theta, which is log squared inflation, so every "difference in log inflation" in the slice now reads "log squared inflation". The rules themselves are unchanged.

- R1-04: FIXED. Added a closing sentence to the decision paragraph saying recipes in a size band share seed labels and that the median paired-difference ratio of 1.024 suggests the covariance mostly cancels in seed-matched gaps, worded as an explanation per R1-20. The paired-ratio sentence already sat directly before the decision paragraph, so no move was needed. Seed-label sharing is documented in supplement_extended_body.tex:645.
- R1-17: FIXED. Replaced the "can't account for" sentence in the R2 not-supported branch with "no evidence" wording and a \pending{hi} bound; moved the bank-differences sentence into the rule paragraph so it applies to every branch, and added that bank two draws most items from train splits, which pretraining data are more likely to contain.
- R1-18: NEEDS-EXPERIMENT (CPU). Text part done ("positive" now reads "that difference interval lies above zero", matching verdicts()). Still needed: extend research/outputs/snap-r1-power (r1_power.py) with a bank-two half at one sixth the items and report not-supported/supported/undecided rates at zero and 0.124 shared-item share.
- R1-19: FIXED. Rule paragraph now gives the 1.026 to 1.456 middle 90% (research/outputs/snap-r1-power/RESULT.txt) and a reading labelled as added after the rule was fixed (a failure inside that range bounds the effect, below 1.026 counts against transfer); the R1-fail branch now cites that reading with a \pending choice and drops the duplicated range sentence.
- R1-20: NEEDS-EXPERIMENT (CPU). Seed-mismatched recipe-pair rerun of the k05-decision logic on saved per-item scores; the R1-04 sentence stays hedged until it exists.
- R1-31: FIXED. "the gap comes from BoolQ" now "most of that gap comes from BoolQ" with the nine defined traits named; added that nine of ten leave-one-out predictions overstate (appendices_bcd.tex:136) and changed "could therefore approximate" to "could at most approximate".
- R1-32: FIXED. Replaced 1.406 (source pipeline, appendices_bcd.tex:335) with 1.414 from the same rescore as 1.262 (research/outputs/snap-r12-analysis/r12_analysis.json, Lambda 1.41372) and labelled the checks as an earlier nested-sample rescore superseded by Section 4.3. The move to app:transport (cut C4) is left to the coordinator.
- R1-33: FIXED. Cut the gain-adjustment and degenerate-proxy sentences and replaced them with a one-line pointer to app:proxy (1.275 and the 99.99% figure remain in appendices_bcd.tex).
- R1-47: FIXED. Simulation now says the covariance was clipped to positive semidefinite, item noise came from the DataDecide margin reliabilities, and WinoGrande's reliability was set to 0.5 (RESULT.txt).
- R1-50: NEEDS-EXPERIMENT (CPU). Masked-matrix bootstrap JSON for the 0.801 to 1.035 interval; no text change.
- R1-51: FIXED. Table~\ref{tab:prediction} now points to Appendix~\ref{app:supplementary}.

Other edits for length (no numbers changed): tightened the R1-pass batch sentence, merged the format-fit sentences, dropped the 0.39 to 1.40 percentiles (still in appendices_bcd.tex:185), fixed the Heineman triad, shortened the operational-comparison clause and the R2-undecided sentence. Slice length 11,566 to 11,553 characters.
