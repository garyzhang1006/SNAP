# Slice B status (main.tex 46-130), round 6

Word count (raw wc -w, no \citep added): before 1700, after 1720, cap 1720.

- R6-03 FIXED (+14, of which -1 from dropping "possible" in the same sentence): "...failed under the plan's default simulation settings, while a simulator matched to each benchmark's variance components puts the gain's share near six percent (Appendix~\ref{app:design})." Source: appendices_bcd.tex:64 "six percent at the estimated scale of 0.042" and :373 (matched simulator copies each benchmark's margin and variance components). Kept "default simulation settings" because round 5 removed the undefined term "default simulator".
- R6-01 FIXED (net 0; "acting alone" dropped since "carried the whole margin excess" already states the whole-excess case): "A uniform component shared across benchmarks that carried the whole margin excess of 0.244 above one would lift it to 1.113, and without cross-format run covariance the upper limit admits at most 0.319 of that excess, although negative cross-format run covariance, which 0.921 implies, could offset a larger component". Offset caveat kept. No "leaves" wording existed at main.tex:71, so the 1.035 limit stays as printed in the preceding sentence's interval.
- R6-13 FIXED (+5): "A configuration's margin influence has Spearman correlation $-0.036$ with its selected step's share of the default final step", matching appendices_bcd.tex:11.
- R6-16 FIXED (+1, with -1 at the later use): main.tex:49 "which adds a batch component beyond the seed."; main.tex:121 "removes the batch component".
- R6-08 FIXED (+1): "A format-following component is untested in DataDecide, and Section~\ref{sec:family}'s cross-bank rule tests one tied to the item set in PolyPythias." No claim about template identity.
- R6-22 SKIPPED (cap reached; cheapest wording ", which BoolQ's high-variance terms drive" costs +5).
