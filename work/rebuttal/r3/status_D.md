# Slice D status (round 3)

Word counts (`wc -w`): slice_D.orig.tex 2457, slice_D.new.tex 2455; main-text part (slice lines 1-43, to \label{maintext:end}) 2006 before and 2006 after. AI-use and ethics statements (slice 44-49) are byte-identical. All six \outcome branches and every \pending and \label are kept. The file has no em or en dashes and no "registered".

- R3-01 FIXED (l.237): "On the released item noise a uniform item effect shared across benchmarks can't produce the margin value (Section~\ref{sec:estimator}), while on accuracy a share of 0.053 of item-noise variance would reproduce 1.078 and the accuracy cross-format interval \ci{0.848}{1.063} doesn't exclude the 1.047 it implies, and a component that follows scoring format remains unmeasured in DataDecide (Appendix~\ref{app:calibration})." The 0.124/0.009 shares and the 0.05/0.007 simulated figures are gone from the main text. Source: results/snap-r3-r2-02/r2_02_shared_component_crossformat.json (accuracy implied_share_q_star 0.05256, induced_cross_format_diagnostic 1.04688, observed cross_format_wild [0.84840, 1.06344]); 1.078 is the printed accuracy value (results/snap-r3-r2-26 split_half 1.0784).
- R3-02 FIXED (l.251): "We score the PolyPythias battery on both banks in float32 with transformers 4.57.1 on Kaggle T4 cards, each run checking its cached scores against an uncached reference on 24 items to within 0.001 nats, and the release will include the hashes of both request files, the bank-two file as a deterministic rebuild from its manifest, with the scoring kernels."
- R3-03 FIXED (l.237): "..., although the fixed-bank value of 1.237 (Section~\ref{sec:estimator}) needs no such check for the released bank." (1.237 as printed at main.tex:63; results/snap-r3-r2-26 full_bank_uncorrected 1.2375.)
- R3-04 FIXED (l.241): "For a fixed released bank the replicate standard deviation of the battery average gives the aggregate uncertainty directly, and we recommend reporting it with a benchmark-removal check, while the split adds the item-general value for a redrawn or extended bank, the per-benchmark run variances, and $K_{\mathrm{eff}}$ for planning run counts." and "The ratio itself belongs to this battery."
- R3-05 FIXED (l.205): "while under independence a uniform item share of 0.124, which on this family's noise lifts the median within-bank estimate only to 1.046, gives supported in 0.033 and undecided in 0.957, so the simulation never tests the rule against a component large enough to explain the inflation, and not supported can't exclude one." Source: results/snap-r2-r1-18/r1_18_rule_two.json, independence_share0124 within_lambda_median 1.04579, supported 0.03275, undecided 0.957. "under independence" kept from the original because the scenario is independence_share0124.
- R3-07 FIXED (l.237, l.251): "Apart from the pre-specified test, which failed at its pre-specified scope," and "The pre-specified test ships with its protocol".
- R3-14 FIXED (l.230): "and what we add is the estimand, run covariance across benchmarks at a fixed configuration, and its measurement on released pretraining runs, which \citet{miller2024} and \citet{heineman2025} don't estimate."
- R3-15 FIXED (l.228): "finds run deviations in accuracy nearly independent across shifted ImageNet test sets" and "Apart from such shifted test sets, we know of no study that estimates the covariance of run noise between distinct benchmarks or an effective benchmark count from that covariance."
- R3-16 FIXED (l.241): "Against one configuration's six-run margin ratio on the 4,755-item three-size PolyPythias transfer runs of Appendix~\ref{app:transport}, ... while against a target pooled across configurations at three sizes the decomposition beats independence, 0.275 against 0.319 on margins and 0.367 against 0.523 on accuracy, and only this pooled target resembles the recipe folds of Section~\ref{sec:prediction}." Values checked in research/outputs/snap-r12-predict-pooled/r12_predict_pooled.json, sets.three_sizes (margin 0.2754/0.3195, accuracy 0.3667/0.5226). The duplicate trailing (Appendix~\ref{app:transport}) was dropped since the sentence names it earlier.
- R3-17 FIXED (l.237): the closing sentence "A scalar component shared by every benchmark would also enter the cross-format pairs ... scoring format." is deleted; the three \outcome{\Rtwocase} sentences stay.
- R3-19 FIXED (l.205): "In an exploratory reading we added after fixing that rule, a failing estimate at or above 1.026 is consistent with the DataDecide value and its jackknife upper limit bounds inflation in this family, while one below 1.026 counts against transfer." R1-fail branch unchanged.
- R3-20 FIXED (l.251): "The earlier 4,755-item PolyPythias transfer check, its five-size extension, and the checkpoint results come from execution records that we didn't rerun for the final analysis." (second "earlier" dropped to avoid repetition), and ", and the plan's screening split was never formed" is cut.
- R3-22 FIXED (l.241): "In a population with the released item counts and equicorrelated seed covariance at the observed mean correlation, three runs each let the margin interval exclude one in 0.997 of replicates". Source: compute extra/heldout/outputs/k08-seed-count/seed_count_power.json (observed_rbar_e 0.0608, runs 3 excludes_one_share 0.99667).
- R3-34 FIXED (l.235): "The full-battery accuracy interval includes one, and at accuracy-like item noise this design detects a true ratio of 1.10 in only 0.135 of replicates, and the primary analysis gives an upper limit of 1.157 at the scored checkpoint." and "without BoolQ at 1.558, without the 26 truncated configurations at 1.120, at the adjacent checkpoint and on the held-out tasks" (1.120 from main.tex:123).
- R3-35 FIXED (l.224): "so shared seed labels don't change the correction's effect."
- R3-40 FIXED (l.228): "\citet{miller2024} gives clustered and paired item-sampling errors with each model treated as fixed".

Cuts paying for the additions (main text): the brief's two named cuts ("A masked matrix needn't stay positive semidefinite, and" at l.218, now "Format and task content aren't separable here."; the 1B-alone cluster-$t$ sentence at l.239), plus three sentences the appendix already carries: "With equal variances the same correlations would give 1.803, a gap mostly due to BoolQ's 68\% trace share." (l.218; appendices_bcd.tex:136), the 1,445-pair median-ratio sentence opening l.224 (appendices_bcd.tex:185), and ", and a separate transfer simulation to a battery whose own ratio is 1.5 leaves it at 0.101" (l.241; appendices_bcd.tex:222). Minor trims: "However," at l.222 and "we haven't tested" to "untested" at l.241. In the reproducibility statement, "an extended supplement with the full-length versions" became "full-length versions".
