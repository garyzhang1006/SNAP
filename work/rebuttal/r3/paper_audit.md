# Round 3 paper-audit (gate mode, reviewer pass)

Target: `deliverables/main.tex` (258 lines), read with `appendix_a.tex` / `appendices_bcd.tex` only to check cross-references and numbers. Execution: sequential single-agent, read-only, no compile. Script run: `audit.py main.tex --mode gate` with `PYTHONDONTWRITEBYTECODE=1`.

## Gate decision: PASS (no submission blockers), with advisories

The script verdict was FAIL, and every blocker it raised is a known false positive. The 37 "undefined reference" blockers point at labels defined in the two appendix files or in the external supplement through `xr`, and `main.log` has 0 undefined references and 0 non-font warnings apart from one `h`-to-`ht` float note. The "author information" check fires on `\author{Anonymous authors}`, which is anonymous by design. EIC screening found no desk-reject condition, since scope fits ICLR, the template is the 2027 style, and the page limit holds per the verified single-outcome builds. Script advisories worth keeping are folded into PA15 (acronym and citation flag) and none of the other presubmission items (ties before `\citet`, unreferenced equation labels, long paragraphs) would change a score.

The one finding likely to cost real score is PA1, where the discussion's own recommendation leaves a reader unsure what SNAP adds once replicates exist. PA2, PA13 and PA20 are cross-section inconsistencies a careful reviewer can catch by comparing numbers.

## Findings

| id | severity | likely to lower score | quote (line) | problem | fix |
|---|---|---|---|---|---|
| PA1 | major | yes | "We recommend paired replicate scores on the chosen battery, which give the aggregate seed standard deviation directly" and "Our ratio therefore measures this battery and helps only where replicates are missing and the target battery's own ratio is close." (241) | SNAP itself needs replicate runs, and line 63 shows the full-bank replicate SD already gives 1.237 against the split's 1.244, so the closing paragraph tells the reader that replicates answer the question directly and that the ratio helps only where replicates are missing, which is where SNAP cannot run. Together with the transfer result in the same paragraph (independence 0.339 beats the decomposition 0.512), the discussion undercuts the method's practical value, and the contribution paragraph (45) never says what the split buys over direct replicate SD. | State once, in the contribution paragraph and in the recommendation, what the split adds over a raw replicate SD: separation of run covariance from item noise so the factor refers to a redrawn bank (line 63 already says this target transfers), the per-benchmark diagonal, and $K_{\mathrm{eff}}$ for planning. Rephrase the recommendation as "report the replicate $\sigma_{\mathrm{agg}}$ directly, and use the SNAP decomposition to plan run counts and compare batteries", and drop "helps only where replicates are missing". |
| PA2 | moderate | yes | "We find covariance gains on margins and no separation on accuracy." (222) vs "independence predicts with mean absolute log error 0.339 against 0.536 for the plug-in and 0.512 for our decomposition" (241) | Section 4.5 reports gains for covariance models, and the discussion reports the decomposition losing to independence, with no sentence connecting the two (different family, per-configuration six-run target). A reviewer reading both will see a contradiction. The phrase "the earlier three-size PolyPythias runs" (241) is also a dangling pointer, since the main text introduces that data only as "A smaller earlier transfer check on 4,755 items" (129) without the three-size detail. | Add one clause in line 241 naming the target difference ("unlike the within-DataDecide folds of Section 4.5, this target is one configuration's six-run ratio, whose own 90% range is 0.70 to 1.85"), and call the data "the 4,755-item three-size transfer check of Section 3" so the pointer resolves. |
| PA3 | moderate | yes | "BoolQ carries 68\% of the covariance trace and single-benchmark removals move the factor from 1.096 to 1.786, so batteries compare through the aggregate standard deviation." (29) | The "so" joins a composition fact to a recommendation that only follows from the trace-normalisation argument of line 91, which the abstract never gives, so the sentence reads as a non sequitur in the most-read paragraph. | "...move the factor from 1.096 to 1.786, and because the factor is relative to each battery's own variance, we compare batteries through the aggregate standard deviation." |
| PA4 | moderate | yes | "A shared item component at 0.124 of item-noise variance would reproduce 1.244." (29) | The abstract states the headline could be reproduced by a nuisance component and gives no counterweight, while the body has two (cross-format diagnostic 0.921 constrains a uniform component, line 71, and the cross-bank rule tests a format-following one, line 203). As written it is self-weakening, because a reader stops at "would reproduce 1.244". | Pair it in one sentence with the constraint, for example "An item component shared across benchmarks at 0.124 of item-noise variance would reproduce 1.244, and cross-format pairs, which give 0.921, rule out a uniform component of that size." |
| PA5 | minor | no | "Cross-half error correlation of 0.1 and the accuracy-like null population bias the estimate down by 0.011 to 0.016, so finite-sample bias works against finding an excess." (113) | Positive cross-half error correlation adds to the cross-half products, so a downward bias is counterintuitive and unexplained, and "so finite-sample bias works against finding an excess" generalises from two simulated populations to the estimator. | Add the mechanism (the correlated error enters only the diagonal terms, which pulls the ratio toward one) and scope the conclusion to "in these populations". |
| PA6 | moderate | yes | "In an exploratory reading we added after fixing that rule, a failing estimate inside the simulated range is a bound and one below 1.026 counts against transfer." (205) | "is a bound" does not say what is bounded or in which direction, and this sentence governs how the pending R1-fail variant (209) will be read. A reviewer will also note that the reading rule was added after the rule was fixed, which invites a post-hoc criticism on exactly the result still pending. | Say "a failing estimate at or above 1.026 is consistent with the DataDecide value, and we report its jackknife upper limit as a bound on inflation in this family, while one below 1.026 counts against transfer", and keep the "exploratory" label next to it. |
| PA7 | minor | no | "However, the two point estimates differ by 0.024" (175) | The quoted estimates are 1.240 and 1.217, which differ by 0.023, and a reader checking the arithmetic will find the mismatch. | Write 0.023, or give the unrounded estimates. |
| PA8 | moderate | yes | "Cross-format covariances alone give inflation 0.921 \ci{0.801}{1.035}" (71), repeated at 218 and "whose diagnostic of 0.921" (237); "averaging 0.624 within formats and 0.007 across them" (159, again 218) | The same diagnostic appears in three sections and the within/cross correlations twice, and Section 4.4 (218) mostly restates 4.1 plus the format fit. Other repeats: the 0.135 power figure (39, 235), the never-formed screening split (125, 251), and the 1.217/1.240 comparison (41, 175, with the same "fixed before any held-out score existed" clause). After heavy editing this reads as padded, and it costs space that PA1 needs. | Keep the 0.921 numbers once (Section 4.4) and replace the line-71 instance with a pointer. Fold Section 4.4 into the end of 4.1 as the format-fit paragraph. Drop the second statement of the screening split in the reproducibility statement. |
| PA9 | moderate | yes | "Centring removes any item effect constant across runs, so only a run-by-item-set deviation shared by both halves enters the numerator, such as a run's deviation on every item of one prompt template, which recurs on any redrawn bank in that format and belongs to fixed-battery run covariance, unlike a component tied to the items both halves share." (71) | This is the identification argument the abstract (PA4) and the R2 rule depend on, packed into one 60-word sentence whose "unlike" clause carries the key distinction. Most readers will not come away knowing which component is target and which is nuisance. | Split into two sentences: one saying a run-by-format deviation (for example one prompt template) is part of the target, because it recurs on any redrawn bank, and one saying that a run deviation tied to items that both halves share is nuisance, and that the estimate is identified only up to that nuisance component. |
| PA10 | minor | no | "On the released bank, the replicate standard deviation of the full-bank average gives inflation 1.237 \ci{1.137}{1.331} on margins" (63) | The sentence does not say where the independent-value denominator comes from when no split is used, so the comparison with 1.244 is hard to interpret. | Add "with the independent value from the split trace" or whatever denominator was used, plus an appendix pointer. |
| PA11 | minor | no | "intervals too wide to separate one from the full-sample estimates" (123) | "one" reads as a pronoun, when the intended meaning is the value 1. | "intervals too wide to distinguish the value one from the full-sample estimates". |
| PA12 | minor | no | "The full-battery accuracy interval includes one at accuracy-like item noise, a design that would detect a true ratio of 1.10 in 0.135 of replicates" and "Exploratory cuts whose intervals exclude one, without BoolQ at 1.558, at the adjacent checkpoint and on the held-out tasks, don't settle it." (235) | The modifier "at accuracy-like item noise" attaches to the interval rather than to the power figure. The list of accuracy cuts that exclude one leaves out the truncation-dropped subset, 1.120 \ci{1.020}{1.208} (123), and the BoolQ weight of 0.0867 (159), so the discussion under-reports evidence against the reading of accuracy as a bound. | "The full-battery accuracy interval includes one, and at accuracy-like item noise this design detects a true ratio of 1.10 in only 0.135 of replicates." Add the truncation subset to the list of cuts. |
| PA13 | moderate | yes | "The PolyPythias transfer, five-size rescore, and checkpoint results come from earlier execution records that we didn't rerun for the final analysis." (251) | "five-size rescore" collides with the new five-size PolyPythias battery that Section 4.3's registered rules read (129, "we score all 45 runs ... on all 37,682 items"), so a reviewer can read this as the rule-read data not having been rerun. The appendix (appendices_bcd.tex:339) shows the phrase means the earlier 4,755-item extension. | "The earlier 4,755-item PolyPythias transfer check, its five-size extension, and the checkpoint results come from earlier execution records..." |
| PA14 | minor | no | "at a design with 40 within-size contrasts" (29) vs "give 40 within-configuration contrasts against 250 in DataDecide" (209) | The same quantity has two names, and only "within-configuration" matches the DataDecide count of 250. | Use "within-configuration" in the abstract. |
| PA15 | moderate | yes | "DataDecide supplies the ten benchmarks ARC-Challenge, ARC-Easy, BoolQ, CommonsenseQA, HellaSwag, MMLU, OpenBookQA, PIQA, Social IQa, and WinoGrande." (119), also SciQ, MedMCQA, DROP, CoQA (41), AGIEval (177), OLMES (43, 131) | `references.tex` has no entry for any of these 14 benchmarks, AGIEval, or the OLMES harness, and the script's acronym check flagged DROP, OLMES, ARC and MMLU. ICLR reviewers expect dataset citations, and one or more may say so. | Add the standard citations at first mention (119, 41, 131). `teamolmo2025` is already in the bibliography if it is the intended OLMES source, otherwise cite the OLMES paper. |
| PA16 | minor | no | "every recipe, recipe-pair and benchmark deletion, along with four of the five size-band deletions, leaves an interval that excludes one" (45) | The recipe-pair deletion claim appears only in the contribution list and in appendices_bcd.tex:379, and Section 4.1 (163) reports recipe and size ranges but not pairs, so the page-2 claim has no main-text support. | Add "and every recipe-pair deletion also excludes one (Appendix~X)" to line 163. |
| PA17 | minor | no | "This paper makes four contributions to measuring run covariance." (45) | In the default build page 1 ends after the SNAP-naming paragraph, and the headline 1.244 in the intro plus the contribution list fall on page 2. The single-outcome abstract is shorter, but the contribution list sits after three more paragraphs and probably stays on page 2 (unverified, no compile). The abstract does carry the contribution. The list also omits the PolyPythias replication and cross-bank check, which is the paper's second pre-specified test. | Optional: fold the second-family test into the fourth contribution ("we register tests on unseen tasks and on a second model family"). No move is needed for page-1 visibility, since the abstract covers it. |
| PA18 | minor | no | "a bar both AGIEval tasks missed by about a thousandth, inside the pilot's sampling error of about 0.010" (177) and "Interval width rather than the point gap decides the registered verdict." (175) | The first is self-weakening protocol detail in the main text, and the appendix (appendices_bcd.tex:397) already carries it in full. The second, placed right after the failure, reads as arguing the registered result away. | Shorten 177 to "a bar both AGIEval tasks narrowly missed (Appendix~X)". Keep the width fact in 175 as a plain statement and drop the "rather than the point gap decides" framing. |
| PA19 | minor | no | "the two kinds of pair have paired-difference deviations within a factor of 0.928 to 1.025, so seed matching doesn't explain the few changed calls" (224) | The conclusion does not follow from the stated ratio without the intermediate step that similar deviations imply similar correction effects. | "...within a factor of 0.928 to 1.025, so shared seed labels don't change the correction's effect." |
| PA20 | moderate | yes | "three runs each let the margin interval exclude one in 0.997 of replicates, and accuracy needs eight to reach 0.803" (241) vs "At 125 configurations and three runs the margin effect clears one in 0.970 of replicates while the accuracy effect clears it in 0.111" (appendices_bcd.tex:379) and "detects a true ratio of 1.10 in only 0.135 of replicates" (39) | Three power figures for the same design (125 configurations, three runs) give 0.997 against 0.970 on margins and 0.111 against 0.135 on accuracy, and the main text does not state which true ratio or population each assumes. A reader comparing them will see the same quantity reported with different values. | State the true ratio and the population in line 241 (for example "at a true 1.244 with equicorrelated covariance"), and make the appendix sentence name its own population, or reconcile it to one figure. |

## Checks that came out clean

The numbers derived from the table agree with it: $K_{\mathrm{eff}}=K/\Lam^2$ for all six rows, $\bar r_E=0.038$ and $K_{\mathrm{eff}}=7.47$ at 1.157, the BoolQ off-diagonal sums of about $-0.15$ and $-0.06$ implied by the removal estimates, $\sigma_{\mathrm{agg}}$ falling from 0.00572 to 0.00516, the interval widths 0.626 and 0.343, the counts of 40 and 250 contrasts, and bank two at 0.18 of bank one. The R2 simulation shares sum to 1.000 and 0.990. The values 1.244, 1.078, 0.113, 0.124, 0.652/0.324, 68%/84%, 1.096/1.786 and 0.928 to 0.954 match everywhere they recur.
