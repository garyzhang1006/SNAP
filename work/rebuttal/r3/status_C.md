# Slice C status (round 3)

Word count: slice_C.orig.tex 1195, slice_C.new.tex 1195 (wc -w), net 0. \label count 5 and 5; both tables and the figure kept; no em or en dashes added.

- R3-07 FIXED: subsection "\subsection{A pre-specified test on unseen tasks}" (label sec:heldout kept); "The pre-specified rule fails on held-out tasks."; "we pre-specified this test on tasks outside them"; "reports the test at its pre-specified scope"; Table 2 caption "The pre-specified test at 530M, 750M, and 1B". The fifth occurrence ("decides the registered verdict") left with the R3-30 deletion.
- R3-27 FIXED: "a comparison we specified before any held-out score existed"; caption "the original rows read the released runs at the same sizes as a comparison specified before any held-out score existed".
- R3-28 FIXED: "while removing any other task leaves it between 1.120 and 1.213, and only the removal of DROP, at 1.213 \ci{1.035}{1.377}, excludes one" (source compute extra/heldout/outputs/k04-heldout-full/heldout_table.csv, without_drop_mc margin/all 1.21347 [1.03473, 1.37663]; SciQ, MedMCQA, CoQA removals have lo 0.800, 0.836, 0.990).
- R3-29 FIXED: "As an exploratory result, the held-out accuracy interval excludes one while the margin interval doesn't, so which scale carries more seed signal depends on the battery." (values from Table 2 / heldout_table.csv; "Inflation is" became "Accuracy inflation is" for the referent).
- R3-30 FIXED: deleted "Interval width rather than the point gap decides the registered verdict."; "a bar both AGIEval tasks narrowly missed (Appendix~\ref{app:heldout})." NOTE for coordinator: no deliverables/*.tex file currently mentions AGIEval outside main.tex:177, so app:heldout does not yet carry the pilot miss detail the pointer implies; APP should add one sentence or the pointer should be dropped.
- R3-31 FIXED: "Recipe removal moves margin inflation between 1.207 and 1.256, and all 300 recipe-pair deletions keep the interval above one, between 1.177 and 1.263 (Appendix~\ref{app:supplementary}). Size removal moves it between 1.178 and 1.289, ..." (source research/outputs/snap-r6-leave-one-out/r6_leave_one_out.json, leave_two_recipes_full_margin 300/300 exclude one, min 1.17726, max 1.26289; matches appendices_bcd.tex:208).
- R3-44 FIXED: "The two point estimates differ by about 0.02" (heldout_table.csv 1.21651 vs 1.24038, gap 0.0239; the leading "However," dropped as a cut).
- Length cuts paying for the additions (no content change): "with interval" dropped before \ci at 1.786, 1.558, 1.217, 1.240, 1.220; "above the 1.097 that the original ten give" became "above the original ten's 1.097"; "We find that the" dropped; "with the scoring hardware and" and "it could take" dropped from the app:heldout pointer (appendices_bcd.tex:399 and :519 carry the hardware).
