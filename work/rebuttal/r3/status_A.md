# Slice A status (main.tex 28-45, work/rebuttal/r3/slice_A.new.tex)

Word count: `wc -w` slice_A.orig.tex 971, slice_A.new.tex 970. Abstract words outside the \outcome scaffolding: 172 before, 165 after. Scaffolding intact (11 \outcome, 17 \pending in both files; braces balanced 97/97). No em or en dashes, no "registered" left.

- R3-01 FIXED: abstract "A uniform item component shared across benchmarks would need more than all released item-noise variance to produce 1.244." (results/snap-r3-r2-02/r2_02_shared_component_crossformat.json, margin implied_share_q_star 1.4225). Brief wording compressed by four words ("On the released item noise," folded into "released item-noise variance") to meet the 165 target.
- R3-02 FIXED: "We therefore score all 45 PolyPythias runs at five sizes on the same 37,682 items, where each of nine seeds reruns one recipe, and pair that battery with a disjoint bank of 6,808 items ..."
- R3-04 FIXED: after the identification sentence, "Unlike the battery average's replicate standard deviation, this target transfers to redrawn banks and separates run variance from item noise." (20 words; "or extended" and the explicit fixed-bank clause dropped to stay within 20.)
- R3-06 FIXED: "Two rules fixed before scoring read the results, and in simulation the first passes in only 0.259 of replicates at a true 1.244 and the second returns undecided in 0.761, so we read both as bounds." (results/snap-r2-r1-18b/r1_18b_jackknife_recentring.json pass_rate_recentred_jackknife 0.2585; results/snap-r2-r1-18/r1_18_rule_two.json run_cov_1244_share0 undecided 0.76125.) The Section~\ref{sec:family} clause now starts a new sentence.
- R3-07 FIXED: abstract "A pre-specified test on four unseen tasks ..." and "Beyond the pre-specified test and two PolyPythias rules, all analyses are exploratory."; intro "we pre-specified a test on SciQ ..."; contributions "We also pre-specify a test on unseen tasks and report that it fails at its pre-specified scope." (the verb "register" changed too, following the finding's intent).
- R3-08 FIXED: "In simulation with margin-like covariance, such a comparison of two independently trained configurations on the margin scale declares a difference in 0.113 of replicates ... (Appendix~\ref{app:supplementary}). On DataDecide's own recipe comparisons the correction changes few calls (Section~\ref{sec:prediction}), so the understatement shows mainly in the error bar of one model's battery average and in planning replicate runs." No new number.
- R3-09 FIXED: "a margin-scale comparison using it declares a difference in 0.113 of null replicates" (kept "using it" so the sentence still names the standard error); "On 375 released DataDecide runs at the largest checkpoint each configuration's runs share, ..."; ", so batteries compare through the aggregate standard deviation" deleted; R1-fail branch "at a design with 40 within-configuration contrasts".
- R3-10 FIXED: abstract cut to 165 by dropping "with interval" twice, "single-benchmark removals give 1.096 to 1.786", "SNAP estimates it from cross-half products ... on a fixed battery of likelihood-scored multiple-choice benchmarks", "has 1.244 times its independent standard deviation", dropping "in 125 configurations" (the introduction carries it) and "across the chosen battery", and "Beyond the pre-specified test and two PolyPythias rules, all analyses are exploratory." Pre-specified-test and exploratory sentences kept.
- R3-11 FIXED: "The accuracy interval \ci{0.993}{1.157} includes one, but this design detects a true 1.10 in 0.135 of replicates." (research/outputs/snap-r6-detect-power/r6_detect_power.json, cells/lambda_1.10/wild_recipe/excludes_one_rate 0.13525.)
- R3-17 FIXED: deleted "The original ten benchmarks on the same runs give 1.240 ... (Section~\ref{sec:heldout})." and the 0.135 clause from the Lambda paragraph. The sec:heldout pointer moved to the preceding sentence: "The test fails on all 225 runs, where held-out inflation is 1.217 \ci{0.872}{1.498} (Section~\ref{sec:heldout})."
- R3-26 FIXED: "Algorithm~\ref{alg:snap} gives the measurement recipe with near-nominal intervals".
- R3-27 NO-CHANGE: the only "fixed before any held-out score existed" in A was deleted under R3-17.
- R3-36 FIXED: "built with the OLMES harness at a pinned commit".
- Extra cuts to hold length (not tied to a finding): removed "the exception being 530M with a lower limit of 0.993" from the contributions paragraph (Section 4 carries the size-band result) and "with interval" before the accuracy \ci in the Lambda paragraph.
