# Round 7 academic reviewer (QUICK mode, fresh context)

Skill: academic-research-skills:academic-paper-reviewer v1.11.1, quick mode, run as a five-seat ICLR panel (Journal-Fit, Methodology, Domain, Perspective, Devil's Advocate) by one model in one context, so the seats are role-separated and not independent. Read only, no compute, no compile. Targets: deliverables/main.tex through `\label{maintext:end}` (line 243) plus the statements, appendix_a.tex and appendices_bcd.tex by targeted search (app:design G5 at :64 to :66 and :375, app:calibration at :258 to :261, the permutation bound at :395). Round 6 edits were read from `git diff 73277c2 ae6d8c6 -- deliverables/`. Items settled or rejected in REBUTTAL_LOG.md rounds 3 to 6 are not re-raised, and none of their rejection reasons has become false in the current text. PolyPythias `\pending` slots are judged as design, with one `\outcome` branch read at a time. Line numbers are source lines, and each main.tex paragraph is one line.

## Panel scores (ICLR scale 1, 3, 5, 6, 8, 10)

| Seat | Profile | R1 fail (likely, 0.741) | R1 pass | Confidence |
|---|---|---|---|---|
| Journal-Fit | Evaluation-track AC | 5 | 6 | 4 |
| Methodology | Statistician, clustered inference | 6 | 6 | 4 |
| Domain | Benchmark and pretraining evaluation | 5 | 6 | 4 |
| Perspective | LLM practitioner | 3 | 3 | 3 |
| Devil's Advocate | Identification and pre-registration skeptic | 3 | 5 | 4 |
| **Mean** | | **4.4** | **5.2** | |

Unchanged from round 6. The round 6 edits fixed the misparses and the undefined terms the panel flagged (the "two part" sentence, the cross-format premise, the reliability definition, the Discussion ending), and the recommendation paragraph now closes the main text, which removes the Perspective seat's complaint about the last result read. No seat moves a full point, because every score-driving objection is structural (below). The R2-supported branch still costs the Domain and Journal-Fit seats one point each (mean 4.0 under R1 fail), as in round 6. Two round 6 edits (AR1, AR2) give the Devil's Advocate seat new material, and if left in place they hold that seat at 3 under R1 pass as well (mean 4.8 under pass).

## Findings

### Text-fixable

| ID | Severity | Verbatim quote (file:line) | Objection | Likely to lower score | Type | Length-neutral fix |
|---|---|---|---|---|---|---|
| AR1 | Moderate | R1-fail abstract branch "which fails a rule fixed before scoring and \pending{bounds inflation in this family / counts against transfer}" (main.tex:29) | The R6-04 edit replaced the design clause with the failure reading, but it left out the label that main.tex:43 ("in a reading added after fixing the rules") and main.tex:203 ("In an exploratory reading we added after fixing that rule") both carry. In the likeliest branch the abstract now presents a post hoc reading as the consequence of a rule fixed before scoring, which is the forking-path objection R6-04 was meant to close, now moved into the abstract. The closing "Beyond the pre-specified test and two PolyPythias rules, analyses are exploratory" doesn't cover it, since the sentence ties the reading to the rule. | yes (new, round 6 regression; DA seat) | text | Delete "On a second family," (4 words) at the start of the branch and insert "under a later reading," (4 words) after "fixed before scoring and", giving "which fails a rule fixed before scoring and, under a later reading, \pending{...}". Net 0 words. |
| AR2 | Moderate | "its check G5, that a run-level multiplicative gain explains under half the margin excess, failed under the plan's default simulation settings, while a simulator matched to each benchmark's variance components puts the gain's share near six percent (Appendix~\ref{app:design})" (main.tex:123) | The R6-03 addition sets a failed planned check beside a favourable second simulator and drops the two caveats its source attaches. appendices_bcd.tex:64 says the default simulator gives about 61 percent at the same gain scale and that "These results describe sensitivity to simulator assumptions and aren't causal bounds", appendices_bcd.tex:375 gives shares from 0.02 to 0.91 across the recipe-resample interval for the gain scale, and appendices_bcd.tex:66 records keeping the margin analysis after the G5 failure as a departure from the plan's response. A reviewer who checks the appendix sees the main text rescuing a failed check with a simulator it didn't plan. | yes (new, round 6 regression; DA and Methodology seats) | text | Replace "failed under the plan's default simulation settings, while a simulator matched to each benchmark's variance components puts the gain's share near six percent" (23 words) with "failed at 61 percent under the plan's default simulator, against eight under one matched to benchmark variance components, a simulator sensitivity rather than a bound" (24 words), and shorten "that a run-level multiplicative gain" to "that a run-level gain" (minus 1). Net 0 words. |
| AR3 | Moderate | "A run's deviation on every item of one prompt template recurs on any redrawn bank in that format and belongs to run covariance of the fixed battery. We identify the estimate only up to a deviation tied to items that both halves share." (main.tex:71), against the abstract's "a uniform item component shared by all benchmarks" (main.tex:29) | Main.tex:71 now counts a template-tied run deviation as target, and R1-05's settled example of the nuisance was exactly "a prompt-format artefact carried by every item of one template" (REBUTTAL_LOG.md:57). The main text no longer gives any concrete example of what the nuisance is, so a methodology reader can't tell how a run-varying component shared by all ten benchmarks and both halves differs from run covariance, and the abstract's bound on a "uniform item component" has no operational referent. REPEAT in topic of R1-05 and R2-01, NEW in form, since the reclassification of the template example removed the settled answer. | no | text | Replace "We identify the estimate only up to a deviation tied to items that both halves share." (16 words) with "We identify it only up to a run's deviation on content both halves share, such as common passages or exemplars." (20 words), and shorten "Disjoint item identifiers alone don't establish the required independence, since" to "Disjoint item identifiers don't establish independence, since" (minus 3), plus "Equation~\ref{eq:identity} covers diagonal entries" to "It covers diagonal entries" (minus 1). Net 0. Whether both halves share few-shot exemplars in the DataDecide requests is unverified here, so drop "or exemplars" if they don't. |
| AR4 | Minor | "Two rules fixed before scoring read the results, and in simulation the first passes in 0.259 of replicates at a true 1.244 and the second returns undecided in 0.761, so a first-rule pass confirms the effect, a failure, in a reading added after fixing the rules, bounds it or counts against transfer" (main.tex:43) | The "so" makes the reading follow from the power figures, which it doesn't, and after the R6-04 insertion the sentence nests a parenthetical inside a list inside a consequence clause (about 70 words). | no | text | Replace ", so a first-rule pass" with ". A first-rule pass". Net 0 words. |
| AR5 | Minor | "Independence predicts it with mean absolute log error 0.339, against 0.536 for the three-run estimate and 0.512 for SNAP with item noise added to both sides. Against a target pooled across configurations SNAP beats independence, 0.275 against 0.319 on margins and 0.367 against 0.523 on accuracy, and only this target resembles the recipe folds of Section~\ref{sec:prediction}." (main.tex:239) | Both compared estimates are fitted on three seeds, so "the three-run estimate" doesn't say what separates it from SNAP (main.tex:228 calls it the phenotypic plug-in). The last clause says this target resembles the recipe folds, yet on accuracy it gives SNAP a large gain (0.367 against 0.523) where Section 4.5 finds "every such interval includes zero", and no interval is given for 0.275 against 0.319. | no | text | Replace "the three-run estimate" with "the phenotypic estimate" (net 0), and replace "and only this target resembles the recipe folds of Section~\ref{sec:prediction}" with "a target pooled like the recipe folds of Section~\ref{sec:prediction}, though without their intervals" (net plus 1, offset by "with item noise added to both sides" becoming "with item noise on both sides"). |
| AR6 | Minor | "\bibitem[Team OLMo et~al.(2025a)]{teamolmo2025}" and "\bibitem[Team Olmo et~al.(2025b)]{olmo2025olmo3}" (references.tex, round 6 addition) | The a/b suffixes pair two author strings that differ in case, so the rendered citations read as one author with inconsistent spelling. | no | text | Keep each team's own spelling and drop the suffixes (they are different author strings), or use "Team OLMo" in both labels. Appendix only. |
| AR7 | Minor | "below 1B the auxiliary runs stop at 25 percent of the 1B compute budget, which adds a batch component beyond the seed" (main.tex:49); also main.tex:121, :138, :205 | The R6-16 definition names a schedule difference "batch", which an ML reader parses as training batch or data batch, the reading R4-17 already removed once. | no | text | Replace "batch component" with "schedule component" at all four places. Net 0 words. |

### Residual repeats noted, not re-raised

The abstract still gives no point value for "cross-format inflation" before "above its interval \ci{0.801}{1.035}" (main.tex:29; R6 AR4, fixed in part). The 0.761 undecided rate at main.tex:43 still lacks its no-shared-component condition and "the shared-item question" its antecedent (R6-06, skipped for length). Both stay below the likely-to-lower bar.

### Structural limits text cannot fix

| ID | Severity | Quote (file:line) | Objection | Likely to lower score | Experiment |
|---|---|---|---|---|---|
| AR-S1 | Critical (DA, adjudicated as structural) | "We recommend reporting the replicate standard deviation of the battery average with a benchmark-removal check." (main.tex:29) | The recommended quantity needs no split, contribution (ii) lists it as a contribution, and the split's own outputs are untested on DataDecide. Log R4-02, R5-05, R6-S. | yes (Perspective, Journal-Fit) | CPU exploratory: one decision where the split and replicate SD disagree (accuracy without BoolQ, 1.285 against 1.558). |
| AR-S2 | Major | "A pre-specified test on four held-out tasks fails its lower-limit rule" (main.tex:29); rule-one power 0.259 (main.tex:43) | The confirmatory record is one failed test, failed plan predictions and a second-family rule that fails in 0.741 of replicates at the true value. | yes (DA, Domain) | None; any new battery is exploratory. |
| AR-S3 | Major | "We didn't score the 375 DataDecide runs on the second bank" (main.tex:235) | No identification check in the headline family. | yes | GPU (R1-06 to R5-11). |
| AR-S4 | Major | "On the 33 configurations that share a final step, margin inflation is 1.082 (\ci{0.121}{1.530})" (main.tex:121) | Checkpoint and schedule confound. | yes (Methodology) | GPU (R4-13). |
| AR-S5 | Major | "BoolQ, the only benchmark in its format, carries 68\% of the margin covariance trace" (main.tex:157) | The factor belongs to one battery and weighting (rejected as text R3-13 to R5-10). | yes (Domain) | none |
| AR-S6 | Moderate | "The accuracy interval \ci{0.993}{1.157} includes one, at 0.135 power against a true 1.10" (main.tex:29) | The scale practitioners report gives a bound; scope stops at 1B and likelihood-scored multiple choice. | yes (Perspective) | GPU (R4-19, R5-26). |

No Devil's Advocate CRITICAL is text-fixable, and AR-S1 stays validated and structural.

## Questions reviewers will ask

1. Under R1 fail, was the "bounds inflation / counts against transfer" reading fixed before scoring, and why does the abstract not say it wasn't?
2. Why keep the margin analysis after G5 failed, and was the matched simulator planned?
3. What concrete object is the shared item component, now that a template-tied deviation counts as run covariance?
