# Slice D status, round 4

Word counts (`wc -w`): main-text part (slice lines 1-43, main.tex 199-241) 2006 before, 2006 after; reproducibility statement (slice 50-51) 300 before, 300 after, unchanged; whole slice 2455 .orig, 2455 .new. AI-use and ethics statements (slice 44-49) byte-identical (diff checked). Labels sec:family, sec:prediction, maintext:end kept; 6 \outcome branches before and after; no em or en dashes.

- R4-22 FIXED. "... gives supported in 0.033 and undecided in 0.957. The simulation never tests the rule against a shared component large enough to explain the inflation, and a not-supported verdict can't rule such a component out." ("therefore" dropped and "a verdict of not supported" shortened to pay for R4-27.)
- R4-07 FIXED. "In an exploratory reading we added after fixing that rule, a failing estimate is read only through its jackknife upper limit, which bounds inflation in this family, since under independence the middle 90\% of estimates runs from 0.848 to 1.139, a range that can't separate transfer from independence, while one below 1.026 counts against transfer." Source: results/snap-r2-r1-18/r1_18_rule_two.json, scenarios.independence_share0.within_lambda_5_95 [0.8479, 1.1390]. Changed from the brief: "and an estimate in that range can't separate the two explanations" became "a range that can't separate transfer from independence" (names the two, 3 words shorter). R1-fail branch's \pending{bounds the effect in this family / counts against transfer} untouched.
- R4-18 FIXED. First sentence: "Covariance models beat independence on margins only when given held-out marginal seed standard deviations." Last sentence: "On accuracy every such interval includes zero, and an operational comparison refitting without held-out marginals separates no model from independence on either scale." Changed from the brief to pay for length: the first sentence is shorter than the brief's version, and it now carries the oracle condition, so the method sentence drops "and giving them the held-out marginal seed standard deviations". Source: research/outputs/snap-r8-prediction/SNAP/C07_margin/result.json and C07_accuracy/result.json, where every entry in paired_difference_intervals_given_valid (full, rank_one_offdiagonal, shrink_50) includes zero.
- R4-35 FIXED. "and the ratio of seed-matched to seed-unmatched paired-difference standard deviations runs from 0.928 to 1.025". Source: results/snap-r2-r1-20/r1_20_seed_mismatch.json, sd_ratio_matched_over_mismatched min 0.92823 (margin 530M), max 1.02454 (margin 750M).
- R4-31 FIXED. "and what we add is the estimand, run covariance across benchmarks at a fixed configuration, which neither \citet{miller2024} nor \citet{heineman2025} estimates, and its measurement on released pretraining runs." A related cut in the same section: "\citet{heineman2025} relate per-benchmark signal and noise on DataDecide and OLMo checkpoints to decision accuracy." ("without estimating covariance between benchmarks" dropped, since the sentence above now says it.)
- R4-32 FIXED. "Apart from the pre-specified test, which failed at its protocol scope,".
- R4-01 FIXED. "A uniform item effect shared across benchmarks that produced the margin value would lift cross-format inflation outside its interval (Section~\ref{sec:estimator}), while on accuracy such an effect could reproduce 1.078 with a cross-format value of 1.047 that the interval \ci{0.848}{1.063} doesn't exclude," with the rest of the sentence and the three \outcome{\Rtwocase} sentences unchanged. Source: results/snap-r3-r2-02/r2_02_shared_component_crossformat.json, accuracy explain.induced_cross_format_diagnostic 1.0469 and observed cross_format_wild [0.8484, 1.0634]. The share 0.053 and "can't produce" are gone from the main text.
- R4-25 FIXED. "take simulated margin coverage over 12,000 pooled replicates to 0.821 at a quarter of latent variance and 0.580 at a half". Source: research/outputs/snap-r6-size-shared-b/r6_size_shared_b.json (10,000 reps: 0.8227, 0.5805) pooled with research/outputs/snap-r6-size-shared/r6_size_shared.json (2,000 reps: 0.8095, 0.5785) gives 0.8205 and 0.58017, which round half-up to 0.821 and 0.580. These match appendices_bcd.tex:389.
- R4-27 FIXED. "equicorrelated seed covariance at the observed $\bar r_E$ of 0.061 on margins and 0.018 on accuracy,". Source: compute extra/heldout/outputs/k08-seed-count/seed_count_power.json, by_phenotype margin observed_rbar_e 0.06082 and accuracy 0.01810. Changed from the brief: I added "on margins and 0.018 on accuracy", because the same sentence reports accuracy's eight-run power, which the file computes at 0.018, so "0.061" alone would be wrong for that clause. Notation matches main.tex:83 and :88.
- Cuts (brief): "whose middle 90\% runs from 0.70 to 1.85," and "The ratio itself belongs to this battery." removed from the Discussion. Extra one-word trims: "in only 0.135" became "in 0.135", and "rather than as evidence" became "rather than evidence".
- R4-20 SKIPPED. The slice has no room left. After the fixes it sits at exactly 2006, and the shortest faithful version of the sentence, about 24 words ("Proxies built from the benchmark scores remove shared covariance by construction, so testing mediation needs held-out loss on the same public checkpoints (Appendix~\ref{app:proxy})."), would need a matching cut from content other rounds kept, such as the pooled-target clause or the format-fit fallback sentence. The coordinator can add it if another slice banks the words.
- Reproducibility statement NO-CHANGE. None of the slice D findings targets it (R4-34 and R4-39 are REJECTED). It already says bank-two hashes "will" ship, which fits rule 3.
