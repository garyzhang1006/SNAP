# Status, slice APP (round 4)

- R4-01 FIXED, appendices_bcd.tex:261 (source results/snap-r3-r2-02/r2_02_shared_component_crossformat.json, kernel snap-r3-r2-02: implied_share_q_star 1.4225, induced_cross_format_diagnostic 1.1133, equal_pair_weight_check 1.1526 -> 1.153; noise definition read from seednoise/reliability.py variance_components, tot - diag(Sigma_E)). New text: "...identical in both halves, and we scaled each benchmark's component to its measured per-half item noise, the within-half variance less the cross-half covariance, which by construction excludes any component identical in both halves. On margins a share of 0.124 of that measured noise with no seed covariance lifts inflation only to 1.024, ..." / "A share carrying the whole observed excess would be 1.422 times that measured noise, a value the definition leaves unconstrained, but it would push the cross-format diagnostic to 1.113, ..." / "while the share of measured per-half item noise that explains the observed 1.078 is 0.053 and implies 1.047," / "A uniform component shared by every benchmark is therefore excluded on margins by the cross-format diagnostic, at 1.113 under noise-proportional scaling and at the 1.153 of equal pair weights (Appendix~\ref{app:supplementary}), while one that follows scoring format stays unmeasured in DataDecide." The code path is named here only, not in the paper. Grep of appendix_a.tex and appendices_bcd.tex for "more than the item", "unattainable", "can't produce", "bounded on" found no other hit; :128 unchanged.
- R4-08 FIXED, appendices_bcd.tex:294: "an upper bound below the planning document's margin threshold of 1.349 (Appendix~\ref{app:design})"; :253: "erases the 0.011 of separation from the planning document's threshold of 1.349 that we report below" (brief quoted "the planned threshold"; the file phrase was "from the planned threshold").
- R4-15 FIXED, references.tex: 17 VERIFIED bibitems from citation_audit.md pasted verbatim in alphabetical position (73 bibitems now; \begin{thebibliography}{60} untouched). appendices_bcd.tex:399: "the AGIEval task LogiQA-en \citep{liu2020logiqa} gave" and "the AGIEval task LSAT-LR \citep{wang2022lsat} gave".
- R4-16 FIXED, appendices_bcd.tex:310 "We also fitted four proxy weightings out of fold"; :316 now ends "The measurement is therefore open to anyone with the compute."; :343 "The repeat gives the five-size excess ratio of 1.07393 stated above and adds the inversion sets." (1.07393 printed at :341); :351 "A jackknife that drops a seed's term without recentring the others understates the standard error and on the same draws passes in 0.356. The uncentred script also assigns ..." (results/snap-r2-r1-18b/r1_18b_jackknife_recentring.json, kernel snap-r2-r1-18b, pass_rate_r1_power_jackknife 0.356).
- R4-18 FIXED, appendices_bcd.tex:183: "differ from independence on both scales by amounts whose 2,000-draw recipe-resampling intervals all include zero" (research/outputs/snap-r8-prediction/SNAP/C07_margin and C07_accuracy result.json, kernel snap-r8-prediction: all six paired_difference_intervals_given_valid straddle zero).
- R4-27 FIXED, appendices_bcd.tex:456: "at the observed effective coefficient $\bar r_E$, 0.061 on margins and 0.018 on accuracy" and "These shares describe a battery with the same $\bar r_E$ and not this battery's structure." (compute extra/heldout/outputs/k08-seed-count/seed_count_power.json, kernel k08-seed-count: observed_rbar_e 0.0608, 0.0181).
- R4-29 FIXED (inference, not a measurement), appendices_bcd.tex:142: "differs from the accuracy estimate of 1.078 by 0.0007, a gap that only items with a margin of exactly zero can produce, since elsewhere the sign score is an affine map of correctness and the tenfold-rescaling check shows the estimate is scale-invariant." Definition confirmed as np.sign(m), so sign(0) = 0, in compute extra/research_kernels/snap-r10-scale-transform/snap-r10-scale-transform.py:66, whose docstring says sign "differs from accuracy only at zero margins". No stored count of zero-margin items was found, so the zero-margin cause is not measured.
- R4-36 FIXED, supplement_extended_body.tex: :694 "\paragraph{Pre-specified held-out test}\label{app:heldout}", :695 "that we had pre-specified earlier.", :701 "At the pre-specified scope of", :707 "pass rate of the pre-specified rule", :756 "for the pre-specified test.}". Line 106 "verified preregistration" left as is, since it disclaims preregistration.
- R4-37 FIXED, supplement_extended_body.tex:135: "Both resample runs within tasks, while our application requires preservation of paired run effects across benchmarks."

No edits to appendix_a.tex (no rule-8 wording found) or main.tex. No em or en dashes in changed files; every \label kept. Not compiled.
- Coordinator extra (influence vs truncation, cut from main.tex:121) NO-CHANGE: app:schedule already states it at appendices_bcd.tex:10, "A configuration's influence, $(T_c-\widehat\Lam^2U_c)/\sum_{c'}U_{c'}$, has Spearman correlation $-0.036$ for margins and $0.001$ for accuracy with the selected step's share of the default final step. Permuting that share within each size over 4,999 draws gives $p=0.314$ and $p=0.990$." These match research/outputs/snap-r21-matched-step/r21_matched_step.json (kernel snap-r21-matched-step): severity.margin.spearman -0.0356, spearman_p_within_size_permutation 0.3144; severity.accuracy.spearman 0.0007, 0.9904. A second sentence would duplicate it.
