# Slice A status (round 4)

Word count over the slice, \citep counted as zero per rule 2: orig 970, new 970 (raw `wc -w` 970 vs 973, the extra 3 are \citep tokens). Abstract words outside the \outcome scaffolding: 165 to 180. Brace balance 0; \outcome 11 and \pending 17 in both files; no em or en dashes.

- R4-01 FIXED. Abstract: "A uniform shared item component producing 1.244 would imply cross-format inflation of 1.113, outside \ci{0.801}{1.035}." Source results/snap-r3-r2-02/r2_02_shared_component_crossformat.json (margin explain.induced_cross_format_diagnostic 1.1133; cross_format_wild lo 0.8007, hi 1.0347). AR6's format-following request declined for length as the brief directs.
- R4-02 FIXED (with R4-03). Line 45: "The battery average's replicate standard deviation answers the fixed-bank question, while the split separates each benchmark's run variance from item noise and defines a target for redrawn banks, untested on DataDecide (Section~\ref{sec:estimator})." Shortened from the brief's wording ("that we haven't tested on DataDecide" to "untested on DataDecide") to meet the slice budget, per the brief's first-fallback instruction; 29 words against the 20 it replaces.
- R4-03 FIXED. Same sentence as R4-02; transfer is now stated as a defined, untested target.
- R4-04 FIXED. Abstract sentence 1 ends "..., against a nominal 0.05, although on DataDecide's recipe comparisons correcting it removes at most 12 of 173 to 223 calls." ("own" dropped for budget). Numbers already printed at main.tex:222.
- R4-05 FIXED. Abstract: "A pre-specified test on four held-out tasks fails its lower-limit rule at 1.217 \ci{0.872}{1.498}, against 1.240 \ci{1.054}{1.397} for the original ten." Intro: "The test fails at its full scope of 225 runs, where held-out inflation is 1.217 \ci{0.872}{1.498} against 1.240 \ci{1.054}{1.397} for the original ten on the same runs (Section~\ref{sec:heldout})." Source compute extra/heldout/outputs/k04-heldout-full/heldout_table.csv (heldout margin/all 1.21651 [0.87217, 1.49823]; original_same_sizes margin/all 1.24038 [1.05397, 1.39741]; n_runs_read 225 in heldout_results.json).
- R4-06 FIXED. Line 43: "..., so a pass confirms the effect while a failing or undecided verdict only bounds it." 0.259 and 0.761 kept.
- R4-11 FIXED. Abstract: "A run-to-run standard error of a benchmark average that sums per-benchmark variances ignores cross-benchmark covariance, ..." / "SNAP (Seed Noise Across Phenotypes) estimates that covariance from cross-half products of run-centred item-half scores on a fixed battery of likelihood-scored multiple-choice benchmarks." / "The accuracy interval \ci{0.993}{1.157} includes one, at 0.135 power against a true 1.10." No numbers added beyond R4-04 and R4-05.
- R4-15 FIXED (A part). Line 41: "SciQ \citep{welbl2017sciq}, MedMCQA \citep{pal2022medmcqa}, and multiple-choice DROP \citep{dua2019drop} and CoQA \citep{reddy2019coqa}"; line 43: "built with the OLMES harness \citep{gu2025olmes} at a pinned commit". All five keys VERIFIED in work/rebuttal/r4/citation_audit.md; the bibitems land via slice APP.
- R4-17 FIXED. Line 43: "A single model family whose replicates mix default and auxiliary runs on different schedules leaves open whether the inflation belongs to DataDecide's design."
- R4-32 FIXED (A part). Abstract "four unseen tasks" to "four held-out tasks"; "Under a rule fixed before scoring" to "Under rules fixed before scoring" in the R2 supported and R2 undecided variants of the R1-pass branch (all branches kept); "fails on all 225 runs" replaced via R4-05.

Cuts that paid for it: deleted line 45 "We also pre-specify a test on unseen tasks ..."; line 37 now opens "Margins carry more seed signal"; line 33 now "Assuming independence drops the off-diagonal terms and weakens a comparison's error control." Extra trims, meaning unchanged: abstract "in simulation ... null replicates" to "of simulated null replicates", "covariance between benchmarks" to "cross-benchmark covariance", "at the largest checkpoint each configuration's runs share" to "at each configuration's last shared checkpoint", "On 375 released DataDecide runs" to "On 375 DataDecide runs", "all analyses are exploratory" to "analyses are exploratory"; intro dropped "on the margin scale" (the sentence already says margin-like covariance), "rate of", "own", "to this setting", "only", and tightened "centring both half scores across each configuration's replicate runs", "so we treat margins as the primary scale and accuracy as a bound", "whose rule requires held-out margin inflation's lower interval limit to exceed one".
