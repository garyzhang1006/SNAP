# Slice B status (round 4)

Word count (`wc -w`): slice_B.orig.tex 1620, slice_B.new.tex 1604, which is 1603 with the one-token \citep list counted as zero, so the slice is 17 words shorter. The banked cut is 17 words, short of the brief's roughly 30, so the coordinator's page-9 recompile should confirm that the benchmark \citep list fits. Labels: 9 in .orig, 9 in .new. There are no \pending or \outcome branches in this slice and no em or en dashes.

- R4-01, FIXED (slice line 26). Dropped the 1.024, 0.124 and 1.42 figures. New text: "A uniform component shared across benchmarks and large enough to carry the observed excess of 0.244 would raise inflation from cross-format covariances alone to 1.113, which the observed 0.921 \ci{0.801}{1.035} excludes (Appendix~\ref{app:calibration}), while a component that follows scoring format is untested in DataDecide, and Section~\ref{sec:family}'s cross-bank rule tests it in PolyPythias." Source: results/snap-r3-r2-02/r2_02_shared_component_crossformat.json (explain.induced_cross_format_diagnostic 1.1133; observed.cross_format_wild 0.9212 [0.8007, 1.0347]). 0.244 is the printed 1.244 minus one.
- R4-09, FIXED (slice line 26, closes the paragraph). New text: "The fixed-bank 1.237 needs no halves and no cross-half assumption, so only the split's redrawn-bank target and the per-benchmark run variances rest on it." The brief's "item-general reading" is replaced because that term first appears at main.tex:239. The 1.237 is already printed at slice line 18 (results/snap-r3-r2-26/r2_26_analytic_vs_split.json full_bank_uncorrected 1.2375). To avoid saying it twice, "and needs no halves" is removed from the line 18 sentence "Its denominator sums per-benchmark replicate variances, so any item component the halves share counts as fixed-bank run variance."
- R4-23, FIXED. Line 4 now adds "We call the set of benchmarks the battery and the set of items the bank." Line 26 now reads "belongs to run covariance of the fixed battery" and "We identify the estimate only up to a deviation tied to items that both halves share." "That excess" is gone, since the new sentence names "the observed excess of 0.244". Line 18 keeps "fixed-bank run variance".
- R4-03, FIXED (line 18). New text: "This target is defined for a redrawn or extended item bank, while a run's item-specific deviations on the released bank fall into $\varepsilon$, so reproducibility on that fixed bank is a separate question."
- R4-10, FIXED (line 18). New text: "On the full battery item noise is 0.037 of the margin diagonal and 0.186 of the accuracy one, and the two targets nearly coincide, while without BoolQ it is 0.578 of the accuracy diagonal and fixed-bank accuracy inflation of 1.285 falls below the split's 1.558." The brief's "well below" loses its adverb. Source: results/snap-r3-r2-26/r2_26_analytic_vs_split.json (share_of_diag_removed 0.0366, 0.1860, 0.5777; no_boolq accuracy full_bank_uncorrected 1.2852, split_half 1.5578).
- R4-24, FIXED (line 68). New text: "Within-benchmark cross-half error correlation of 0.1 and the accuracy-like null population bias the estimate down by 0.011 to 0.016".
- R4-08, FIXED (line 78). New text: "The plan also predicted margin inflation above 1.349 and accuracy inflation above 1.40, which none of its $\binom{25}{17}=1{,}081{,}575$ possible estimation sets reaches, and its gain check G5 failed under the default simulator." The appendix pointer is dropped because the preceding sentence already ends in (Appendix~\ref{app:design}). Sources: deliverables/appendices_bcd.tex:68 (thresholds), :377 (1,081,575 sets, maxima 1.317 and 1.191), :375 (G5 fails at the default cell).
- R4-15, FIXED (line 72). New text: "... Social IQa, and WinoGrande \citep{clark2018arc,clark2019boolq,talmor2019commonsenseqa,zellers2019hellaswag,hendrycks2021mmlu,mihaylov2018openbookqa,bisk2020piqa,sap2019socialiqa,sakaguchi2020winogrande}." All keys are VERIFIED in citation_audit.md, and line 84 gets no second OLMES citation.
- R4-32, FIXED (line 84). New text: "Both rules of Section~\ref{sec:family} were committed to the repository before scoring of either bank began."
- R4-28, SKIPPED for length. The sentence would add about 33 words, which would put the slice at roughly 1636, above the 1620 limit. The \ci{1.130}{1.349} value stays in deliverables/appendices_bcd.tex:253.

Cuts: The brief's three cuts are all applied. Line 66 loses "We also report a configuration percentile bootstrap that resamples both sums but ignores recipe dependence.", and line 68 now introduces it as "a configuration percentile bootstrap that ignores recipe dependence drops to 0.872". Line 76 loses the rank-correlation sentence; that result appears nowhere else in main.tex, appendix_a.tex or appendices_bcd.tex, so the APP coordinator may want to restore it in app:schedule. Line 74 loses the 25-percent sentence, which appendices_bcd.tex:6 still carries. The other cuts are mine:
- The algorithm caption loses "The primary analysis used a random frozen split, checked against a passage-aware one, and the pre-specified test kept passage-linked items in one half." Line 26 and sec:composition still cover the passage-aware check.
- Line 48 becomes "Subtracting item-sampling variance from the full-data diagonal instead needs a model of item-by-run noise, and on the released runs it gives 1.246 \ci{1.144}{1.340} against 1.244 on margins (Appendix~\ref{app:implementation})." This drops the 5-percent width clause.
