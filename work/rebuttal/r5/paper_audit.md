# Round 5 paper audit (gate + reviewer mode)

Target: `deliverables/main.tex` (256 lines). Mode: `gate` script run via `audit.py --mode gate`, then EIC screening and a reviewer read of the full main text, done as a sequential single-agent pass. Read-only, no compute. I did not open REBUTTAL_LOG.md or work/rebuttal/r1..r4. I opened the appendix files only through grep, to check whether terms were defined there and to check counts.

## Gate decision

**PASS (conditional).** There are no submission blockers. The script verdict was FAIL, but every blocking item is a `\ref` to a label defined in `appendix_a.tex`, `appendices_bcd.tex`, or `supplement_extended` through `xr`, and these are known false positives because main.log reports 0 undefined references. Other script output is advisory only: L1 citation ties, unreferenced equation labels `eq:scores`/`eq:model`/`eq:effective`, and long-paragraph G2 warnings at lines 29, 62, 70, 121, and 203.

EIC screening scores: pitch 5.5, venue fit 8, fatal flaws 8, presentation 5.5, for a weighted score of 6.75, which is a **Conditional Pass**. I found no fatal flaw. The main risk to the score is readability. The abstract and page 1 put about twenty numbers in front of the reader before the paper defines "inflation", "cross-format", "fixed-bank", or "auxiliary". The abstract also opens with a concession that the effect barely changes real decisions.

## Findings

| id | severity | lowers score? | verbatim quote (line) | problem | fix |
|---|---|---|---|---|---|
| PA1 | major | yes | "A run-to-run standard error of a benchmark average that sums per-benchmark variances ignores cross-benchmark covariance, and a margin-scale comparison using it declares a difference in 0.113 of simulated null replicates, against a nominal 0.05, although on DataDecide's recipe comparisons correcting it removes at most 12 of 173 to 223 calls." (L29) | The first sentence joins three claims and undercuts its own motivation with "although". It also uses "margin-scale" and "calls" before defining them, and "12 of 173 to 223" cannot be parsed without Section 4.5. A 90-second reader comes away with "the correction does not matter". | Split it into two sentences. The first states the problem and the 0.113 against 0.05 figure. Move the decision-call concession later in the abstract and word it as "changes at most 12 of roughly 200 recipe-comparison calls". Say "per-byte log-likelihood margin" once, before any use of "margin-scale". |
| PA2 | major | yes | "BoolQ carries 68\% of the covariance trace, and single-benchmark removals give 1.096 to 1.786. A uniform shared item component producing 1.244 would imply cross-format inflation of 1.113, outside \ci{0.801}{1.035}." (L29) | The abstract never defines "inflation" as a quantity. It says "1.244 times its independent standard deviation" and then uses "cross-format inflation", "covariance trace", and "give 1.096 to 1.786" with no noun. "Cross-format inflation" also stays undefined in the main text until L216. A reader cannot parse the identification sentence. | Define inflation once in the abstract, e.g. "the inflation factor Λ, the ratio of the average's standard deviation to its value under independence, is 1.244". Then either drop the cross-format sentence from the abstract or rewrite it in plain terms, e.g. "a shared item effect large enough to explain this would also inflate cross-format pairs, which it does not". |
| PA3 | moderate | yes | "Two rules fixed before scoring read the results, and in simulation the first passes in 0.259 of replicates at a true 1.244 and the second returns undecided in 0.761, so a pass confirms the effect while a failing or undecided verdict only bounds it." (L43) | This contradicts L203/L207, where an R1 estimate "below 1.026 counts against transfer", so a failing verdict can do more than bound the effect. An undecided R2 verdict also bounds nothing about the effect size, because R2 tests identification. | Change the ending to "a first-rule pass confirms the effect, a failure bounds it or counts against transfer depending on the estimate, and an undecided second rule leaves the shared-item question open". |
| PA4 | moderate | yes | "In an exploratory reading we added after fixing that rule, a failing estimate is read only through its jackknife upper limit, which bounds inflation in this family, since under independence the middle 90\% of estimates runs from 0.848 to 1.139, a range that can't separate transfer from independence, while one below 1.026 counts against transfer." (L203) | This sentence is over-compressed. "only through its upper limit" conflicts with "one below 1.026 counts against transfer", which reads the point estimate. "one" has three possible referents, and the "since" clause does not justify the upper-limit reading. | Split it into two sentences, one for the upper-limit reading and one for the point-estimate threshold. Replace "one below 1.026" with "an estimate below 1.026, the 5th percentile under a true 1.244". |
| PA5 | moderate | yes | "A single model family whose replicates mix default and auxiliary runs on different schedules leaves open whether the inflation belongs to DataDecide's design." (L43) | "Default" and "auxiliary" runs are used at L43, L49, L119, L121, and in Table 1 ("compares the two auxiliary runs"), but the main text never says that each configuration has one default and two auxiliary runs, or that auxiliary runs below 1B stop at 25% of the 1B budget. That fact appears only in appendix_a.tex:4 and appendices_bcd.tex:6. | Add one sentence to Section 3 before L119: "Each configuration has one default run and two auxiliary runs, and below 1B the auxiliary runs stop at 25 percent of the 1B compute budget." |
| PA6 | moderate | yes | "On the released bank, the replicate standard deviation of the full-bank average gives inflation 1.237 \ci{1.137}{1.331} on margins and 1.067 \ci{0.994}{1.134} on accuracy, against 1.244 and 1.078 from the split." (L63) | Section 2.1 reports results before the estimator exists. "The split" is defined in Section 2.2 (L66), Λ̂ is defined at L81, and the intervals are defined in Section 2.4. The next sentences, with 0.037/0.186/0.578 and 1.285 against 1.558, stack four more numbers into a model-definition paragraph. | Move the fixed-bank comparison (L63 from "On the released bank" to the end) to the end of Section 2.2 or into Section 4.1, and leave one sentence at L63 that names the two targets. |
| PA7 | moderate | yes | "A uniform component shared across benchmarks and large enough to carry the observed excess of 0.244 would raise inflation from cross-format covariances alone to 1.113, which the observed 0.921 \ci{0.801}{1.035} excludes" (L71) | "Cross-format" inflation is used here before the paper says which benchmarks share a scoring format. The main text only says BoolQ is alone in its format (L157), and L216 later calls 0.921 "diagnostic inflation" without defining it. A value below one from "off-diagonal covariance alone" needs explaining. | Define the diagnostic at first use: Λ recomputed with within-format off-diagonal entries set to zero. List the formats in one parenthesis, e.g. "(ranked-choice / yes-no / ...)", which the author needs to fill in. |
| PA8 | moderate | yes | "Algorithm~\ref{alg:snap} gives the measurement recipe with near-nominal intervals, whose wild recipe-cluster coverage runs from 0.928 to 0.954 across twelve simulated populations." (L45) | This overclaims against L237, where a band-shared run effect, which the paper says the release makes possible because "the release shares one seed label across every recipe inside a size band", takes coverage to 0.821 and 0.580. "Near-nominal" holds only for the twelve populations, and those exclude that structure. | Scope it: "near-nominal (0.928 to 0.954) in twelve simulated populations without run effects shared across a size band". |
| PA9 | moderate | yes | "This paper makes four contributions to measuring run covariance." (L45) | The paragraph has five sentences, and it is unclear which four are the contributions. The second ("The battery average's replicate standard deviation answers the fixed-bank question...") depends on "fixed-bank question" and "redrawn banks", which are defined only in Section 2.1. It also says "untested on DataDecide" with no referent. | Enumerate the contributions as (i)-(iv), one sentence each. Replace "fixed-bank question" with "uncertainty for the released items". |
| PA10 | moderate | no | "Across 2,000 such permutations the permuted estimates sit about 0.001 below the observed margin value" (L159) vs "500 permutation replicates" (L249) | The reproducibility statement lists 500 permutation replicates, but the main text reports a 2,000-permutation result. The appendix has both (appendices_bcd.tex:117 at 500, :263 at 2,000). A reader checking reproducibility sees a mismatch. | List both in L249: "500 and 2,000 permutation replicates for the two permutation checks". |
| PA11 | moderate | yes | "We never drew the analysis plan's split of eight screening and seventeen estimation recipes ... and its gain check G5 failed under the default simulator." (L123) | "Gain check G5" and "the default simulator" are dangling. Neither is defined in the main text, and a reviewer reads this sentence as a failed pre-registered check with no explanation. | Either explain it in six words, e.g. "its check that margins gain over accuracy (G5)", and point to Appendix~\ref{app:design}, or cut the G5 clause from the main text. |
| PA12 | minor | no | "Because both quantities normalise by the battery's own trace, they can rise when removing a high-variance benchmark lowers $\sigma_{\mathrm{agg}}$" (L91) | The antecedent of "both quantities" is ambiguous. If it means Λ and K_eff, K_eff = K/Λ² moves opposite to Λ, so "they can rise" is wrong. σ_agg is also first used here and is defined only in the Table 1 caption. | Write "$\Lam$ and $\bar r_E$ can rise" and define σ_agg inline: "the standard deviation of the battery mean". |
| PA13 | minor | no | "where $\Sig=N^{-1}\sum_c\Sigma_{E,c}$ is the configuration-averaged covariance matrix" (L86) | $N$ is undefined in the main text. | Add "over the $N$ configurations" or "$N=125$". |
| PA14 | minor | no | "Margins carry more seed signal, with per-benchmark reliability averaging 0.652 against 0.324 for accuracy" (L37, repeated at L155) | "Reliability" has no definition in the main text, and it is the stated reason for choosing the primary scale. | Add a parenthesis: "(the share of a benchmark's replicate variance that is run variance rather than item noise)", or give the exact definition from the appendix. |
| PA15 | minor | no | "A permutation that reshuffles each run's item deviations inside every group leaves all trait scores fixed" (L159) | "Group" is undefined in the main text. It means 66 groups, per appendices_bcd.tex:263. | Write "inside each of the 66 benchmark or subject groups". |
| PA16 | minor | yes | "Against one configuration's six-run margin ratio on the 4,755-item three-size PolyPythias transfer runs of Appendix~\ref{app:transport}, independence predicts with mean absolute log error 0.339 against 0.536 for the plug-in and 0.512 for our decomposition" (L239) | The sentence is over-compressed, with a five-modifier noun stack. "The decomposition" is not a term the main text defines, and "six-run" conflicts with nine PolyPythias seeds unless explained. The result, where independence beats SNAP on the single-configuration target, is buried in the recommendations paragraph. | Split L239 into a recommendation paragraph and a prediction-check paragraph. Unpack the target as "the margin ratio measured on six runs of one configuration". Name the model as in Section 4.5. |
| PA17 | minor | no | "A permutation test that aligns runs by batch slot bounds that effect at 0.044 of the seed covariance on margins" (L237) | "Batch slot" is undefined. The sentence also reads first as "aligns runs by batch", with "slot bounds" as the verb. | Write "A permutation test that aligns runs by their position in the release's seed order bounds that effect at 0.044...". |
| PA18 | minor | yes | "run covariance" (L45, L63), "seed covariance" (L237, L239), "seed-covariance models" (L220), "run noise" (L226), "Seed Noise Across Phenotypes" (L35); plus "inflation", "the factor", "the ratio" (L91, L157, L233) | The terms drift. L49 says auxiliary schedules add run variation beyond the seed, so "seed covariance" and "run covariance" are not interchangeable here. Λ goes by three names. | Use "run covariance" everywhere except when the seed alone is meant. Use "inflation" for Λ and drop "the factor"/"the ratio". |
| PA19 | minor | no | "on the same checkpoints at 530M, 750M, and 1B, whose rule requires held-out margin inflation's lower interval limit to exceed one" (L41) | "whose" attaches to "1B" or "checkpoints" instead of to the test. | Write "...at 530M, 750M, and 1B. Its rule requires the lower interval limit of held-out margin inflation to exceed one." |
| PA20 | minor | no | "We recommend reporting paired replicate scores with a benchmark-removal check." (L29) vs "the replicate standard deviation of the battery average ... we recommend reporting it with a benchmark-removal check" (L239) | The recommendation differs between the abstract and the discussion. "Paired replicate scores" is not what L239 recommends. | Match the abstract to L239: "We recommend reporting the replicate standard deviation of the battery average with a benchmark-removal check." |
| PA21 | minor | no | "Covariance models beat independence on margins only when given held-out marginal seed standard deviations. We compared five covariance models" (L220) | Only three of the five models are named, and "rank-one plus gain" leaves "gain" undefined. | Name all five in one parenthesis and define "gain" in one clause, or point to Table~\ref{tab:prediction} for the definitions. |
| PA22 | minor | no | "Exploratory cuts whose intervals exclude one, without BoolQ at 1.558, without the 26 truncated configurations at 1.120, at the adjacent checkpoint and on the held-out tasks, don't settle it." (L233) | The list leaves out accuracy cuts that also exclude one: the weight 0.0867 (L157), and the five recipe deletions and the WinoGrande removal reported in appendices_bcd.tex:383. This reads as selective. | Add "and five single-recipe deletions (Appendix ...)" or say "among others, listed in Appendix ...". |

The findings come from one pass by one agent, so none of them is an independent reviewer consensus. Numeric consistency checks that passed: K_eff and r̄_E against Λ in Table 1 and at L134 and L239; the BoolQ off-diagonal shares at L157 (−0.15 and −0.06); the 40 against 250 contrasts; and the 6,808 bank-two count against the 600-item caps.
