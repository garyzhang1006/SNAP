Word count (wc -w): slice_B.orig.tex 1604 raw, 1602 with each \citep as zero; slice_B.new.tex 1700 raw, 1690 with each \citep as zero; cap 1669, so B runs 21 over after both brief-listed banks and the R5-28 fallback skip, and the remaining R5-17 sentence (14 words) is the trim-order item that would recover most of it.
R5-01 FIXED: "Cross-format inflation, which keeps only the diagonal and the covariances between benchmarks of different scoring formats, is 0.921 \ci{0.801}{1.035}. A uniform component shared across benchmarks and acting alone would lift it to 1.113 if it carried the whole margin excess of 0.244 above one, and the upper limit admits at most 0.319 of that excess, although negative run covariance across formats, which 0.921 below one implies, could offset a larger component (Appendix~\ref{app:calibration}). A component that follows scoring format is untested in DataDecide, and Section~\ref{sec:family}'s cross-bank rule tests it in PolyPythias." Source results/snap-r5-shared-bound/r5_shared_bound.json (cross_format_wild 0.9212 [0.8007, 1.0347], explain_induced_cross_format 1.1133, fraction_of_excess_lambda_scale 0.3187). Deviation from the brief text: "whole excess of 0.244" became "whole margin excess of 0.244 above one", because the R5-16 move leaves 1.244 unprinted in Section 2 before this sentence.
R5-14 FIXED: "DataDecide's seed controls initialisation and data order together. Each configuration has one default and two auxiliary runs, and below 1B the auxiliary runs stop at 25 percent of the 1B compute budget, which adds run variation beyond the seed." Source deliverables/appendices_bcd.tex:6.
R5-15 FIXED: "and its check G5, that a run-level multiplicative gain explains under half the margin excess, failed under the plan's default simulation settings." Source deliverables/supplement_extended_body.tex:95; Appendix~\ref{app:design} pointer kept.
R5-16 FIXED: the three fixed-bank sentences moved from the end of the Sec 2.1 model paragraph to the end of the paragraph after Eq. effective's discussion, after the analytic item-by-run sentence; third now opens "Under that item-by-run model, item noise is 0.037 of the full-bank margin diagonal and 0.186 of the accuracy one, ..." (results/snap-r3-r2-26/r2_26_analytic_vs_split.json share_of_diag_removed 0.0366, 0.186, no_boolq 0.5777, 1.2852, 1.5578). Line 71 now reads "The fixed-bank inflation reported below needs no halves and no cross-half assumption". "so reproducibility on that fixed bank is a separate question" kept.
R5-17 FIXED (shortened per brief bank, reworded to define the term): "A configuration's influence on the margin estimate has Spearman correlation $-0.036$ with truncation (Appendix~\ref{app:schedule})." Source deliverables/appendices_bcd.tex:10.
R5-19 FIXED: "ARC-Challenge and ARC-Easy \citep{clark2018arc}, BoolQ \citep{clark2019boolq}, CommonsenseQA \citep{talmor2019commonsenseqa}, HellaSwag \citep{zellers2019hellaswag}, MMLU \citep{hendrycks2021mmlu}, OpenBookQA \citep{mihaylov2018openbookqa}, PIQA \citep{bisk2020piqa}, Social IQa \citep{sap2019socialiqa}, and WinoGrande \citep{sakaguchi2020winogrande}"; the trailing list deleted; all keys are in references.tex.
R5-28 PARTLY FIXED: "Because $\Lam$ and $\bar r_E$ normalise by the battery's own trace," applied; the sigma_agg definition clause ", the standard deviation of the battery mean," SKIPPED under the brief's fallback because B stays over 1669 (1690 with it skipped, 1697 with it in).
R5-29 FIXED: "is the covariance matrix averaged over the $N$ configurations".
Bank cut applied: "Either pooled sum can be nonpositive in a finite sample, and our calibration simulations check the bias of the ratio." No other text removed; all \label commands unchanged; no em or en dashes.
