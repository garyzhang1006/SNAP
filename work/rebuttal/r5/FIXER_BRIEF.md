# Round 5 fixer brief

Merged findings, statements and quotes are in REBUTTAL_LOG.md under "### Merged findings (round 5)". Each slice fixer edits only its own line range or files, so the five slices can run in parallel. Line numbers refer to deliverables/main.tex at commit fceef7c.

| Slice | Range | Findings |
|---|---|---|
| A | main.tex 28-45 (abstract 28-30 and introduction 32-45; title at line 14 stays) | R5-01, R5-03, R5-04, R5-05, R5-06, R5-12, R5-18, R5-21, R5-30, R5-34, R5-35, R5-38 |
| B | main.tex 46-130 (Sections 2 and 3) | R5-01, R5-14, R5-15, R5-16, R5-17, R5-19, R5-28, R5-29 |
| C | main.tex 131-198 (Section 4 up to, not including, sec:family at 199) | R5-01 (format list), R5-31 |
| D | main.tex 199-241 (sec:family through \label{maintext:end}) and the reproducibility statement at 248-249 | R5-01, R5-02, R5-13, R5-22, R5-27, R5-32, R5-33, R5-36, R5-37 |
| APP | appendix_a.tex, appendices_bcd.tex, references.tex | R5-01, R5-02, R5-39, R5-40, R5-41 |

The tail of main.tex begins at line 242: the AI-use statement (242-243) and the ethics statement (245-246) stay untouched, the reproducibility statement (248-249) belongs to slice D, and 250-256 are the references and appendix inputs.

## Global rules

1. Invent no number. Use only numbers already printed in the paper or stored in a result file, and when you move a number into the text, name its source path in your status note. Round half-up to the paper's precision. The shared-bound kernel has landed (results/snap-r5-shared-bound/r5_shared_bound.json), so its values may now be used where this brief names them: largest admissible margin share 0.420 (bound_at_observed_upper_limit.largest_share 0.4198), fraction of the margin excess it carries 0.319 on the inflation scale (fraction_of_excess_lambda_scale 0.3187) and 0.295 on the covariance scale (0.2951), accuracy admissible share 0.072 (0.0717) against the explaining 0.053 (0.0526). The Lambda that share produces alone (within_battery_lambda_from_share 1.0778) stays out of the main text, and so does the reviewer's "about 1.08": it collides with the accuracy estimate 1.078. The abstract may say "about a third" for 0.319.
2. Add no citation unless its key is already in deliverables/references.tex. A \citep costs little source text; count it as zero words in your slice word count.
3. PolyPythias bank one is being scored now and bank two hasn't been scored. No sentence outside the \outcome branches may say these runs were scored, that the release ships their outputs or hashes, or describe any PolyPythias bank result as existing. DataDecide on bank two stays unscored (GPU).
4. Use no em dashes and no en dashes as punctuation. Write continuous academic prose in the paper's register, with contractions as the surrounding text uses them, no bullet lists in the paper (the (i) to (iv) markers of R5-18 are inline), no "not X but Y" contrasts, and none of the banned vocabulary in CLAUDE.md.
5. The main text must still end on page 9 with a single outcome selected. Measured at fceef7c in a scratch build of all six (\Ronecase, \Rtwocase) combinations: maintext:end falls on page 9 in every build, followed on page 9 by the AI-use statement and most of the ethics statement, about 12 text lines. This round spends part of that. Word caps, raw `wc -w` over the slice with each \citep counted as zero, against the fceef7c counts (A 973, B 1604, C 1181, D 2006 for lines 199-241): A at most 1043 (+70), B at most 1669 (+65), C at most 1151 (-30, a banked cut), D at most 2041 (+35). The reproducibility statement (300 words) sits after maintext:end and doesn't count. Report before and after counts in the status file. The coordinator recompiles all six combinations after the merge and checks `maintext:end` in each aux file; that check is binding, and if any build spills to page 10 the coordinator drops edits in the order given under "Trim order" below.
6. The \pending and \outcome scaffolding in the abstract, the introduction and sec:family stays, with every branch, and so do all \label commands. The AI-use and ethics statements (main.tex:242-246) stay untouched.
7. Don't edit outside your slice. Pointers use labels that already exist (app:derivations, app:design, app:schedule, app:supplementary, app:formatfit, app:scale, app:paired, app:composition, app:checkpoint, app:calibration, app:proxy, app:transport, app:heldout, app:decision, app:implementation, app:centring, sec:estimator, sec:data, sec:composition, sec:heldout, sec:family, sec:prediction, tab:primary, tab:heldout, tab:prediction, tab:decision, alg:snap).
8. Shared-component wording (replaces round 4 rule 8). No sentence may say or imply that the cross-format diagnostic excludes a uniform shared item component outright. Every such sentence says the component acts alone (with no cross-benchmark run covariance), and where this brief gives the bound it states that such a component could still carry up to 0.319 of the margin excess and that negative cross-format run covariance, which the observed 0.921 below one implies, could offset a larger one. 1.113 stays as the value a component carrying the whole excess alone would produce. Nothing may say a shared component is impossible or exceeds the item noise (round 4 rule 8 still holds on that).
9. Don't cut what an earlier round fixed. You may not delete a sentence or clause that a round 1 to 4 fix added unless this brief names that finding and says where its content survives in the main text. Moving a sentence keeps it. If you need a cut this brief doesn't list, SKIP the addition that needed it and report the word count instead. `git log -S "<phrase>" -- deliverables/main.tex` shows which commit added a phrase (e33a01d, f23e293, 2becd1b and fceef7c are rounds 1 to 4).
10. Define and split before you add. Where a term is used undefined or a sentence is over-compressed, the fix is a short definition or a split sentence. Add no claim beyond the ones this brief writes out.
11. Write one status line per finding to work/rebuttal/r5/status_{A,B,C,D,APP}.md as FIXED, SKIPPED (with reason and word count) or NO-CHANGE, quoting the new text.

## Slice A (main.tex 28-45)

R5-01, R5-03, R5-04, R5-06 and R5-35 all edit the abstract, so apply them as one rewrite of the sentences outside the \outcome scaffolding. Target text for those sentences, with the SNAP sentence, the pre-specified-test sentence, the outcome scaffolding and the "Beyond the pre-specified test" sentence kept as they are:

"A run-to-run standard error of a benchmark average that sums per-benchmark variances ignores covariance between benchmarks, and in simulation a margin comparison using it declares a difference in 0.113 of null replicates, against a nominal 0.05. [SNAP sentence unchanged.] On 375 DataDecide runs at each configuration's last shared checkpoint, the ten-benchmark per-byte margin average has inflation, the ratio of its standard deviation to that under independence, of 1.244 \ci{1.143}{1.338}, or 1.237 \ci{1.137}{1.331} from replicate standard deviations alone, which need no item split. BoolQ carries 68\% of the covariance trace, and single-benchmark removals give 1.096 to 1.786. Acting alone, a uniform item component shared by all benchmarks could carry at most about a third of the margin excess before inflation across scoring formats leaves \ci{0.801}{1.035}. The accuracy interval \ci{0.993}{1.157} includes one, at 0.135 power against a true 1.10, and on DataDecide's recipe comparisons the correction removes at most 12 of the 173 to 223 calls on pairs with a resolved 1B ordering. [Pre-specified test sentence unchanged.] [Scaffolding unchanged.] Beyond the pre-specified test and two PolyPythias rules, analyses are exploratory. We recommend reporting the replicate standard deviation of the battery average with a benchmark-removal check."

That is 228 words outside the scaffolding against 180 now. Sources: 1.237 \ci{1.137}{1.331} from results/snap-r2-r1-08 (printed at main.tex:63); "about a third" from results/snap-r5-shared-bound (0.3187); the resolved-pair scope from main.tex:222 and compute extra/heldout/outputs/k05-decision/decision_results.json (calls_on_resolved). Named cuts under rule 9: the abstract's "1.113" sentence (R4-01) goes, and 1.113 survives in the main text at main.tex:71 (slice B keeps it); the decision clause (R4-04) moves from the first sentence to the accuracy sentence and keeps its numbers. If the sentence order reads badly, keep the content and reorder, but don't drop the resolved-pair scope (R5-03) or the 1.237 (R5-04).

R5-05, R5-18 and R5-21 (edit line 45 together). Replace the paragraph with:

"This paper makes four contributions to measuring run covariance. (i) We define the item-general run covariance that cross-half products estimate, identified up to an item component shared across benchmarks and halves. (ii) The replicate standard deviation of the battery average gives the uncertainty of rerunning the released items and is what we recommend reporting, while the split adds per-benchmark run variances net of item noise and a redrawn-bank target, untested on DataDecide, and the two part where item noise is large, as for accuracy without BoolQ at 1.285 against 1.558 (Section~\ref{sec:estimator}). (iii) Algorithm~\ref{alg:snap} gives the measurement recipe, with near-nominal intervals unless a run effect follows the size band. (iv) We measure it across the 375 released runs, where every recipe, recipe-pair and benchmark deletion, along with four of the five size-band deletions, leaves a margin interval that excludes one."

Sources: 1.285 and 1.558 from results/snap-r3-r2-26/r2_26_analytic_vs_split.json (printed at main.tex:63). Named cuts under rule 9: "untested on DataDecide" (R4-03) stays in (ii); the coverage range 0.928 to 0.954 (R3-26) leaves this paragraph and survives at main.tex:113; "where margin inflation is 1.244" leaves (iv) and survives at main.tex:39. The leverage-corrected interval (KA9, R4-28) stays out.

R5-12. Line 43: "so a pass confirms the effect while a failing or undecided verdict only bounds it" becomes "so a first-rule pass confirms the effect, a failure bounds it or counts against transfer, and an undecided second rule leaves the shared-item question open". Keep 0.259 and 0.761.

R5-30. Line 37: after "with per-benchmark reliability averaging 0.652 against 0.324 for accuracy" insert "(run variance's share of a benchmark's replicate variance)" before the section pointer. Source: appendices_bcd.tex:34 defines reliability as $\widehat\sigma_E^2/(\widehat\sigma_E^2+\widehat\sigma_{\mathrm{noise}}^2)$.

R5-34 and R5-38 (edit line 41 together). "SciQ \citep{welbl2017sciq}, MedMCQA \citep{pal2022medmcqa}, and multiple-choice DROP \citep{dua2019drop} and CoQA \citep{reddy2019coqa} on the same checkpoints at 530M, 750M, and 1B, whose rule requires held-out margin inflation's lower interval limit to exceed one." becomes "SciQ \citep{welbl2017sciq}, MedMCQA \citep{pal2022medmcqa}, and DROP \citep{dua2019drop} and CoQA \citep{reddy2019coqa} in multiple-choice form, on the same checkpoints at 530M, 750M, and 1B. Its rule requires the lower interval limit of held-out margin inflation to exceed one."

Cuts that pay for A (none removes round 1 to 4 content): line 39, drop "with recipe-cluster interval", ", which excludes one," and ", which includes one" (keep "at the scored checkpoint"; main.tex:134 and Table 1 carry the verdicts); line 43, drop "on the same 37,682 items" (main.tex:127 carries it); line 37, drop "on the same items" at the end of the second sentence. If A still runs over 1043, shorten the R5-12 clause to "so a first-rule pass confirms the effect and other verdicts read as Section~\ref{sec:family} describes" before cutting anything else.

## Slice B (main.tex 46-130)

R5-01 (B part, line 71). Replace "A uniform component shared across benchmarks and large enough to carry the observed excess of 0.244 would raise inflation from cross-format covariances alone to 1.113, which the observed 0.921 \ci{0.801}{1.035} excludes (Appendix~\ref{app:calibration}), while a component that follows scoring format is untested in DataDecide, and Section~\ref{sec:family}'s cross-bank rule tests it in PolyPythias." with:

"Cross-format inflation, which keeps only the diagonal and the covariances between benchmarks of different scoring formats, is 0.921 \ci{0.801}{1.035}. A uniform component shared across benchmarks and acting alone would lift it to 1.113 if it carried the whole excess of 0.244, and the upper limit admits at most 0.319 of that excess, although negative run covariance across formats, which 0.921 below one implies, could offset a larger component (Appendix~\ref{app:calibration}). A component that follows scoring format is untested in DataDecide, and Section~\ref{sec:family}'s cross-bank rule tests it in PolyPythias."

Sources: results/snap-r5-shared-bound/r5_shared_bound.json (margin observed cross_format_wild 0.9212 [0.8007, 1.0347], explain_induced_cross_format 1.1133, fraction_of_excess_lambda_scale 0.3187, and the bound_at_observed_point note that a value below one requires negative cross-format seed covariance). 1.113 and the format-following clause (R4-01) stay.

R5-16 (lines 63, 71, 93). Move the three sentences of line 63 from "On the released bank, the replicate standard deviation" to the end of the paragraph so that they follow the last sentence of line 93, which introduces the analytic item-by-run correction. Rewrite the third of them to open "Under that item-by-run model, item noise is 0.037 of the full-bank margin diagonal and 0.186 of the accuracy one, ..." and keep the rest of it (CA6; results/snap-r3-r2-26 share_of_diag_removed). At line 71, "The fixed-bank 1.237 needs no halves and no cross-half assumption" becomes "The fixed-bank inflation reported below needs no halves and no cross-half assumption", and the rest of that sentence stays. Line 63 keeps its sentence ending "so reproducibility on that fixed bank is a separate question". Under rule 9 this moves R2-12, R3-03, R4-09 and R4-10 content inside Section 2 without deleting any of it.

R5-14. Line 49: "DataDecide's seed controls initialisation and data order together, and auxiliary training schedules add further run variation." becomes "DataDecide's seed controls initialisation and data order together. Each configuration has one default and two auxiliary runs, and below 1B the auxiliary runs stop at 25 percent of the 1B compute budget, which adds run variation beyond the seed." Sources: appendices_bcd.tex:6 ("DataDecide stops the two auxiliary runs below 1B at 25 percent of the 1B compute budget"), and seednoise population.py (batch 0 default, 1 and 2 auxiliary). This restores the sentence the round 4 slice B cut removed from line 119.

R5-15. Line 123: "and its gain check G5 failed under the default simulator" becomes "and its check G5, that a run-level multiplicative gain explains under half the margin excess, failed under the plan's default simulation settings". Source: supplement_extended_body.tex:95 ("G5 & N5 accounts for less than 50 percent of excess ... Fail, default simulator share 0.61") and appendices_bcd.tex:66, :375. Keep the Appendix~\ref{app:design} pointer.

R5-17. Line 121, after the sentence on the 33 configurations that share a final step, add: "A configuration's influence on the margin estimate has Spearman correlation $-0.036$ with its scored step's share of the default final step (Appendix~\ref{app:schedule})." Source: appendices_bcd.tex:10. This restores the round 4 slice B cut with the number.

R5-19. Line 117: cite each benchmark after its name, so the two Clark keys fall in different \citep calls: "ARC-Challenge and ARC-Easy \citep{clark2018arc}, BoolQ \citep{clark2019boolq}, CommonsenseQA \citep{talmor2019commonsenseqa}, HellaSwag \citep{zellers2019hellaswag}, MMLU \citep{hendrycks2021mmlu}, OpenBookQA \citep{mihaylov2018openbookqa}, PIQA \citep{bisk2020piqa}, Social IQa \citep{sap2019socialiqa}, and WinoGrande \citep{sakaguchi2020winogrande}", and delete the single list at the end.

R5-28. Line 91: "Because both quantities normalise by the battery's own trace, they can rise when removing a high-variance benchmark lowers $\sigma_{\mathrm{agg}}$," becomes "Because $\Lam$ and $\bar r_E$ normalise by the battery's own trace, they can rise when removing a high-variance benchmark lowers $\sigma_{\mathrm{agg}}$, the standard deviation of the battery mean,".

R5-29. Line 86: "is the configuration-averaged covariance matrix" becomes "is the covariance matrix averaged over the $N$ configurations".

Cuts that pay for B: the R5-01 rewrite runs about 34 words longer, and B's other additions add about 64, so B must bank about 33 words. Bank them, in this order, from text no earlier round added: line 93, "Unbiased estimates of $T_c$ and $U_c$ don't make their ratio or square root unbiased, and either pooled sum can be nonpositive in a finite sample, which our calibration simulations check." becomes "Either pooled sum can be nonpositive in a finite sample, and our calibration simulations check the bias of the ratio." (about 13 words; the round 1 clause "which our calibration simulations check" keeps its content); then shorten the R5-17 addition to "Influence on the estimate shows no rank correlation with truncation, at Spearman $-0.036$ (Appendix~\ref{app:schedule})." If B still exceeds 1669, SKIP R5-28's σ_agg clause and report the count.

## Slice C (main.tex 131-198)

R5-01 (C part, format list; PA7). Line 157: "since margin correlations track scoring format," becomes "since margin correlations track scoring format (cloze for HellaSwag, PIQA and WinoGrande, yes/no for BoolQ, and multiple choice for the other six),". Source: results/snap-r5-shared-bound/r5_shared_bound.json "formats" (arc_challenge, arc_easy, csqa, mmlu, openbookqa, socialiqa: mc; hellaswag, piqa, winogrande: cloze; boolq: yesno), consistent with appendices_bcd.tex:130 and :132.

R5-31. Line 159: "inside every group" becomes "inside each of the 66 item groups, MMLU's 57 subjects and the other nine benchmarks,". The split 57 + 9 = 66 is inferred from appendices_bcd.tex:263 and main.tex:56; confirm it against the permutation kernel before writing, and otherwise write "inside each of the 66 item groups of Appendix~\ref{app:calibration}".

Cuts that bank C's 30 words (all three sentences predate the loop, and their content is in the appendix): line 134, delete "Inflation reaches 1.244 on margins, where the raw interval excludes one, and 1.078 on accuracy, where it includes one (Table~\ref{tab:primary})." and end the first sentence with "(Table~\ref{tab:primary})"; line 161, delete "Among equal-weight subsets of the same size, those containing BoolQ average 0.115 lower margin inflation at two benchmarks and 0.596 lower at nine than those without it." (appendices_bcd.tex:206 keeps it); line 161, delete "The corresponding accuracy ranges run from 1.065 to 1.099 and from 1.047 to 1.115." (appendices_bcd.tex:191 keeps it).

## Slice D (main.tex 199-241, reproducibility statement 248-249)

R5-13. Line 203, from "In an exploratory reading" to the end of the paragraph, becomes: "In an exploratory reading we added after fixing that rule, a failing estimate at or above 1.026, the 5th percentile of estimates at a true 1.244, is read only through its jackknife upper limit, which bounds inflation in this family, because under independence the middle 90\% of estimates runs from 0.848 to 1.139 and overlaps that range, so the estimate alone can't separate transfer from independence. A failing estimate below 1.026 counts against transfer." Sources: results/snap-r2-r1-18b (1.0258) and results/snap-r2-r1-18 (independence_share0 within_lambda_5_95 [0.8479, 1.1390]). The R1-fail branch's "\pending{bounds the effect in this family / counts against transfer}" stays.

R5-01 (D part). Line 235: "A uniform item effect shared across benchmarks that produced the margin value would lift cross-format inflation outside its interval (Section~\ref{sec:estimator})," becomes "A uniform item effect shared across benchmarks and acting alone could carry at most 0.319 of the margin excess before cross-format inflation leaves its interval (Section~\ref{sec:estimator})," and the accuracy clause and the three \outcome{\Rtwocase} sentences stay. Source: results/snap-r5-shared-bound (0.3187).

R5-02 (D part). Line 239: "In a population with the released item counts and equicorrelated seed covariance at the observed $\bar r_E$ of 0.061 on margins and 0.018 on accuracy, three runs each let the margin interval exclude one in 0.997 of replicates, and accuracy needs eight to reach 0.803." becomes "In an equicorrelated population at the observed $\bar r_E$ of 0.061 on margins and 0.018 on accuracy, with the default simulator's item noise, three runs each let the margin interval exclude one in 0.997 of replicates and accuracy needs eight to reach 0.803, but at the accuracy battery's own noise three runs detect a true 1.078 in only 0.111 of replicates (Appendix~\ref{app:supplementary})." Sources: compute extra/heldout/outputs/k08-seed-count/seed_count_power.json, compute extra/heldout/kaggle/k08-seed-count/k08-seed-count.py (default_spec with no item_sd), appendices_bcd.tex:381 and :393 (0.111). R4-27's $\bar r_E$ label stays.

R5-27. Move the sentence of line 239 that begins "Against one configuration's six-run margin ratio" to the end of the paragraph as a new paragraph and rewrite it as: "On the 4,755-item PolyPythias transfer runs at three sizes (Appendix~\ref{app:transport}), we fitted each configuration on three of its nine seeds and predicted the margin ratio measured on the other six. Independence predicts it with mean absolute log error 0.339, against 0.536 for the plug-in and 0.512 for SNAP with item noise added to both sides, while against a target pooled across configurations SNAP beats independence, 0.275 against 0.319 on margins and 0.367 against 0.523 on accuracy, and only this pooled target resembles the recipe folds of Section~\ref{sec:prediction}." Source: appendices_bcd.tex:345 (84 three-run fits, six left-out runs, "adds each benchmark's estimated item noise to both sides").

R5-32 and R5-33 (line 237). "A permutation test that aligns runs by batch slot bounds that effect at 0.044 of the seed covariance on margins" becomes "A permutation test that lines up each configuration's default and auxiliary runs across a band's recipes bounds that effect at 0.044 of the run covariance on margins". Sources: results/snap-r3-r2-43/r2_43.py (runs ordered by batch), seednoise population.py (batch 0 default, 1 and 2 auxiliary).

R5-33 (line 220) and R5-36. "fitting the seed-covariance models off fold" becomes "fitting the run-covariance models off fold", and "On margins the rank-one, rank-one plus gain, and full models" becomes "On margins the rank-one, rank-one plus gain (a run-level multiplicative gain), and full models". Change "seed covariance" to "run covariance" nowhere else.

R5-37. Line 233: "and on the held-out tasks, don't settle it." becomes "and on the held-out tasks, among others listed in Appendix~\ref{app:supplementary}, don't settle it."

R5-22 (reproducibility statement). "500 permutation replicates" becomes "500 and 2,000 permutation replicates for the two permutation checks" (appendices_bcd.tex:117 and :263).

Cuts that pay for D: line 235, delete ", although the fixed-bank value of 1.237 (Section~\ref{sec:estimator}) needs no such check for the released bank" (R3-03; its content survives at main.tex:71, whose R4-09 sentence says the fixed-bank value needs no halves and no cross-half assumption, and in the abstract after R5-04); line 216, "cross-format off-diagonal covariance alone gives diagnostic inflation of 0.921 with interval \ci{0.801}{1.035} against 1.244 for the full matrix (Appendix~\ref{app:supplementary})" becomes "cross-format inflation (Section~\ref{sec:estimator}) is 0.921 against 1.244 for the full matrix" (the interval survives at main.tex:71 and in the abstract).

## Slice APP

R5-01 (APP part). appendices_bcd.tex:128: "and below the 1.113 that a uniform shared item component carrying the whole margin excess would imply" becomes "and below the 1.113 that a uniform shared item component carrying the whole margin excess alone, with no seed covariance, would imply". appendices_bcd.tex:261: after the sentence ending "which the observed interval \ci{0.801}{1.035} excludes.", add "Under the same zero-seed-covariance model the largest share the upper limit allows is 0.420, which on its own carries 0.319 of the margin excess on the inflation scale and 0.295 on the covariance scale, and no positive share fits the point estimate, since a value below one already requires negative cross-format seed covariance, which could also offset a larger component. On accuracy the limit allows a share of 0.072, above the 0.053 that explains all of 1.078, so it doesn't bind." Then "A uniform component shared by every benchmark is therefore excluded on margins by the cross-format diagnostic," becomes "A uniform component shared by every benchmark is therefore excluded on margins as the whole explanation only when it acts alone,". Source for all: results/snap-r5-shared-bound/r5_shared_bound.json. Don't print 1.0778.

R5-02 (APP part). appendices_bcd.tex:456: "The seed-count simulation draws populations with 125 configurations, the released item counts, and an equicorrelated seed covariance" becomes "The seed-count simulation draws populations from the default item-level simulator, with 125 configurations, the released item counts, its default item noise, and an equicorrelated seed covariance", and after "These shares describe a battery with the same $\bar r_E$ and not this battery's structure." add "At the accuracy battery's own noise the same three-run design detects 1.078 in 0.111 of replicates and in 0.103 against the measured matrix, so the accuracy shares here overstate its power." Sources: k08-seed-count.py (default_spec called with no item_sd), appendices_bcd.tex:381 (0.111), supplement_extended_body.tex:673 (0.103).

R5-39. appendices_bcd.tex:142: "a gap that only items with a margin of exactly zero can produce" becomes "a gap that only items whose stored margin is zero, from exact ties or float16 underflow, can produce". Source: seednoise/store.py stores margins as float16, and phenotypes.py sets correctness to margin > MARGIN_EPS with MARGIN_EPS = 0.0.

R5-40. appendices_bcd.tex:389: "while the cluster-robust $t$ interval holds 0.935 and 0.898" becomes "while the size-clustered cluster-robust $t$ interval holds 0.935 and 0.898". Source: research/results/simulation_cells.csv, snap-r6-size-shared rows, cluster_t_size pooled over 12,000 replicates 0.9348 and 0.8983 (cluster_t_recipe 0.8210 and 0.5778).

R5-41. appendices_bcd.tex:204: "The conclusion on margins holds across four fifths of the simplex and all of its neighbourhood." becomes "The conclusion on margins holds in 0.817 of flat-Dirichlet weightings and in all 2,000 near-flat ones."

## Trim order (coordinator, only if a single-outcome build spills to page 10)

Drop in this order and recompile after each step: R5-37, R5-36, the R5-33 wording changes, R5-28's σ_agg clause, R5-21's clause in (iii) (keep "near-nominal intervals"), R5-17's added sentence, then the "the replicate standard deviation of that average" part of R5-04 moves out of the abstract into the introduction's line 39. Never drop R5-01, R5-03, R5-12, R5-13, R5-14, R5-15 or R5-16.

## Not for fixers

NEEDS-EXPERIMENT, CPU and exploratory: R5-07 (observed false-positive rate on null seed pairs within PolyPythias configurations, from the reduced runs in research/outputs/snap-r12-polypythias, whose suitability is unverified), R5-09 (rule-one power with item noise from the 4,755-item PolyPythias transfer reductions), R5-20 (rule-two verdict rates at the share that reproduces 1.244 on PolyPythias noise). GPU: R5-11 (DataDecide on bank two), R5-26 (larger or multi-format battery, matched final checkpoints, held-out loss, populations above 1B or generative). REJECTED: R5-08, R5-10, R5-23, R5-24, R5-25, R5-42 to R5-51, reasons in REBUTTAL_LOG.md.

Out-of-loop action for the author, carried from round 3 (KA7): a third-party timestamp of the rule commit made before PolyPythias scoring ends is the only way the paper can cite independent corroboration for the two rules (R5-23). It reaches an outside service, so the loop doesn't do it.
