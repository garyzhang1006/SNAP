# Slice D status, round 5

Word count (raw `wc -w`, \citep as zero, sec:family through \label{maintext:end}): orig 2006, new 2041 (cap 2041). Whole file: orig 2455, new 2497. AI-use and ethics statements byte-identical (diff checked). No em or en dashes.

- R5-13 FIXED (brief text verbatim): "In an exploratory reading we added after fixing that rule, a failing estimate at or above 1.026, the 5th percentile of estimates at a true 1.244, is read only through its jackknife upper limit, which bounds inflation in this family, because under independence the middle 90\% of estimates runs from 0.848 to 1.139 and overlaps that range, so the estimate alone can't separate transfer from independence. A failing estimate below 1.026 counts against transfer." Sources: results/snap-r2-r1-18b (1.0258), results/snap-r2-r1-18/r1_18_rule_two.json (independence_share0 within_lambda_5_95 [0.8479, 1.1390]). R1-fail \pending branch kept.
- R5-01 FIXED (D part, brief text): "A uniform item effect shared across benchmarks and acting alone could carry at most 0.319 of the margin excess before cross-format inflation leaves its interval (Section~\ref{sec:estimator})," accuracy clause and three \outcome{\Rtwocase} sentences kept. Source: results/snap-r5-shared-bound/r5_shared_bound.json (fraction_of_excess_lambda_scale 0.3187). Named cuts applied: ", although the fixed-bank value of 1.237 ... for the released bank" deleted; line 216 now "cross-format inflation (Section~\ref{sec:estimator}) is 0.921 against 1.244 for the full matrix".
- R5-02 FIXED: "In an equicorrelated population at the observed $\bar r_E$ of 0.061 on margins and 0.018 on accuracy, with the default simulator's item noise, three runs each let the margin interval exclude one in 0.997 of replicates and accuracy needs eight to reach 0.803, but at the accuracy battery's own noise three runs detect a true 1.078 in 0.111 of replicates (Appendix~\ref{app:supplementary})." Sources: compute extra/heldout/outputs/k08-seed-count/seed_count_power.json (0.9967, 0.8033), k08-seed-count.py:71 (default_spec, no item_sd), appendices_bcd.tex:393 (0.111). Dropped the brief's "only" before 0.111 for the word cap.
- R5-27 FIXED, moved to a new final paragraph and split into three sentences (brief text minus "measured", "while" and "pooled" for the word cap): "On the 4,755-item PolyPythias transfer runs at three sizes (Appendix~\ref{app:transport}), we fitted each configuration on three of its nine seeds and predicted the margin ratio on the other six. Independence predicts it with mean absolute log error 0.339, against 0.536 for the plug-in and 0.512 for SNAP with item noise added to both sides. Against a target pooled across configurations SNAP beats independence, 0.275 against 0.319 on margins and 0.367 against 0.523 on accuracy, and only this target resembles the recipe folds of Section~\ref{sec:prediction}." Source: appendices_bcd.tex:345. Note: this 0.319 (independence error) sits two paragraphs from the shared-bound 0.319; both are sourced, different quantities.
- R5-32 FIXED in shortened form (brief text ran 7 words over the cap): "A permutation test that aligns default and auxiliary runs across recipes bounds that effect". Source: results/snap-r3-r2-43/r2_43.py (runs ordered by batch), seednoise population.py (batch 0 default, 1 and 2 auxiliary).
- R5-33 FIXED: line 237 "0.044 of the run covariance on margins"; line 220 "fitting the run-covariance models off fold". No other "seed covariance" changed.
- R5-36 SKIPPED for the word cap (first in trim order after R5-37; would add 4 words, "(a run-level multiplicative gain)", taking D to 2045).
- R5-37 SKIPPED for the word cap (first in trim order; would add 5 words, "among others listed in Appendix~\ref{app:supplementary},", taking D to 2046).
- R5-22 FIXED (reproducibility statement, outside the cap): "500 and 2,000 permutation replicates for the two permutation checks". Source: appendices_bcd.tex:117 (500) and :263 (2,000).
