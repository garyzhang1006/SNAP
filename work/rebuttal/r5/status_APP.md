# Round 5 status, slice APP (deliverables/appendices_bcd.tex only; appendix_a.tex, references.tex and supplement_extended_body.tex unchanged, since the brief assigns them no APP edit)

- R5-01 FIXED. appendices_bcd.tex:128 now reads "below the 1.113 that a uniform shared item component carrying the whole margin excess alone, with no seed covariance, would imply". appendices_bcd.tex:261 adds "Under the same zero-seed-covariance model the largest share the upper limit allows is 0.420, which on its own carries 0.319 of the margin excess on the inflation scale and 0.295 on the covariance scale, and no positive share fits the point estimate, since a value below one already requires negative cross-format seed covariance, which could also offset a larger component. On accuracy the limit allows a share of 0.072, above the 0.053 that explains all of 1.078, so it doesn't bind." and changes the conclusion to "A uniform component shared by every benchmark is therefore excluded on margins as the whole explanation only when it acts alone,". Source results/snap-r5-shared-bound/r5_shared_bound.json, kernel garyzhang11111/snap-r5-shared-bound (largest_share 0.4198, fraction_of_excess_lambda_scale 0.3187, covariance scale 0.2951, accuracy largest_share 0.0717, q* 0.0526; bound_at_observed_point note). 1.0778 not printed.
- R5-02 FIXED. appendices_bcd.tex:456 now opens "The seed-count simulation draws populations from the default item-level simulator, with 125 configurations, the released item counts, its default item noise, and an equicorrelated seed covariance" and ends with the added "At the accuracy battery's own noise the same three-run design detects 1.078 in 0.111 of replicates and in 0.103 against the measured matrix, so the accuracy shares here overstate its power." Sources: compute extra/heldout/kaggle/k08-seed-count/k08-seed-count.py:71 (default_spec called with no item_sd), appendices_bcd.tex:381 (0.111), supplement_extended_body.tex:673 (0.103).
- R5-39 FIXED. appendices_bcd.tex:142 "a gap that only items whose stored margin is zero, from exact ties or float16 underflow, can produce". Source: seednoise/store.py:26 (astype(np.float16)), phenotypes.py:30 and :148 (MARGIN_EPS = 0.0, correct = margin > MARGIN_EPS).
- R5-40 FIXED. appendices_bcd.tex:389 "while the size-clustered cluster-robust $t$ interval holds 0.935 and 0.898". Numbers unchanged; research/results/simulation_cells.csv snap-r6-size-shared rows confirm the direction (cluster_t_size stays near nominal, cluster_t_recipe falls to 0.808 and 0.5785 on the 2,000-replicate rows I pooled; I did not reproduce the brief's 12,000-replicate pooled 0.9348 and 0.8983, which likely combine rows from other kernels, and the printed values were left as they were).
- R5-41 FIXED. appendices_bcd.tex:204 "The conclusion on margins holds in 0.817 of flat-Dirichlet weightings and in all 2,000 near-flat ones." (0.817 and 2,000 already printed earlier in the same paragraph.)

Checks: \label count in appendices_bcd.tex 27 before and after; no em or en dashes in added lines; no citations added; nothing compiled.
