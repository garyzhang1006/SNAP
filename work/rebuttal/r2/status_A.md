Slice A status, round 2. wc -w: slice_A.orig.tex 1145, slice_A.new.tex 971 (abstract 345 to 344; 22 lines to 18).

- R2-03 FIXED. Abstract, both R2=1 branches: "leaves \pending{cross lambda}, which gives no support to the shared-item explanation". Introduction: "the shared-item explanation is \outcome{\Rtwocase}{1}{R2 not supported}{not supported}...".
- R2-07 FIXED. Paragraph 1 now ends: "against a nominal rate of 0.05 (Appendix~\ref{app:supplementary}), and the same understatement reaches the error bar of one model's battery average and the number of replicate runs a study plans." The "On DataDecide's recipe comparisons ..." sentence is deleted (contradicted by results/snap-r2-r1-20/r1_20_seed_mismatch.json). 0.113 source: appendices_bcd.tex:218, inside app:supplementary.
- R2-10 FIXED. Abstract reordered and shortened by one word: "A run-to-run standard error for a benchmark average built from per-benchmark variances alone ignores the covariance between benchmark scores, and in simulation a comparison using it declares a difference in 0.113 of replicates with no true gap, against a nominal 0.05. SNAP measures that covariance for a fixed battery of likelihood-scored multiple-choice benchmarks from cross-half products of run-centred item-half scores. Across 375 released DataDecide runs in 125 configurations, the standard deviation of the ten-benchmark per-byte margin average is 1.244 times its independent value, with interval \ci{1.143}{1.338}. BoolQ carries 68\% of the covariance trace and single-benchmark removals move the factor from 1.096 to 1.786, so batteries compare through the aggregate standard deviation. A shared item component at 0.124 of item-noise variance would reproduce 1.244. Our accuracy interval \ci{0.993}{1.157} includes one." Registered test, \outcome block, exploratory sentence and recommendation unchanged apart from R2-03. Dropped to fit: "whose replicates differ in seed and partly in checkpoint schedule", "under a low-powered design", and "at the largest checkpoint step shared within each of" (the introduction keeps the checkpoint rule). 1.096 and 1.786 from main.tex:163; 0.124 from main.tex:241.
- R2-11 FIXED. Lines 47 and 49 merged: "This paper makes four contributions to measuring run covariance. We define the item-general run covariance that cross-half products estimate, identified up to an item component shared across benchmarks and halves. Algorithm~\ref{alg:snap} gives the measurement recipe with calibrated intervals, whose wild recipe-cluster coverage runs from 0.928 to 0.954 across twelve simulated populations. We measure it on two score scales across the 375 released runs, where margin inflation is 1.244 and every recipe, recipe-pair and benchmark deletion, along with four of the five size-band deletions, leaves an interval that excludes one, the exception being 530M with a lower limit of 0.993. We also register a test on unseen tasks and report that it fails at its registered scope." Cut: the K_eff clause, the 0.626/0.343 clause, the 0.113 to 0.051 sentence, the exploratory sentence, and also "and state which assumptions the released runs can't be shown to satisfy" and "at a magnitude that BoolQ sets" to keep the four items flat.
- R2-18 FIXED. Line 41 paragraph deleted; line 39 now ends "(Table~\ref{tab:primary}), although at accuracy-like item noise this design detects a true ratio of 1.10 in only 0.135 of replicates." Source research/outputs/snap-r6-detect-power/r6_detect_power.json, cells.lambda_1.10.wild_recipe.excludes_one_rate = 0.13525.
- R2-31 FIXED. "with 25 data recipes and five of its model sizes, each with three released replicates" and "a disjoint bank of 6,808 items built with the OLMES harness at the commit DataDecide used". No citation added.
