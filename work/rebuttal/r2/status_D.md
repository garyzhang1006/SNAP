# Slice D status (round 2)

Word count: slice_D.orig.tex 2458, slice_D.new.tex 2457 (wc -w). Both new R2 Discussion branches count in wc though only one prints, so the printed text shrinks by more than one word. \outcome count 6 to 8, every \pending kept, \label{maintext:end} still ends the Discussion, no em or en dashes.

- R2-04 FIXED: "It returns not supported when that cross-bank interval sits above one and the jackknife interval for the within-minus-cross difference in log squared inflation includes zero."
- R2-05 FIXED (paragraph split after the bank caveats; setup sentence shortened and pointed at app:transport, which appendices_bcd.tex:349 already carries in full): "At a true 1.244 with no shared item component the second returns not supported in 0.226, supported in 0.013 and undecided in 0.761, and under independence with a 0.124 item share it returns supported in 0.033 and undecided in 0.957, so an undecided verdict is expected under either explanation and not supported doesn't exclude a shared component. A first-rule pass confirms the effect in this family. In an exploratory reading we added after fixing that rule, a failing estimate inside the simulated range is a bound and one below 1.026 counts against transfer." Source results/snap-r2-r1-18/r1_18_rule_two.json (run_cov_1244_share0: 0.22575, 0.013, 0.76125; independence_share0124: 0.03275, 0.957). I wrote "under independence with a 0.124 item share" instead of "a 0.124 share alone produces the within-bank excess", since the same file puts that scenario's within-bank median at only 1.046.
- R2-06 FIXED: "Over 4,000 replicates the first rule passes in 0.259 at a true 1.244 and in 0.016 at independence, with the middle 90\% of estimates between 1.026 and 1.456." Source results/snap-r2-r1-18b/r1_18b_jackknife_recentring.json (pass_rate_recentred_jackknife 0.2585, null 0.0155, p5_p95 1.0258 and 1.4563), rounded half-up.
- R2-08 FIXED: main.tex:228 now reads "In single-run comparisons the correction changes the wrong-call rate by at most 0.019 for runs sharing a seed label and 0.016 for runs that don't, every interval reaching zero, and the two kinds of pair have paired-difference deviations within a factor of 0.928 to 1.025, so seed matching doesn't explain the few changed calls." The 1.024 clause at main.tex:245 is deleted, and the 1.024 median stays at main.tex:226. Source results/snap-r2-r1-20/r1_20_seed_mismatch.json (max |diff| 0.0193 matched, 0.0160 mismatched; sd_ratio_matched_over_mismatched 0.9282 to 1.0245). Deviation from the brief: the brief's "0.93 to 1.03" isn't a half-up rounding of 1.0245 (that gives 1.02), so I printed 0.928 to 1.025 at the paper's three-decimal precision. I also dropped "which seed-matched paired comparisons cancel" from main.tex:243 for the same reason and to pay for length.
- R2-09 FIXED: "Against one configuration's six-run margin ratio on the earlier three-size PolyPythias runs, whose middle 90\% runs from 0.70 to 1.85, independence predicts with mean absolute log error 0.339 against 0.536 for the plug-in and 0.512 for our decomposition, while against a target pooled across configurations the decomposition beats independence, 0.275 against 0.319 on margins and 0.367 against 0.523 on accuracy (Appendix~\ref{app:transport})." Source appendices_bcd.tex:343 and 345. "Our ratio therefore measures this battery ..." kept.
- R2-22 FIXED: main.tex:220 opens "Margin correlations track scoring format (Section~\ref{sec:composition}), and cross-format off-diagonal covariance alone gives ...", and the fit sentence keeps "their mean of 0.624 and cross-format ones to 0.007". The 0.05 result moved into the 0.124 sentence: "..., while at a share of 0.05 only 0.007 of simulated replicates reach the margin value. We haven't measured such an effect or separated it from seed covariance (Appendix~\ref{app:calibration})." The passage-aware sentence and the whole sign-reversal sentence (0.246, 0.229, 455 of 1,500) are cut, along with the dependent "However, noisy gaps give no lower bound ..." sentence; APP must land these under R2-40. Extra length cuts in the same subsection: the equal-variance sentence now reads "With equal variances the same correlations would give 1.803, a gap mostly due to BoolQ's 68\% trace share.", and "next to the scoring-scale and proxy analyses" is gone.
- R2-23 FIXED: added "\outcome{\Rtwocase}{2}{R2 supported}{ The cross-bank check in PolyPythias finds such a component in one family, so the within-bank value can overstate run covariance there.}" and "\outcome{\Rtwocase}{3}{R2 undecided}{ The cross-bank check in PolyPythias leaves that item effect undecided in one family.}" after the R2=1 sentence.
- R2-24 FIXED: "adapts the split-half construction of \citet{spearman1910,brown1910}, the attenuation correction of \citet{spearman1904}, and the variance components of \citet{searle1992} and generalizability theory \citep{brennan2001}, and what we add is the estimand and its measurement." cronbach1951 is dropped here and is still cited at appendices_bcd.tex:77.
- R2-35 FIXED: "take simulated margin coverage over 10,000 replicates to 0.823 at a quarter of latent variance and 0.581 at a half, although the size-band-clustered margin interval \ci{1.030}{1.426} of Table~\ref{tab:primary} still excludes one." Source research/outputs/snap-r6-size-shared-b/r6_size_shared_b.json (wild_recipe 0.8227, 0.5805). appendices_bcd.tex:385 and supplement_extended_body.tex:649 still print the pooled 12,000-replicate 0.821 and 0.580, so the coordinator should confirm the two figures read as consistent.
- R2-36 FIXED: "The full-battery accuracy interval includes one at accuracy-like item noise, a design that would detect a true ratio of 1.10 in 0.135 of replicates" and "In a population with the released item counts and equicorrelated seed covariance, three runs each let the margin interval exclude one in 0.997 of replicates, and accuracy needs eight to reach 0.803." Source compute extra/heldout/outputs/k08-seed-count/seed_count_power.json (0.99667, 0.80333).
- R2-37 FIXED: "Bank two holds under a fifth of bank one's items, which widens both intervals." For length, I cut "We report this pattern without reading it in either direction." from the same undecided branch, since the new simulation paragraph now covers it.
- R2-38 FIXED: "\subsection{Concentration by scoring format}".
- R2-39 FIXED: "come from earlier execution records that we didn't rerun for the final analysis." The loss-proxy clause is deleted.

Other length cuts: the first rule's rationale lost "nine seeds are the replicate unit and" (the eight degrees of freedom already imply it), and a few words were trimmed ("only 0.490", "transfer runs", "decide what the runs show").
