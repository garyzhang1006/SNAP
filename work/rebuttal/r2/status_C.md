# Slice C status (main.tex 137-204), wc -w orig 1215, new 1195

- R2-19 FIXED. Table 1 caption: "or cluster-robust $t(4)$ intervals over the five size bands where marked. The auxiliary contrast compares the two auxiliary runs and removes the default-versus-auxiliary batch component." The app:proxy clause is gone. Source research/outputs/snap-r6-twoway/r6_twoway.json (interval_size df 4, margin 1.030 to 1.426, accuracy 0.961 to 1.184, matching the table rows).
- R2-20 FIXED. Deleted "A sweep of 2,000 weightings from a flat Dirichlet leaves 0.817 of margin intervals excluding one." The appendix copy is untouched.
- R2-21 FIXED. "Across 2,000 such permutations the permuted estimates sit about 0.001 below the observed margin value and 0.002 below accuracy, so item-level sharing accounts for little of either, although an effect carried by every item of a benchmark survives this permutation untouched." The tail fractions 0.087 and 0.352 are dropped, and 0.001 and 0.002 were already printed in the paper.
- R2-32 FIXED. Row: "Original, four-benchmark subsets, all sizes (percentiles) & Margin & 1.176 & 0.982--1.476". Caption: "...across all five sizes with subset 5th to 95th percentiles, not an interval, from the committed battery-size curve." The caption comes out 18 words against the old 18.
- R2-33 FIXED. "and a power simulation of the rule at each scope it could take".
- R2-34 FIXED. "(Figure~\ref{fig:covariance} in Appendix~\ref{app:supplementary})".
