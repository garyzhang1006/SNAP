# Rebuttal prep for the cross-benchmark covariance submission

Every number below was grepped out of `/Users/garyzhang/Documents/ChatGPT/iclr main track/deliverables/main.tex` or `appendices_bcd.tex` before use, and rows run from the objection most likely to move a score downward to the least.

| # | Objection as a reviewer would write it | Where the paper already answers it | Draft response | Status |
|---|---|---|---|---|
| 1 | The one confirmatory test in the paper fails its own preregistered rule at its full registered scope, so the central claim is unsupported. | main.tex 21 (abstract) reports that the test "fails its lower-limit rule at 1.217", and main.tex 173 states that the width-matched simulation "can't rescue the test". main.tex 151 gives "held-out margin inflation is 1.217 with interval \ci{0.872}{1.498}", "The original ten benchmarks on the same runs give 1.240 with interval \ci{1.054}{1.397} and pass it", "the held-out interval is 0.626 wide against 0.343 for the original, and the rule reads the lower limit", and main.tex 173 gives "In simulation at this scope with four traits, the rule passes in 0.926 of replicates at a true 1.244 under margin-like item noise and in 0.524 under accuracy-like noise", and "That later calculation can't rescue the test". Table 2 (tab:heldout, label at main.tex 156) carries the held-out margin row at main.tex 162 and the original ten row at main.tex 165, and its caption at main.tex 155 states that the subset row gives "the median over 60 four-benchmark subsets of the original battery across all five sizes ... from the battery-size curve the protocol committed to". main.tex 147 carries the fixed-battery position, "Our design treats this battery as fixed, and that spread isn't a distribution over benchmark collections". appendices_bcd.tex 385 gives the three earlier results and the subset counts, "32 of their 60 fall below the full-scope 1.217", "A three-size curve run after that result, and so post hoc", and "0.55 of them excluding one against 0.57 there". appendices_bcd.tex 391 gives "At three sizes the median widths at a true 1.244 are 0.258, 0.306, and 0.431 at the three fixed noise levels and 0.640, 0.839, and 1.009 at the added ones, against the observed 0.626" and "at 2.00 the pass rate is 0.262 with coverage 0.918", and states that the three noise levels were fixed "before the two-size analysis ran". The 0.820 pass rate at the intermediate noise level appears only in tab:rulepower, supplement_extended_body.tex 719. | We agree that the registered rule fails at its full scope, and the abstract says so in the same sentence that reports the interval. The held-out point estimate of 1.217 differs by 0.024 from the 1.240 that the original ten benchmarks give on the same 225 runs, so the two batteries land in the same place on the point estimate while only the original interval clears the rule, because the held-out interval runs 0.626 wide against 0.343 and the rule reads the lower limit. The simulation at the three noise levels we fixed before the two-size analysis ran gives three-size median widths of 0.258, 0.306 and 0.431 at a true 1.244, all narrower than the observed 0.626, and passes that true value in 0.926 of replicates under margin-like item noise, in 0.820 at the intermediate level and in 0.524 under accuracy-like noise, so the observed width is matched only at the 2.00 noise level we added after seeing the two-size width, where the median simulated width is 0.640 and the pass rate is 0.262, and that later calculation can't rescue the test. Among four-benchmark subsets of the original battery, 32 of 60 fall below 1.217 on the curve the protocol committed to across all five sizes, where 0.57 of the subsets exclude one, and a three-size curve run after the result, and so post hoc, gives the same count of 32 with 0.55 excluding one. We report those subset counts as composition diagnostics on a battery our design treats as fixed rather than as a distribution over benchmark collections, and we do not read the failed test as evidence of independence, because the observed interval contains both one and 1.244 and so cannot separate transfer of the original covariance from its absence. | partly answered |
| 1b | The original battery passes the rule on the same runs while the held-out battery fails, which shows the covariance does not transfer to unseen tasks. | main.tex 173 records that the later width-matched calculation "can't rescue the test, and the accuracy interval on the same runs is 0.293 wide", main.tex 151 gives held-out margin inflation of 1.217 with interval 0.872 to 1.498 against 1.240 with interval 1.054 to 1.397 for the original ten on the same runs and the widths 0.626 against 0.343, Table 2 (tab:heldout, main.tex 153 to 170) carries both rows at 162 and 165, main.tex 175 gives the leave-one-task-out range with tab:heldoutloto at supplement_extended_body.tex 736 to 752, appendices_bcd.tex 387 gives the pooled fourteen-task estimate of 1.419 with interval 1.051 to 1.714, and appendices_bcd.tex 389 states that "Item count therefore doesn't explain the four-task failure rate". | The two point estimates differ by 0.024 and the original interval of 1.054 to 1.397 sits entirely inside the held-out interval of 0.872 to 1.498, so these data do not separate the two batteries, which is the reading the abstract already gives when it says the test could not separate transfer of the original covariance from its absence. What differs is width, 0.626 against 0.343, and the rule reads the lower limit, while the appendix reports that item count doesn't explain the four-task failure rate. Two exploratory checks point the same way without being the registered test. Pooling the four held-out tasks with the original ten gives margin inflation of 1.419 with an interval of 1.051 to 1.714 that excludes one, and the held-out point estimates with one task removed at a time run from 1.090 to 1.213 and stay above one, although three of those four intervals include one and only the removal of DROP leaves an interval, 1.035 to 1.377, that excludes it, and the paper reads that spread as dependence on battery composition. We label both checks exploratory, as we do every analysis apart from the registered test. | answered |
| 1c | The last 20 held-out runs were scored on different hardware and a different torch version, so the three-size result mixes two scoring setups. | appendices_bcd.tex 383 records the pre-scoring check, "Before each run was scored, a spread of its items was checked against a one-sequence reference path to within 0.001 nats, with a slower agreeing path as the fallback." appendices_bcd.tex 385 discloses the split setup, "the last 20 at 1B on three RTX 3090 cards with torch 2.14.0 in place of the T4 image's 2.10.0 after the weekly quota ran out, with the same scorer, float32 weights, 6,000-token cap, and pinned transformers 4.57.1, and every one of those 20 runs passed the 0.001-nat batch check below 0.0001." appendices_bcd.tex 505 gives the wall time, "the last 20 at 1B on three RTX 3090 cards in 6,068 seconds, after the weekly quota ran out." appendices_bcd.tex 519 gives the hardware-only comparison, "A separate Turing-versus-Ada comparison on one configuration reports mostly identical item scores, a mean absolute margin difference of $1.0\times10^{-3}$, and a largest difference of $3.5\times10^{-2}$," with "the mean difference has ratio $2.6\times10^{-3}$" against a margin standard deviation of 0.391. appendices_bcd.tex 511 gives the one setup effect measured on an inflation estimate, "The transport rescore changed hardware and token budget together and shifted inflation by 0.6 percent," whose two values are at appendices_bcd.tex 329, "its three-size margin value of 1.41372 differs from the pipeline's 1.40574 by 0.6 percent." main.tex 173 carries the main-text disclosure, "the last 20 at 1B on three RTX 3090 cards after the weekly GPU quota ran out, with the same scorer and settings apart from torch 2.14.0 in place of 2.10.0, and the batch check passed by more than a factor of ten", and main.tex 151 the interval, "held-out margin inflation is 1.217 with interval \ci{0.872}{1.498}". | The split is disclosed rather than buried, since the appendix states that 205 of the 225 runs were scored on Kaggle T4 cards and the last 20 at 1B on three RTX 3090 cards with torch 2.14.0 in place of the T4 image's 2.10.0, while the scorer, float32 weights, 6,000-token cap and pinned transformers 4.57.1 were held the same across both setups. The request file did change twice after the freeze, and the paper records that neither change touched a prompt, continuation, label or item order, so both setups scored the same prompts, continuations and labels in the same order. Before each run was scored, a spread of its items was checked against a one-sequence reference path to within 0.001 nats, and every one of the 20 RTX 3090 runs cleared that batch check below 0.0001, which the main text reports as passing by more than a factor of ten. We have not rescored any of those 20 runs on a T4 card, so the paper does not measure a setup effect on the held-out estimate directly, and we say so rather than claim the two setups were proved equivalent. Two indirect measurements are reported, and neither covers the held-out runs. The card-architecture comparison in the appendix changes hardware alone on one configuration and gives a mean absolute margin difference of $1.0\times10^{-3}$, a ratio of $2.6\times10^{-3}$ against a margin standard deviation of 0.391, though its two cards are Turing and Ada rather than the Turing and Ampere cards of the split. The transport rescore changes hardware and token budget together and moves the margin estimate by 0.6 percent, which is 1.41372 against 1.40574. Neither number bounds the effect on the 20 held-out runs, so we offer them only for scale, and a shift of the larger of the two applied to the held-out margin interval, whose lower limit is 0.872 against a point estimate of 1.217, would leave that limit near 0.877 and so far below the one the registered rule reads. Closing the objection would take a T4 rescore of those 20 runs, which we have not run. | partly answered |
| 2 | The whole margin result is an artifact of one benchmark, since dropping BoolQ moves inflation from 1.244 to 1.786. | main.tex 145 reports the removal estimates and states "BoolQ contributes 68\% of the margin covariance trace and 84\% of the accuracy trace" together with "Lowering BoolQ's weight from a tenth to 0.0867 already stops the accuracy interval including one"; main.tex 147 states "Our design treats this battery as fixed, and that spread isn't a distribution over benchmark collections" and "A sweep of 2,000 weightings from a flat Dirichlet leaves 0.817 of margin intervals excluding one"; appendices_bcd.tex 183 gives the weighting sweep and its knife edge, "Its interval stops including one at a BoolQ weight of 0.0867, a reduction of about one seventh"; appendices_bcd.tex 194 adds "The weighting we shipped is therefore the conservative one on accuracy"; appendices_bcd.tex 196 gives the subset comparison, "At fixed benchmark count, subsets that contain BoolQ have mean margin inflation lower by 0.115 at two benchmarks and by 0.596 at nine"; the full trait-removal table is tab:original10 in supplement_extended_body.tex 337 to 347. | BoolQ pulls both conclusions toward independence rather than away from it, because the off-diagonal terms involving it are negative and sum to about -0.15 of the margin trace and -0.06 of the accuracy trace, so its removal lowers the denominator by more than the numerator and raises margin inflation to 1.786 and accuracy inflation to 1.558. The same direction shows up across subsets, since equal-weight subsets that contain BoolQ average 0.115 lower margin inflation at two benchmarks and 0.596 lower at nine than subsets without it. Benchmark removal nonetheless moves the estimates more than recipe or size removal does, because the other single-trait removals mostly lower the margin estimate, from 1.246 for WinoGrande down to 1.096 for CommonsenseQA against the full-battery 1.244. On weights rather than removals, 0.817 of margin intervals from 2,000 flat-Dirichlet weightings still exclude one, while the accuracy interval stops including one once BoolQ's weight falls from a tenth to 0.0867, which the appendix calls a knife edge and which makes the shipped flat weighting the conservative one on accuracy. We report the fixed battery as the estimand and treat this spread as composition sensitivity rather than as a distribution over benchmark collections. | partly answered |
| 3 | The correction changes the rate of wrong recipe calls by at most 0.009, so the paper measures something that does not matter. | main.tex 210 states that the correction "changes the share of wrong calls by at most 0.009, from rates between 0.029 and 0.070" and that "so these rates aren't false-positive rates in the usual sense", main.tex 25 gives the simulation where a comparison assuming independence "declares a difference in 0.113 of replicates with a true gap of zero, against a nominal rate of 0.05", main.tex 226 states that a scaled independence standard error "returns the simulated false-positive rate from 0.113 to 0.051", with the same simulation at supplement_extended_body.tex 405, and appendices_bcd.tex 414 and the rows of tab:decision at appendices_bcd.tex 427 to 434 give the per-size call counts, wrong-call rates and their recipe-bootstrap difference intervals. | We report the recipe-call result as the operational answer and we claim no decision gain from it, since every recipe-bootstrap interval for the change in the wrong-call rate reaches zero across the eight size and scale cells of tab:decision. The covariance matters instead for any procedure that carries a nominal level, because in simulation with margin-like covariance a comparison that assumes independence declares a difference in 0.113 of replicates when the true gap is zero against a nominal 0.05, and a scaled independence standard error whose ratio is estimated on the same population returns that rate to 0.051. The recipe-call exercise scores each call against the ordering at 1B, which itself moves between sizes and so also counts genuine rank changes, which is why the paper says these rates aren't false-positive rates in the usual sense. The two settings answer different questions, and we do not claim that the simulated gain transfers to a battery whose own ratio differs, since the discussion reports that carrying the same scaling to a battery with a ratio of 1.5 leaves the error at 0.101. | partly answered |
| 4 | Cross-half products cannot separate run covariance from item noise that is shared across benchmarks and repeated across halves. | main.tex 63 concedes that "a 0.124 share of item variance would reproduce 1.244", and appendices_bcd.tex 247 adds that accuracy reaches 1.078 at a share near 0.009 and that the available diagnostic "has no power". | We state this limitation in the estimator section and repeat it in the limitations paragraph rather than burying it, and we bound what we can. A share of 0.05 reproduces the observed margin value in only 0.007 of replicates, and a permutation that breaks the tie between particular items and particular runs leaves 0.087 of draws at or above 1.244, so the item-level channel accounts for about 0.001 of the estimate. The scalar component shared across all benchmarks remains unmeasured, and we present the margin estimate as identified only up to it. | open |
| 5 | The covariance sits entirely inside scoring formats, so this measures prompt format and not run-to-run structure. | main.tex 185 reports mean correlations of "0.624 within scoring formats and 0.007 across formats" and, retaining only cross-format off-diagonal covariance, "a diagnostic inflation of 0.921 with interval \ci{0.801}{1.035} against 1.244 for the full matrix", and appendices_bcd.tex 130 adds that "Excluding BoolQ gives a cross-format margin diagnostic of 1.006"; main.tex 233 repeats in the discussion that "On margins, the covariance sits almost entirely within scoring formats."; appendices_bcd.tex 122 and 124 give the masked calculation, 0.921 with interval 0.801 to 1.035 for margins, and the BoolQ exclusion at 1.006 with interval 0.972 to 1.039. | The reviewer is reading our own diagnostic correctly, and the discussion draws the same conclusion, since retaining only cross-format off-diagonal covariance leaves a margin diagnostic of 0.921 with interval 0.801 to 1.035 against 1.244 for the full matrix. Each benchmark appears in exactly one scoring format in this release, so format and task content cannot be separated causally, and we decline to attribute the concentration to either. We also report that excluding BoolQ lifts the cross-format margin diagnostic only to 1.006, compared with 1.786 without the restriction, which keeps the format reading alive rather than refuting it. | partly answered |
| 6 | At 750M the compared checkpoint sits near 41 percent of the default run's final step, so the estimate reflects schedule mismatch. | main.tex 102 states "at an average of 41.5\% of their final step, with a range of 18\% to 44\%" and gives the truncation subset, "margin inflation is 1.276 with interval \ci{1.114}{1.420}"; main.tex 135 carries the auxiliary contrast row, "Margin & Auxiliary contrast & 1.256 & \ci{1.090}{1.400}"; appendices_bcd.tex 8 gives the index, "At 750M it averages 41.5 percent, with values from 17.7 to 44.0 percent", and appendices_bcd.tex 10 gives the influence correlation, "Spearman correlation $-0.036$ for margins and $0.001$ for accuracy with the selected step's share of the default final step. Permuting that share within each size over 4,999 draws gives $p=0.314$ and $p=0.990$". | Dropping the 26 severely truncated configurations, which removes the 750M band, raises margin inflation to 1.276 with interval 1.114 to 1.420 and accuracy to 1.120 with interval 1.020 to 1.208, and we chose these subsets after seeing the full-sample estimates, so they read as sensitivity rather than as a test. A configuration's influence has Spearman correlation -0.036 for margins and 0.001 for accuracy with the selected step's share of the default final step, at permutation p values of 0.314 and 0.990 over 4,999 draws. Neither check can see an offset shared by every configuration, which is the limitation the main text states where it compares the two scored checkpoints, and only the auxiliary contrast, at 1.256 with interval 1.090 to 1.400, bounds that channel. | partly answered |
| 7 | The analysis plan has no independent timestamp and its screening split was never drawn, so nothing here is confirmatory. | main.tex 104 records the abandoned split, "The analysis plan assigned eight recipes to screening and seventeen to estimation, but we used all recipes without drawing that partition", together with the enumeration over the $\binom{25}{17}=1{,}081{,}575$ possible estimation sets; main.tex 236 repeats that "The original analysis plan lacks an independently corroborated timestamp. Its proposed screening split was never implemented either."; appendices_bcd.tex 60 says "Its claimed date of September 5, 2026 lacks an independent timestamp" and treats the document "as a reported plan and its checks as exploratory assessments"; and appendices_bcd.tex 68 lists each planning criterion with its outcome "without treating the document as verified preregistration". | We label every analysis apart from the registered held-out test as exploratory, both in the abstract and in the contributions paragraph of the introduction, and the appendix describes the planning document as a reported plan whose checks are exploratory assessments rather than verified preregistration. An exhaustive enumeration over all 1,081,575 seventeen-recipe estimation sets gives margin estimates from 1.037 to 1.317 and accuracy estimates from 0.963 to 1.191, none of which reaches the planned thresholds of 1.349 and 1.40. That sweep cannot restore a holdout or undo choices made after viewing the data, and we say so in the same paragraph. | open |
| 8 | Clustering on 25 recipes is wrong because the release shares one seed label across every recipe inside a size band. | main.tex 224 concedes the objection in the paper's own words, that "the release shares one seed label across every recipe inside a size band", and reports that a band-following run effect takes simulated coverage "to 0.821 at a quarter of latent variance and 0.580 at a half", that the batch-slot permutation "bounds that effect at 0.044 of the seed covariance on margins, where simulated coverage is 0.941 at a share of 0.05", and that "its power is only 0.490 at the bound". appendices_bcd.tex 369 gives the size-band fallback, where "the margin interval runs from 1.030 to 1.426 and still excludes one, while the matching accuracy interval runs from 0.961 to 1.184 and still includes one", together with the qualification that the two-way clustered variance turns negative on full-battery margins and that five size clusters are too few for it. appendices_bcd.tex 373 records that on accuracy "the size-clustered wild interval covers only 0.832 even without sharing", and appendices_bcd.tex 379 gives the permutation null itself, "anti-conservative where it is meant to exclude", with the share at "0.008 on full-battery margins with a 95 percent upper bound of 0.044" and recipe-clustered coverage of 0.946, 0.941 and 0.929 at shares of 0.02, 0.05 and 0.10. | We concede the sharing in the limitations paragraph itself rather than in a footnote, and we show what it would cost, since a run effect following the band takes simulated coverage to 0.821 at a quarter of latent variance and 0.580 at a half. Clustering on the five size bands instead leaves both conclusions standing, with the margin interval at 1.030 to 1.426 and the accuracy interval at 0.961 to 1.184, and the appendix records in the same line what that fallback costs, since the two-way clustered variance turns negative on full-battery margins with five size clusters too few for it, and on accuracy the size-clustered wild interval covers only 0.832 even without sharing, so that fallback needs its $t$ version. A batch-slot permutation measures the band-shared component directly and puts it at 0.008 on full-battery margins with a 95 percent upper bound of 0.044, and a sweep at shares of 0.02, 0.05 and 0.10 leaves recipe-clustered coverage at 0.946, 0.941 and 0.929, so coverage stays near nominal up to twice that bound. The main text states that the test's power reaches only 0.490 at its own bound, and the appendix adds that the null is anti-conservative where it is meant to exclude, so we treat the bound as weak evidence rather than as a clean exclusion, and the objection stands to the extent that these data cannot rule the channel out. | partly answered |
| 9 | Inside a single model size the margin interval includes one, so the pooled result may be an artifact of pooling. | appendices_bcd.tex 88 gives the band structure, "Each size band contains 25 configurations, one per recipe, so its clusters are singletons without within-band replication". appendices_bcd.tex 101 gives the 1B margin row of Table~\ref{tab:original7}, "1B & 1.149 & \ci{\mathrm{n/a}}{1.751} & 1.205 & \ci{0.805}{1.497}\\", under a caption at appendices_bcd.tex 92 stating "The intervals use the wild cluster bootstrap-t calculation described in Appendix~\ref{app:implementation}." appendices_bcd.tex 106 gives "The cluster-robust $t(24)$ interval on the log of the squared ratio gives 0.811 to 1.628." with "none of them excludes one" for all three positive constructions. main.tex 224 carries the main-text statement, "the cluster-$t$ 1B margin interval of 0.811 to 1.628 includes one". appendices_bcd.tex 112 gives the within-band power, "at a true margin ratio of 1.2446 a single band excludes one in 0.313 of replicates at three runs, 0.739 at six and 0.957 at ten", and on the same line "Simulation says these per-size rows should be read as estimates with wide uncertainty rather than as intervals carrying their nominal level." appendices_bcd.tex 108 gives the BoolQ-removal bands, where "Every band then excludes one" on margins. appendices_bcd.tex 365 gives the pooled figure, "At 125 configurations and three runs the margin effect clears one in 0.970 of replicates". | A single size band holds 25 configurations, one per recipe, so its clusters are singletons without within-band replication, and power inside one band is low at the run count the release ships, since at a true margin ratio of 1.2446 a single band excludes one in 0.313 of replicates at three runs, 0.739 at six and 0.957 at ten. The 1B margin row therefore behaves as the pooled estimate predicts it should, and the same appendix line tells the reader to take the per-size rows as estimates with wide uncertainty rather than as intervals carrying their nominal level. The shipped wild interval for that cell has no finite lower endpoint, and the three positive constructions we report, 0.811 to 1.628, 0.834 to 1.643 and 0.491 to 1.656, all include one. Our conclusions come from the pooled design over 125 configurations, where the margin effect clears one in 0.970 of replicates at three runs. Removing BoolQ does make every band exclude one on margins, with estimates of 1.548, 1.864, 1.874, 1.888 and 1.649 from 150M to 1B, but we read that in the paper as the composition sensitivity reappearing at size resolution rather than as independent support for the pooled estimate, so we do not offer it as evidence against a pooling artifact. What we can say is that at three runs a single band excludes one in fewer than a third of replicates even when the pooled effect holds, so the 1B row's failure to exclude one weighs little against the pooled result, not that pooling has been independently confirmed inside a band. | partly answered |
| 10 | Wild cluster bootstrap intervals over 25 clusters cannot be trusted at their nominal level. | main.tex 97 reports wild bootstrap coverage "between 0.928 and 0.954 across the twelve populations", and the discussion at main.tex 237 states that a band-following run effect would "take simulated margin coverage to 0.821 at a quarter of latent variance and 0.580 at a half", and main.tex 112 gives the later centred test-inversion interval, which "covers between 0.935 and 0.956 in a 10,000-replicate repeat of the same populations and gives \ci{1.095}{1.323} for margins" and "moves the margin lower endpoint down by 0.048". appendices_bcd.tex 235 reports that "A repeat of all twelve populations at 10,000 replicates puts the centred inversion between 0.935 and 0.956 and the wild interval between 0.933 and 0.953". appendices_bcd.tex 237 states "We repeated every cell at 10,000 replicates with new seeds. No cell moves by more than 0.011 under that repeat." appendices_bcd.tex 239 reports that "A band-shared quarter breaks all three levels together, at 0.743, 0.828 and 0.928." appendices_bcd.tex 241 gives the residual-scaled alternative, which "covers between 0.943 and 0.958 at 4,000 replicates". appendices_bcd.tex 253 states "The wild lower endpoint shouldn't be read as a firm floor." | We measured the shortfall rather than assuming it away, and across twelve simulated populations at 2,000 replicates the shipped wild interval covers between 0.928 and 0.954, which is at most 0.022 below nominal, while a repeat of every cell at 10,000 replicates with new seeds moves no cell by more than 0.011 and puts the interval between 0.933 and 0.953. That bound holds inside the twelve populations of the coverage table and not outside them, because the band-shared populations fall lower and a band-shared quarter breaks the 0.90, 0.95 and 0.99 levels together at 0.743, 0.828 and 0.928. A centred cluster test-inversion interval that we added later covers between 0.932 and 0.957 on the same simulated moments at 2,000 replicates, between 0.935 and 0.956 in the 10,000-replicate repeat that the main text quotes, and on the observed data it gives 1.095 to 1.323 for margins, which moves the margin lower endpoint down by 0.048 and still excludes one. We also report a residual-scaled wild construction that covers between 0.943 and 0.958 at 4,000 replicates, and because we chose it after seeing four of the twelve cells we report the shipped interval throughout and record the better-calibrated one beside it. The appendix states that the wild lower endpoint shouldn't be read as a firm floor, and we make no claim that the shipped interval reaches its nominal level. | partly answered |
| 11 | Swapping the two AGIEval tasks out of a registered battery after a pilot looks like a convenient post hoc change. | main.tex 151 says the protocol's rule replaced any task whose 1B pilot accuracy fell below chance plus 0.02, "a bar that both AGIEval tasks missed by about a thousandth, inside the pilot's sampling error of about 0.010", and appendices_bcd.tex 383 gives the pilot reading, "LogiQA-en gave mean accuracy 0.269 against a bar of 0.270 and LSAT-LR gave 0.219 against 0.220, with chance at 0.25 and 0.20", together with "The rule rather than the evidence therefore decided the swap, and we applied it as written" (the same paragraph appears at supplement_extended_body.tex 695). | The protocol fixed the task list, the replacement rule, the statistic, the interval, the pass rule and a scope rule before any production score existed, and it records every later change as a dated amendment, so the bar of chance plus 0.02 and the rule that acts on it were both part of that fixed protocol. When the backup rule read the pilot's three 1B runs, LogiQA-en came in at mean accuracy 0.269 against a bar of 0.270 and LSAT-LR at 0.219 against 0.220, with chance at 0.25 and 0.20, and because both fell short the rule replaced them in its listed order with DROP and CoQA as multiple choice. We say plainly in the appendix that each miss is smaller than the pilot's binomial standard error of about 0.010 over three runs and 651 or 510 items, which is why we write that the rule rather than the evidence decided the swap and that we applied it as written. | answered |
| 12 | The recommendation to scale by an estimated ratio will not transfer to any battery other than this one. | main.tex 226 says "carried to a battery whose own ratio is 1.5, it leaves the error at 0.101", appendices_bcd.tex 212 gives the simulated transfer sweep, with "The transferred rule holds between 0.04 and 0.06 only within about 0.05 of the source ratio" and the matched rule holding between 0.048 and 0.053, and supplement_extended_body.tex 411 states "The correction is therefore not a constant that a reader can borrow from this paper". | We agree, and the paper already says so, since the transferred rule holds between 0.04 and 0.06 only within about 0.05 of the source ratio, and a correction estimated on the shipped margin design leaves the simulated false-positive rate at 0.101 once it is carried to a battery whose own ratio is 1.5. What we recommend for practice is paired replicate scores across the chosen battery, which give the aggregate seed standard deviation directly, together with a benchmark-removal sensitivity check. In the same simulated sweep a matched rule re-estimated on each target holds between 0.048 and 0.053, so the correction is not a constant a reader can borrow from this paper, and readers should measure the ratio on their own battery. | answered |

## What would close the items that are not answered

Row 1 would move only with more held-out tasks scored on the same 225 runs, since the fail is a width result, and that needs compute. Row 1c would close with a T4 rescore of the 20 runs scored on the RTX 3090 cards, which needs compute. Row 2 would close with a second replicate population whose battery does not contain a single benchmark carrying 84 percent of the accuracy trace, which means scoring new runs and therefore needs compute. Row 3 would close with a decision setting that carries a nominal level, or failing that with an abstract that leads on the nominal-level result rather than on the recipe calls, and the reframe needs no compute. Row 4 would close only with an independent item pool per benchmark scored on the same checkpoints, since the appendix already shows the regression diagnostic has no power, and that needs compute. Row 5 would close with at least one benchmark scored in two formats on the same runs, which needs compute. Row 6 would close with a 750M rescore at steps matched across all three runs near their released final steps, which needs compute. Row 7 would close only with a third-party timestamp on the plan file, and a refuter in Scan 98 rejected dropping the thresholds from main.tex 104, since the shortfall against them costs the authors and its removal while Appendix B and the supplement still carry them would read as suppression of an adverse comparison, so the sentence now calls them the plan's thresholds and the row stands as it is. Row 9 would close only with more runs per size band, since a single band at three runs excludes one in 0.313 of replicates even when the pooled effect holds, and that is outside our control. Row 10 would close with an interval construction fixed before any coverage cell was seen, which needs no compute but cannot be done retroactively. Row 8 would close with a release whose seed labels are crossed against recipes rather than shared inside a size band, which is outside our control and not a compute question.
