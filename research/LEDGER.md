# SNAP task ledger

States follow the plan (NOT STARTED / RUNNING / VERIFIED / BLOCKED / DROPPED WITH REASON) and the handoff (PLANNED / IMPLEMENTED / PILOT VERIFIED / SUBMITTED / RUNNING / FINISHED UNCHECKED / VERIFIED / BLOCKED). Evidence labels are OBSERVED (read or run this session), REPORTED (stated in a document or an earlier result table), PROPOSED, INFERRED. Code written and job submitted are never VERIFIED.

Constraints in force: one working process, no subagents, no local compute (Kaggle only, private kernels, 5 concurrent, 2 GPUs), no tokens in any file, no invented names or numbers, manuscript at most 9 main-text pages with the byte-identical ICLR 2027 style.

Baseline copies and SHA256 of the manuscript source and PDF: `research/baseline_2026-09-15/SHA256SUMS.txt` (OBSERVED, taken 2026-09-15 before any edit).

| Task | State | Evidence | Notes |
|---|---|---|---|
| R0 artifact recovery | VERIFIED | OBSERVED | Original pipeline is the `seednoise` package at `/Users/garyzhang/claude/seed-noise` (git 5a59526, clean). Kernel `snap-r0-baseline` reran `analyze --n-boot 4999 --skip-nulls` on the 375 reduced runs; `tab_primary`, `tab_reliability`, `tab_bakeoff`, `tab_mediation`, `tab_cheverud`, `tab_practitioner` and both `matrices_*.json` are byte-identical to `results/datadecide/` (`tab_gates` differs only by the absent null-gate columns). Margin 1.24395 (wild 1.1429 to 1.33768), accuracy 1.07837 (wild 0.992604 to 1.15665). Timing: analyze 6.2 s, kernel 120 s. Outputs in `research/outputs/snap-r0-baseline/`. |
| R0 snap_compute reconciliation | VERIFIED | OBSERVED | `snap_adapter.py` converts the npz runs to `snap-scores-v1` with the original split. C01 on the original split gives margin 1.243950 and accuracy 1.078373, equal to the original to six decimals. C02 ran 41 tests OK. snap_compute recipe-percentile 95 percent intervals: margin 1.13441 to 1.32207, accuracy 1.00288 to 1.15495 (the accuracy interval excludes 1, unlike the wild interval). Gate A passed. |
| R1 estimand specification | VERIFIED | OBSERVED | `research/R1_estimand.md`, written 2026-09-15 before any grouped estimate was opened; comparison set and weighting frozen there. |
| R5 mathematical tests | VERIFIED | OBSERVED | C02 41 tests OK in `snap-r0-adapter`; `seednoise selftest` OK in `snap-r0-baseline`. Numerical identity of `moments`/`summarize` with `estimator._T_from/_U_from` checked on the same run. |
| R2 BoolQ passage repair | VERIFIED | OBSERVED | Detectability simulation complete 11:15 EDT (2,355 s): paired split differences within Monte Carlo error at all five interaction scales; leak bounded at 0.008 percent of the BoolQ diagonal by the observed histogram.  Passages recovered from the release's own `requests/boolq-requests.jsonl.gz`: 3,270 of 3,270 passages match google/boolq validation exactly at the same native_id; 2,938 unique passages; group sizes {1: 2692, 2: 197, 3: 27, 4: 13, 5: 7, 7: 1, 9: 1}; 578 items sit in groups of two or more. The partition equals the index-join partition from `snap-r2-boolq-map` (label convention there is inverted, 3,270 of 3,270 inverse agreement). Grouped estimates run in kernel `snap-r2-grouped` (C01 original, item, group, no-BoolQ; C03 50 splits; C09; original-pipeline intervals via `snap_intervals.py`). Passage-effect simulation with the observed histogram runs in `snap-r2-group-sim`. |
| R3 broader dependence audit | VERIFIED | OBSERVED | Doc-level stems from the requests files (v2 audit, `research/outputs/snap-r3-requests-audit/requests_audit/requests_audit_report.json`). Cross-trait shared stems: 2 (arc_challenge and arc_easy 1, arc_easy and mmlu 1). Within-trait repeated stems (docs in repeated stems): piqa 62 (max repeat 4), socialiqa 48, hellaswag 45 (max 3), mmlu college_physics 36 of 102, mmlu high_school_psychology 22, arc_easy 10, mmlu nutrition 10 (max 6), arc_challenge 4, openbookqa 4; winogrande and csqa 0. Repeated MMLU stems are generic question texts with different choices, so they are distinct items sharing a stem string rather than shared passages. No grouping applied outside BoolQ; inventory reported. |
| R4 matched-run reconstruction | VERIFIED | OBSERVED + REPORTED | `research/R4_run_manifest.csv` (375 rows). OBSERVED from npz metadata: seed 2 carries batch flag 0 at every size; auxiliary seeds are 14 and 15 below 1B and 4 and 5 at 1B; the selected step is identical across the three runs in all 125 configurations (150M 37500 or 38157, 300M 45000 or 45787, 530M 51250 except 22500 in one recipe, 750M 27500 except 11250, 16250, 26250 in one recipe each, 1B 69369 except 57500 in one recipe). REPORTED from the draft: seed 2 is the default run and auxiliary runs below 1B stop at 25 percent of the 1B budget. The schedule label in every training signature carries that basis. |
| R6 inference calibration | VERIFIED | OBSERVED | `r6_calibration.py` cells in kernels `snap-r6-calibration-a` (null margin and accuracy noise, margin-like rho 0.061, accuracy-like rho 0.018, BoolQ-share variants) and `-b` (recipe-shared 0.5, Student df 4, one dominant recipe scale 6, cross-half error correlation 0.1, rho 0.2), 2000 reps, 4999 draws, seed 20260915. Four procedures scored: wild bootstrap-t, cluster t, config percentile, snap recipe percentile. |
| R7 proxy repair | RUNNING | OBSERVED | CPU audit snap-r7-proxy-audit VERIFIED 2026-09-15 16:58 EDT: reproduces shipped after-competence 1.24399/1.07595; the 1e-12 clip binds for WinoGrande (margin) and PIQA plus WinoGrande (accuracy), giving WinoGrande 100.00% of the margin proxy's standardised input; dropping WinoGrande from the margin proxy gives 0.890 [0.805, 0.968]; recipe 5-fold cross-fitted proxy with SD shrinkage alpha 0.5 gives 0.731 [0.673, 0.784] margin and 1.004 [0.925, 1.075] accuracy. Calibration VERIFIED 17:37 EDT (snap-r7-proxy-null, snap-r7-proxy-null2, 300 reps per cell): under independence every adjustment returns 0.999-1.002; with one common correlation 0.178 (Lambda 1.242) repaired margin adjustments return 0.875-0.948; with the observed seed covariance they return 0.745 (cross-fit) and 0.889 (drop floored), matching observed 0.731 and 0.890. Repaired proxies remove shared covariance by construction, so the explanatory role is removed (plan Gate D) and the result is written to main.tex and Appendix C app:proxy with Table tab:proxy. GPU pilot snap-r7-pilot v3 RUNNING (v1 hf_olmo unsupported, fixed by mapping onto LlamaForCausalLM; v2 choice-grouping IndexError, fixed). Full G01 held-out-loss campaign waits on the pilot's seed-to-branch mapping. |
| R8 prediction comparison | VERIFIED | OBSERVED | `snap-r8-prediction` v2 C07, 5 recipe folds, 2,000 paired draws, both phenotypes. No covariance model beats independence on the held-out variance moment; every paired interval includes zero. Distinct target from the draft's log-SD Table 2, which keeps its REPORTED ranking without paired intervals. See R8_R9_results.md. |
| R9 decisions | VERIFIED | OBSERVED | 1,500 seed-aligned same-size recipe pairs per phenotype; paired sd over independence sd has median 1.024 (margin) and 1.081 (accuracy), 5th to 95th percentiles 0.39 to 1.40 and 0.36 to 1.62; seed-matched cross-configuration covariance positive in 0.49 of pairs. See R8_R9_results.md. |
| R12 transfer | VERIFIED | OBSERVED | snap-r12-polypythias v2 (63 runs, seednoise scorer, T4x2, 6,000-token cap, 17:12 EDT) and snap-r12-analysis (17:14 EDT) reproduce every REPORTED rescore value: margin 1.41372 / 1.26199, accuracy 2.28589 / 1.71085, config intervals 0.939-1.781 and 0.927-1.537, SEs 0.190 / 0.150; adjacent 160M SDs 0.00513 / 0.00451, RMS shifts 0.00456 / 0.00710, ratios 0.890 / 1.573, correlations 0.573 / -0.070, BoolQ 0.0438 / 0.0370. Five-size excess ratio is 1.07393 (supplement said 1.07395, corrected). New: accuracy config interval 0.869-2.428 at five sizes, undefined lower and 2.971 upper at three sizes; centred accuracy adjacent ratio 1.279. The 30,000-token source-pipeline value 1.40574 stays REPORTED. Manuscript updated 17:25 EDT, build exit 0, main text 9 pages, verify clean. |
| R10 scoring-scale interventions | VERIFIED | OBSERVED | snap-r10-scale-transform, design fixed 16:45 EDT: identity reproduces 1.243950 and x10 is invariant; m/s_j 1.182 [1.094, 1.265], winsorised 1.134 [1.048, 1.213], tanh 1.141 [1.053, 1.222], tanh(2m/s) 1.120 [1.032, 1.200], sign 1.079 [0.992, 1.158]. Written to Appendix C app:scale (Table tab:scale) and one main-text paragraph 17:30 EDT. |
| R11 composition | VERIFIED | OBSERVED | snap-r11-composition: 1,013 equal-weight subsets with wild intervals, fixed-K identity effects, item-count weights 1.119 [1.068, 1.170] / 1.043 [0.963, 1.119], inverse-seed-SD weights 1.803 [1.544, 2.023] / 1.707 [1.228, 2.076] with weights held fixed. Written to Appendix C app:composition (Table tab:fixedk) and main-text composition paragraphs 17:30 EDT. |
| R13 literature audit | VERIFIED | OBSERVED | `research/R13_literature_audit.md`: 60 entries verified, 4 metadata defects, 1 overstatement (bouthillier2021), 10 uncited entries, comparison matrix, forward-search log dated 2026-09-15. |
| R14 prospective stage | DROPPED WITH REASON | | A prospective split cannot be recovered after the full population influenced the analysis; a new population needs GPU rescoring (R12). The manuscript states this. |
| R15 to R17 manuscript and package | VERIFIED | OBSERVED | Edits applied 2026-09-15 11:00 EDT to main.tex, appendix_a.tex, appendices_bcd.tex, references.tex: passage-aware BoolQ results replace the leakage claims (abstract, intro, estimator, results, discussion, appendix A), R6 coverage paragraph and Table tab:coverage added, C07 operational comparison and C08 pair ratios added to results, bouthillier sentence reworded, novelty statement per R13, martin1977/kish1965/sha2026 restored, seven uncited entries removed (53 bibitems), bui2025 venue and pages fixed, karamcheti count and zhou matrix size corrected, R7 and R12 disclosed in discussion and reproducibility statement. Build exit 0, 32 pages, main text ends on page 8, zero overfull, verify_final.py passes except table_numeric_tokens_preserved, which is false only because tab:coverage is new (all 16 prior tables unchanged token for token). Snapshot in research/revision_2026-09-15/ with SHA256SUMS. Detectability sentence added to appendix A at 11:20 EDT after snap-r2-group-sim completed. Review and style pass 11:40 EDT: accuracy audit of all 147 main-text decimals, contributions paragraph added, six long sentences split, rhythm edits in four flat paragraphs, honest scores in R16_review.md (overall 5 borderline, stop-slop 40 of 50). Build exit 0, 32 pages, main text page 8, verify_final.py clean. |
| R18 resource measurement | RUNNING | OBSERVED | Every kernel logs Python version, package versions, and wall time. CPU kernels: Python 3.12.13, numpy 2.0.2, scipy 1.16.3. |

## Kaggle job log

| Kernel | Purpose | State | Submitted | Result |
|---|---|---|---|---|
| snap-r0-baseline | seednoise selftest and `analyze --skip-nulls` | VERIFIED | 2026-09-15 10:21 EDT, v1 | byte-identical tables, see R0 |
| snap-r0-adapter | build snap-scores-v1, run C00, C02, C01 | VERIFIED | 2026-09-15 10:21 EDT, v1 | Lambda equal to six decimals, 41 tests OK |
| snap-r2-boolq-map | native_id table and BoolQ passage groups by index join | VERIFIED | 2026-09-15 10:21 EDT, v1 | 2,938 passages, inverse label agreement 3,270 of 3,270 |
| snap-r3-requests-audit | doc-level provenance from the 66 requests files | VERIFIED | v1 10:25 EDT (superseded, used few-shot context), v2 10:30 EDT | 3,270 exact passage matches, partition equal to index join |
| snap-r6-calibration-a | R6 cells 0 to 5 | VERIFIED | 2026-09-15 10:30 EDT, v1 | 560 s; wild coverage 0.938 to 0.954 |
| snap-r6-calibration-b | R6 cells 6 to 10 | VERIFIED | 2026-09-15 10:30 EDT, v1 | wild coverage 0.928 to 0.947 |
| snap-r2-grouped | R2 comparison set, C03, C09, original-pipeline intervals | VERIFIED | 2026-09-15 10:34 EDT, v1 | 128 s; see research/R2_R3_results.md |
| snap-r2-group-sim | r2_group_sim.py, 500 reps x 5 tau, observed histogram | VERIFIED | 2026-09-15 10:34 EDT, v1 | 2,355 s; see research/R2_R3_results.md |
| snap-r8-prediction | C07 both phenotypes, r9_pairs, C08 | VERIFIED (v1 ERROR at 10:48 EDT, r9_pairs.py missing because the kernel was pushed before dataset v5 attached; v2 pushed 10:50 EDT, complete 10:53 EDT, 116 s) | 2026-09-15 10:50 EDT, v2 | |
| snap-r19-size-intervals | per-size Lambda intervals on the C01 moments, theta-scale wild bootstrap-t beside config percentile, log-scale cluster-t, and log-scale wild bootstrap-t | VERIFIED | 2026-09-15 12:05 EDT push, complete 12:07 EDT, v1, 21 s | full-battery margin 1.243950 and 1B margin 1.14898 reproduce the shipped values; 1B margin finite intervals 0.834 to 1.643 (config percentile), 0.811 to 1.628 (log cluster-t), 0.491 to 1.656 (log wild), all include one; written into appendices_bcd.tex after Table tab:original7 and main.tex limitations; output research/outputs/snap-r19-size-intervals/r19_size_intervals.json |
| snap-r20-step-index-a | index-only stream of five DataDecide recipe tarballs | DROPPED WITH REASON | v1 ERROR at 13:24 EDT | c4, dclm-baseline and dclm-baseline-25p-dolma1.7-75p indexed; Falcon-with-cc-top-orig-10p and Falcon-with-cc-top-tulu-10p returned `HTTPError: HTTP Error 404: Not Found` three times each because the release names those tarballs in lower case while the members inside use the capitalised recipe name; superseded by snap-r20-step-index-f |
| snap-r20-step-index-b | index-only stream of five DataDecide recipe tarballs, records every recipe/size/seed/step member | VERIFIED | 2026-09-15 13:08 EDT push, v1, 409 s | 5 of 5 recipes, 2,866 runs listed, byte counts equal Content-Length |
| snap-r20-step-index-c | index-only stream of five DataDecide recipe tarballs, records every recipe/size/seed/step member | VERIFIED | 2026-09-15 13:08 EDT push, v1, complete by 13:17 EDT | 5 of 5 recipes, 3,002 runs listed, byte counts equal Content-Length, no failures |
| snap-r20-step-index-d | index-only stream of five DataDecide recipe tarballs, records every recipe/size/seed/step member | VERIFIED | 2026-09-15 13:08 EDT push, v1, 244 s | 5 of 5 recipes, 2,952 runs listed, byte counts equal Content-Length |
| snap-r20-step-index-e | index-only stream of five DataDecide recipe tarballs, records every recipe/size/seed/step member | VERIFIED | v1 ERROR 13:14 EDT, v2 pushed 13:16 EDT, complete 13:26 EDT | 5 of 5 recipes, 3,006 runs listed, byte counts equal Content-Length after draining the gzip tail |
| snap-r20-step-index-f | set a re-indexed with the release tarball names falcon-with-cc-top-orig-10p and falcon-with-cc-top-tulu-10p | VERIFIED | 2026-09-15 13:27 EDT push, complete by 13:31 EDT | 5 of 5 recipes, 3,010 runs listed, byte counts equal Content-Length; members carry the capitalised names that match the shipped configs |
| snap-r22-prev-step-a | download five recipe tarballs, reduce every configuration at the largest step all seeds share below the selected step | VERIFIED | v1 ERROR 13:27 EDT on capitalized recipe names, v2 COMPLETE 14:18 EDT, 75 runs, all 25 configurations have an earlier shared step, member_recipe recorded, wall 2,713 s, output in research/outputs/snap-r22-prev-step-a | v1 reduced c4, dclm-baseline, dclm-baseline-25p-dolma1.7-75p at 55 s per recipe with gaps of 1,250 steps below 1B and 1,869 at 1B; the two Falcon-with-cc-top recipes failed with `ValueError: unknown recipe` |
| snap-r22-prev-step-b | download five recipe tarballs, reduce every configuration at the largest step all seeds share below the selected step, record both steps and the gap | VERIFIED | 2026-09-15 13:57 EDT COMPLETE, 72 runs in runs_prev, gaps 657 to 1,869 steps, no failures, output in research/outputs/snap-r22-prev-step-b | dclm-baseline-75p-dolma1.7-25p 750M has no shared step below 11,250 because seed 14 stops there, so it has no previous-step runs |
| snap-r22-prev-step-c | reduce every configuration at the largest step all seeds share below the selected step | VERIFIED | 13:22 EDT push, complete by 13:32 EDT | 5 of 5 recipes, 75 runs, gaps 657 to 1,869 steps (the dclm-baseline-top-fw recipes step 657 at 150M and 787 at 300M) |
| snap-r22-prev-step-d | reduce every configuration at the largest step all seeds share below the selected step | VERIFIED | 13:24 EDT push, complete by 13:33 EDT | 5 of 5 recipes, 72 runs; dolma1.7-no-math-no-code at 1B has no step shared by all seeds below its selected step, so R23 drops that one configuration from both sides of the comparison |
| snap-r22-prev-step-e | download five recipe tarballs, reduce every configuration at the largest step all seeds share below the selected step | VERIFIED | 2026-09-15 14:15 EDT COMPLETE, 75 runs in runs_prev, every configuration has an earlier shared step, no failures, wall 2,740 s, output in research/outputs/snap-r22-prev-step-e | |
| snap-r10-gain-uncertainty | recipe bootstrap of the gain SD, default and matched simulators at its quantiles, simulator-versus-observed checks | VERIFIED | 2026-09-15 pushed before 17:00 EDT, v1, 2,889 s | gain SD 0.0420, bootstrap median 0.041 [0.011, 0.063], nonpositive 0.9 percent; default share 0.02 to 0.91, matched -0.01 to 0.12; matched copies margin means, item noise, seed variance but configuration-mean SD 0.02 to 0.45 of observed |
| snap-r8-paired-log | paired uncertainty for the log-scale prediction table: fold-level contrasts, 1,000 partitions, 2,000-draw full-refit recipe bootstrap | VERIFIED | 2026-09-15 18:01 EDT, v1, COMPLETE 18:02 EDT, 155 s | reproduces all 12 fold means exactly; margin P1-P0 [-0.0585, -0.0006], P3-P0 [-0.0599, -0.0036], P1g-P0 [-0.0591, -0.0021], P1-P3 [-0.0032, 0.0116]; accuracy every contrast with P0 includes zero |
| snap-r8-known-truth | log-scale prediction comparison on simulated populations with known seed covariance, error against truth and against the noisy target | VERIFIED | 2026-09-15 18:02 EDT, v1, 2,599 s | target noise 0.011 to 0.012 margin, 0.024 to 0.032 accuracy; matched margin P1, P1g, P3 beat P0 by 0.043 to 0.046 observed and 0.048 to 0.051 true; accuracy true errors within 0.0032; P2 and P2R not assessable in these populations |
| snap-r8-clipping | log-scale comparison under clip, drop, and training-fill rules for nonpositive held-out variances, with 2,000-draw paired bootstrap | VERIFIED | 2026-09-15 18:46 EDT push, COMPLETE 18:48 EDT, 143 s | clip reproduces paired-log exactly; margin P3-P0 and P1g-P0 exclude zero under all three rules, P1-P0 includes zero under training fill (upper 7e-5); accuracy all intervals include zero |
| snap-r23-inversion | selected and previous-step intervals on 123 configurations under wild, cluster-t, and both inversion constructions | VERIFIED | 2026-09-15 18:49 EDT push, 27 s | reproduces 1.233/1.192/1.082/1.130; previous-step accuracy wild [1.044, 1.210], centred [1.059, 1.222], restricted [1.053, 1.231], so exclusion of one holds |
| snap-r6-fieller | restricted and centred cluster test-inversion intervals on observed data and on the twelve R6 populations | VERIFIED | 2026-09-15 18:03 EDT, v1, COMPLETE 18:04 EDT, 75 s | recomputed cluster-t coverage equals R6 in all 12 cells; centred 0.931 to 0.957, restricted 0.946 to 0.970; observed margin [1.095, 1.323], accuracy [0.995, 1.162]; 530M margin [0.765, 1.737] |
| snap-r6-matched-a | coverage of wild, cluster-t, and both inversion intervals in populations with size-band seed covariance fitted to the data | RUNNING | 2026-09-15 18:14 EDT, v1 | |
| snap-r6-matched-b | as snap-r6-matched-a plus recipe variance scales from observed recipe U sums | RUNNING | 2026-09-15 18:14 EDT, v1 | |
| snap-r12-inversion | PolyPythias transport intervals under centred and restricted test inversion, each configuration a cluster | VERIFIED | 2026-09-15 18:52 EDT push, COMPLETE by 18:53 EDT | reproduces 1.41372/1.26199/2.28589/1.71085 and the configuration intervals; centred margin [0, 2.106] and [0.519, 1.664], accuracy unbounded at three and five sizes; restricted unbounded throughout |
| snap-r21-matched-step | inflation on the schedule-matched configurations and influence against truncation severity | VERIFIED | COMPLETE, output in research/outputs/snap-r21-matched-step, all 375 recorded steps equal the index common step, 92 mismatched and 26 severe (1 at 530M, 25 at 750M) | margin without severe 1.2758 wild [1.114, 1.420], shared final step (n=33, 24 recipes) 1.0821 wild [0.121, 1.530], influence vs severity Spearman -0.036 within-size permutation p=0.314, accuracy 0.0007 p=0.990. Band mean severity 0.867 at 530M and 0.415 at 750M contradicts the REPORTED 18 and 89 percent, which match the 750M minimum 0.177 and the 530M maximum 0.887 |
| snap-r23-prev-step-analysis | C01 and intervals at the previous shared step, paired recipe bootstrap of the difference, checkpoint shift over between-seed SD | VERIFIED | COMPLETE 14:21 EDT, 86 s, 123 configurations kept (dclm-baseline-75p-dolma1.7-25p 750M and dolma1.7-no-math-no-code 1B dropped), output in research/outputs/snap-r23-prev-step-analysis. Margin 1.2326 wild [1.139, 1.319] to 1.1915 [1.104, 1.275], change -0.041 recipe bootstrap [-0.078, 0.001]. Accuracy 1.0823 [0.992, 1.163] to 1.1303 [1.044, 1.210], change 0.048 [-0.022, 0.130]. Shift ratio 0.469 and 0.631, centred 0.493 and 0.618, 750M 0.889 and 1.048, within-config corr 0.871 and 0.811 | asserts shipped selected steps equal the R22 common steps |

Source dataset: `garyzhang11111/snap-compute-src` (private, created 2026-09-15 10:19 EDT). v1 snap_compute, seednoise git archive at 5a59526, `snap_adapter.py`, `boolq_map.py`; v2 `requests_audit.py`; v3 requests_audit v2 and `r6_calibration.py`; v4 (10:34 EDT) adapter `--exclude`, `snap_intervals.py`, `r2_group_sim.py`; v5 (10:48 EDT) `r9_pairs.py`. Four of five kernel slots in use at 10:35 EDT; no GPU kernel submitted.
- 2026-09-15 second review pass: 18 skills run read-only, rebuild exit 0, verify clean, stop-slop 42/50 self-scored, reviewer overall 5 unchanged. Snapshot refreshed in research/revision_2026-09-15/. R7 and R12 remain BLOCKED on GPU budget authorization.
- 2026-09-15 third pass: ml-paper-voice, adversarial-reviewer, remove-ai-marks run read-only, 6 rhythm/notation edits, rebuild exit 0, verify clean, numeric tokens accounted for, stop-slop 44/50 self-scored, rating 5 unchanged.
- 2026-09-15 independent readers: stop-slop 38/43/45/48 across four builds, ICLR reviewer rating 3 with ten defects, nine fixed in text, re-review pending. Reviewer names the 1B-only interval or DataDecide checkpoint sensitivity as the cheapest evidence for a 6, BLOCKED on compute authorization.
- 2026-09-15 reviewer re-review: rating 3 to 5, soundness 2 to 3, presentation 2 to 3, contribution 2, confidence 5. One more phrase fixed at main 140. Bar for 6 is compute, BLOCKED on authorization.
- 2026-09-15 final: independent stop-slop 49/50 confirmed on shipped build, independent ICLR rating 5. R15-R17 VERIFIED for this pass. R7, R12, and the reviewer's 1B-only or checkpoint-sensitivity job remain BLOCKED on compute authorization.
- 2026-09-15 final: independent stop-slop 50/50 on the shipped build (ninth pass, two reader caveats recorded), independent ICLR rating 5. Snapshot hash matches deliverable.
- 2026-09-15 12:20 EDT: snap-r19-size-intervals VERIFIED (OBSERVED). The reviewer's 1B-only finite interval now exists and includes one on all three positive constructions, so it adds honesty rather than support. The DataDecide-internal checkpoint sensitivity remains BLOCKED: the release stores one tarball per step under recipe/size/seed-N/step-S.tar.gz, about 4.9 GB per recipe, so an earlier-step rescoring means re-streaming all 25 recipe tarballs on Kaggle CPU (about nine kernels as in the original fetch), which is a campaign, not a rerun. Build after the edits: 32 pages, main text 8, 0/0/0 house-style counters, PDF sha256 prefix 9cd500d338cf02d42147.
- 2026-09-15 12:25 EDT: independent re-review after R19: rating 5 unchanged (soundness 3, presentation 3, contribution 2), reviewer states the 1B probe was its own error because 25 singleton clusters cannot resolve Lambda near 1.15. Stop-slop reader re-scored the build 50/50 with the two additions. Reviewer's next probe: Lambda on the 33 configurations whose three runs share a maximum step, plus per-configuration influence against truncation severity (selected step over the default run's final step). BLOCKED: per-run maximum steps aren't on disk (appendices_bcd.tex line 5 says the fetch configuration table was omitted, R4_run_manifest.csv carries only the selected common step, and reduce_recipe doesn't persist the tar index), so recovering them means streaming the 25 recipe tarballs (about 4.9 GB each) through Kaggle CPU kernels for an index-only pass, which needs the user's authorization. Main-text pointer fixed to Appendix app:supplementary. Build 32 pages, main text 8, counters 0/0/0.
- 2026-09-15 12:45 EDT: fresh pass on the current build. Skills re-invoked and executed against the files: superpowers:using-superpowers, humanizer (0 defects), stop-slop (self 48, independent 50 after two one-sentence edits), ml-paper-voice (0 defects), paper-humanizer (0 defects under house overrides), ars-reviewer, ars-abstract, ars-citation-check, ars-disclosure, ars-rebuttal-audit, ars-revision-coach, ars-3w, ars-outline (results in R16_review.md). One passive with a known actor fixed at main.tex line 144. Verify counters 0/0/0 on all three files, 32 pages, main text 8, PDF sha256 prefix 6c9b2b3ac7cc96752e50. R15 to R17 VERIFIED on this build.
- 2026-09-15 13:08 EDT: user directed that all compute run on Kaggle and that no cluster is available; read as authorization for the index-only campaign the reviewer's matched-step probe needs. Five CPU kernels pushed, one per five recipes, within the five-concurrent cap.
- 2026-09-15 13:17 EDT: per-kernel background pollers run for index kernels a, c, and e (a and c re-push the patched script once on failure), and a queue script pushes snap-r21-matched-step when all five report COMPLETE.
- 2026-09-15 13:25 EDT: user asked for five kernels active at once. Added the DataDecide checkpoint-sensitivity campaign (R22 reductions, R23 analysis), which answers the main-text statement that checkpoint effects at DataDecide's selected steps remain unmeasured. A background scheduler keeps five tracked kernels RUNNING or QUEUED and pushes R21 and R23 when their inputs complete; each kernel has its own background poller.
- 2026-09-15 13:27 EDT: snap-r22-prev-step-a v1 carries the same capitalised tarball names and will fail those two recipes; the scheduler pushes the corrected v2 once v1 finishes, and snap-r23 waits for v2. The shipped selected-step runs were fetched with the lower-case names, so the manuscript data are unaffected.
- 2026-09-15 14:05 EDT: main.tex and Appendix B.1 now state the OBSERVED band means (87 and 41 percent) and ranges from the R21 index in place of the REPORTED 18 and 89 percent, and Appendix B.1 adds Table tab:truncation with the R21 subset estimates. The previous-step comparison can cover at most 123 configurations, since two lack an earlier shared step.

- 2026-09-15 14:40 EDT: abstract, introduction, limitations, and Appendix C now report the R23 OBSERVED previous-step estimates, and Table tab:checkpoint replaces the claim that DataDecide checkpoint sensitivity remains unmeasured. The accuracy near-independence reading is now stated as checkpoint dependent.
- 2026-09-15 14:50 EDT: independent audit of the R21 and R23 passages found one rounding error (0.871 to 0.870) and four overstatements, all corrected. Abstract and introduction now state that the paired change intervals include zero and use the 123-configuration baselines.

- 2026-09-15 17:30 EDT: R10 and R11 results added to main.tex and appendices_bcd.tex; build exit 0, 34 pages, main text 9 pages, verify clean except table_numeric_tokens_preserved (new tables only). R7 audit shows the shipped competence adjustment reduces to a WinoGrande-only proxy; manuscript competence text waits for snap-r7-proxy-null.
- 2026-09-15 17:25 EDT: R12 OBSERVED values written to main.tex (transfer paragraph, limitations) and Appendix C (transport and adjacent-checkpoint paragraphs).- 2026-09-15 17:35 EDT: snap-r11-removal (OBSERVED) reproduces all 20 removal estimates in tab:original10 and adds wild intervals and aggregate and independence SDs; table rebuilt and one appendix passage added (BoolQ removal lowers aggregate SD; WinoGrande removal accuracy interval 1.023-1.178 excludes one). Build exit 0, 35 pages, main text 9, verify clean.
- 2026-09-15 17:23 EDT: snap-c11-integrity (C11, OBSERVED) passes artifact integrity for all 36 snap_compute outputs (C00, C01, C02, C03, C07, C08, C09) behind the manuscript's C-job numbers: job identity, FINISHED_UNCHECKED status, config hash, and every artifact SHA256. C11 certifies integrity only, never scientific claims.

- 2026-09-15 17:45 EDT: competence proxy audit and calibration written to main.tex (method paragraph, limitations) and Appendix C app:proxy; build exit 0, 36 pages, main text 9, verify clean.

- 2026-09-15 18:05 EDT: independent audit fixes applied (BoolQ off-diagonal wording -0.15 and -0.06, 0.0007 gap, item-count weighting sentence, undefined accuracy subsets, tab:proxy caption, limitations names WinoGrande and repaired margin proxies, app:proxy wording, three- and five-size accuracy factors, fixed-K sentence, stale PolyPythias accuracy interval, BoolQ adjacent values no longer called largest since only BoolQ was computed). R12 kernel docstring corrected to the 6,000-token cap. snap-r10-gain-uncertainty (OBSERVED) written to Appendix gain calibration and one main-text clause, and the matched-simulator description now matches matched_spec. Build exit 0, 37 pages, main text 9, verify clean except table_numeric_tokens_preserved (new tables only).
- 2026-09-15 18:05 EDT: correction to kernel docstrings. The clock times written as "design fixed" in several docstrings are later than the files' modification times, so they were estimates, not records. File mtimes are proxy-audit 16:49, proxy-null 16:53, proxy-null2 16:59, r12-analysis 16:59, r11-removal 17:16, gain-uncertainty 16:58, branch-index 17:24, r10-scale-transform 16:38, r11-composition 16:37, r8-paired-log 17:58, r8-known-truth 17:59, r6-fieller 18:01. Each kernel was pushed before its output existed, so the designs still precede their results.
- 2026-09-15 18:05 EDT: R10 row now VERIFIED for the gain part as well. Three new CPU kernels fill the free Kaggle slots, covering the paired log-scale ranking uncertainty and target noise the plan asks for under R8 and the test-inversion interval family listed under R6.
- 2026-09-15 18:15 EDT: snap-r8-paired-log and snap-r6-fieller (OBSERVED) written to the abstract, the prediction and inference paragraphs of main.tex, Appendix app:paired, tab:coverage (new Inversion column), and the size-band paragraph. The 530M margin interval no longer excludes one under inversion, and the manuscript says so. Build exit 0, 38 pages, main text 9, verify clean except table_numeric_tokens_preserved.

- 2026-09-15 18:25 EDT: independent audit of the new gain, paired-prediction, test-inversion, and proxy passages found 13 defects (two rounding errors at -0.058 and -0.059, the 95 to 97 percent share, an overstated atypical-partition claim, the restricted inversion conservative in eleven of twelve populations, margin-only configuration-mean ratios, 1.2 and 6.1 percent tolerances, factor 7.5, tab:proxy caption, computed-interval wording, six-model count); all corrected, one redundant sentence removed. Rebuild exit 0, main text 9 pages, verify clean, snapshot refreshed with SHA256SUMS.
- 2026-09-15 18:50 EDT: snap-r8-known-truth (OBSERVED) added to Appendix app:paired as one paragraph. Build exit 0, main text 9 pages, verify clean. snap-r8-clipping pushed into the freed slot.
- 2026-09-15 19:00 EDT: snap-r8-clipping (OBSERVED) added to the tab:original11 paragraph and one main-text clause; the abstract now names only the full correlation model as beating independence on margins, since the rank-one interval reaches zero under training fill. Build exit 0, main text 9 pages, verify clean.
- 2026-09-15 19:05 EDT: snap-r23-inversion (OBSERVED) added as two sentences to Appendix C checkpoint paragraph. Earlier ledger times for snap-r8-clipping and snap-r23-inversion pushes were written as 18:52 and 18:59 but the Kaggle pushes happened at about 18:46 and 18:49 by the poll logs.
- 2026-09-15 19:10 EDT: snap-r12-inversion (OBSERVED) added to the PolyPythias transport paragraph and one main-text clause; PolyPythias accuracy inflation is unbounded under inversion. Build exit 0, main text 9 pages, verify clean.

| snap-r6-precision | R6 decisive cells (cross-half 0.1, dominant recipe, margin-like, accuracy-like) at 10,000 replicates, seed 20260917, wild, cluster-t, centred and restricted inversion | pushed 2026-09-15 about 19:15 EDT | RUNNING |

2026-09-15 19:15 EDT. Pushed snap-r6-precision to fill the fifth slot, per the plan item that decisive coverage cells receive adequate replication (Monte Carlo SE 0.0022 at 10,000). Refreshed revision_2026-09-15 snapshot and SNAP_revised_draft.pdf after the known-truth, clipping, r23 and r12 inversion edits (build exit 0, main text 9 pages, SHA256SUMS OK).

2026-09-15 about 19:25 EDT. Independent opus audit of the known-truth, clipping, r23 and r12 inversion passages found five defects, all verified against the JSON and fixed. (1) Python round() had turned exact ties such as 0.9315 into 0.931, so tab:coverage now uses half-up rounding regenerated from snap-r6-calibration-a/b summary.json and snap-r6-fieller r6_fieller.json, which changes eight cells (recipe 0.937, 0.929, 0.927; cluster-t 0.936, 0.911, 0.926; config 0.937, 0.930; inversion 0.932) and the text values 0.911 and 0.932. (2) The restricted inversion is above 0.95 in eleven cells but more than two Monte Carlo SEs above in only eight. (3) The PolyPythias repeat reproduces the earlier estimates, bootstrap intervals and SEs, and the inversion sets are new. (4) Under the drop rule P3 stays best on accuracy (0.0105 to 0.0090). (5) Under independent seeds only P1, P1 G and P3 lose 0.001 to 0.007, while P2 loses 0.011. Rebuild exit 0, main text 9 pages.

| snap-r7-branch-index | branch index for the 375 runs, metadata only | pushed 2026-09-15 about 17:58 EDT | COMPLETE, 6,454 s |
| snap-r7-crossfit | cross-fitted regularised proxies (shipped, shrunk, one-factor, clip-drop), 999-draw recipe bootstrap with full refit, 200 independence-null replicates | pushed 2026-09-15 about 19:30 EDT | RUNNING |

2026-09-15 about 19:30 EDT. snap-r7-branch-index returned 40 of 125 repositories readable and 85 with HTTP 401, so 120 of 375 runs were matched to a branch. Median Jaccard overlap 1.0, minimum 0.042, selected step present for all 120, branch final step agreeing with the evaluation final step in 99 of 120. That closes the preferred R7 route, since held-out loss needs weights we can't download, and app:proxy now records it. Pushed snap-r7-crossfit to fill the freed slot with the plan's fallback, a prespecified regularised proxy learned on training recipes only and scored out of fold against an unadjusted baseline and its own independence null.

| snap-r6-precision | four decisive cells, 10,000 replicates, seed 20260917 | pushed about 19:15 EDT | COMPLETE, 1,293 s |
| snap-r6-precision-b | remaining eight cells, 10,000 replicates, cell index offset by ten | pushed about 19:42 EDT | RUNNING |

2026-09-15 about 19:42 EDT. snap-r6-precision reports wild coverage 0.9334, 0.9354, 0.9404 and 0.9449 with Monte Carlo SE near 0.0025, centred inversion 0.9353, 0.9561, 0.9419 and 0.9466, restricted 0.9513 to 0.9696, and cluster-t 0.9156 to 0.9416. The margin-like wild shortfall is about four SE below nominal, so it isn't simulation noise, and the cross-half cell misses above in 0.054 of replicates. Written into the coverage appendix and one main-text sentence, build exit 0, main text 9 pages. Pushed snap-r6-precision-b for the other eight cells.

2026-09-15 about 19:50 EDT. snap-r7-crossfit COMPLETE in 620 s. Split-half reliability of the shipped proxy is -0.0857 on margins and -0.0718 on accuracy, shrunk gives 0.9127 and 0.3012, one-factor gives 0.9653 and 0.7383. Cross-fitted margin inflation 1.2454 shipped, 0.7307 shrunk, 0.9725 factor, 0.8890 clip-drop, and on accuracy 1.0728, 1.0041, 1.0167, 0.9936. The 999-draw refitting bootstrap gives factor margins 0.935 to 1.018 and factor accuracy 0.970 to 1.104. Independence nulls return 0.998 to 1.003 for all four variants, so no variant manufactures covariance. Factor weight concentration 0.353 margins and 0.467 accuracy, minimum fold weight correlation 0.847. Written into app:proxy, with the reliability fact and the gated-weights reason added to the main-text limitation. Build exit 0, main text 9 pages.

| snap-r6-matched-b | fitted scenario size_recipe, 2,000 replicates | pushed about 18:20 EDT | RESULTS COMPLETE, kernel state ERROR at the final rows write |
| snap-r6-variants | six interval constructions on the four hard cells, 4,000 replicates, seed 20260918 | pushed 2026-09-15 about 20:08 EDT | RUNNING |

2026-09-15 about 20:08 EDT. snap-r6-matched-b wrote r6_matched_size_recipe.json and then raised TypeError: Object of type bool is not JSON serializable while writing rows_size_recipe.json, so Kaggle marks the kernel ERROR although every reported number survived. Margin coverage is wild 0.946, cluster-t 0.9005, centred 0.9765, restricted 0.985, and accuracy coverage is 0.945, 0.927, 0.958, 0.9705. The observed maximum recipe influence share of 0.520 on margins sits at the 40th percentile of that scenario, and the accuracy share of 0.255 sits below 0.7975 of it. Patched both matched kernels to serialise numpy bools, without rerunning, since only the per-replicate rows file was lost. Pushed snap-r6-variants to ask whether Mammen weights, Webb weights, leave-one-cluster-out residual scaling, or the G/(G-1) correction reach nominal coverage where the shipped wild interval covers 0.933 to 0.945.

| snap-r6-matched-a | fitted scenario size, 2,000 replicates | pushed about 18:20 EDT | RESULTS COMPLETE, kernel state ERROR at the final rows write |
| snap-r6-crosshalf | cross-half error correlation sweep at zero seed correlation, 1,000 replicates per grid point, seed 20260919 | pushed 2026-09-15 about 20:22 EDT | RUNNING |

2026-09-15 about 20:22 EDT. snap-r6-matched-a hit the same rows-write TypeError after writing r6_matched_size.json. Margin coverage is wild 0.9535, cluster-t 0.9455, centred 0.9535, restricted 0.9695, and accuracy is 0.947, 0.940, 0.951, 0.966. That scenario doesn't reproduce the observed recipe concentration, since the observed margin influence share of 0.520 sits above 0.9905 of its replicates, whereas the recipe-scaled scenario puts it at the 40th percentile. Both scenarios now appear in the coverage appendix, labelled as fitted and not independent confirmation. Build exit 0, main text 9 pages.

| snap-r6-crosshalf | cross-half correlation sweep, zero seed correlation, 1,000 replicates per point | pushed about 20:22 EDT | COMPLETE, 37 s |
| snap-r6-sharednoise | shared half effect across benchmarks, sweep of q rho_f, 2,000 replicates per point, seed 20260920 | pushed 2026-09-15 about 20:33 EDT | RUNNING |

2026-09-15 about 20:33 EDT. snap-r6-crosshalf shows that within-benchmark cross-half noise correlation can't manufacture inflation. With the true correlation at zero, the mean estimate stays between 0.9969 and 1.0012 for every correlation from -0.1 to 0.5, no margin replicate reaches 1.2439, and only the dispersion falls, from 0.0567 to 0.0461. The accuracy null reaches the observed 1.0784 in 0.189 of replicates at zero and 0.068 at 0.5, which is a statement about the accuracy estimate's weakness rather than about the correlation. Written into the coverage appendix, with the explicit note that noise shared across benchmarks within one half is a different threat. That threat is what snap-r6-sharednoise now measures, since a common half effect enters the numerator alone.

2026-09-15 about 20:40 EDT. snap-r6-sharednoise COMPLETE in 52 s. A scalar half effect shared by every benchmark and repeated in the opposite half raises the margin mean monotonically from 0.9993 at share zero to 1.6058 at share 0.4, and interpolation puts the observed 1.2439 at share 0.1242. At share 0.05 only 0.0065 of replicates reach the observed value. Accuracy reaches its observed 1.0784 at share 0.0091, and 0.213 of replicates reach it at share zero. Written into the coverage appendix as an assumption a reader would have to make, not as a measured bound, together with the note that the passage-aware split covers within-benchmark structure only.

| snap-r6-sharednoise | shared half effect sweep | pushed about 20:33 EDT | COMPLETE, 52 s |
| snap-r6-shared-fit | regression of off-diagonal seed covariance on the item-noise outer product, 999-draw bootstrap, 500 matched nulls and 500 contaminated replicates | pushed 2026-09-15 about 20:47 EDT | RUNNING |

2026-09-15 about 20:55 EDT. snap-r6-precision-b COMPLETE in 2,610 s, so all twelve coverage cells now rest on 10,000 replicates for the wild, cluster-t, centred and restricted procedures. No cell moves by more than 0.011 against its 2,000-replicate value. The ranges are wild 0.9334 to 0.9527, cluster-t 0.9156 to 0.9486, centred 0.9353 to 0.9561 and restricted 0.9513 to 0.9696. The ordering restricted at least centred at least wild at least cluster-t holds in eleven of twelve cells, failing only at mean correlation 0.2 where wild beats centred by 0.0016. Written into the coverage appendix, and the main-text sentence now covers all twelve.

| snap-r15-composition | all 1,013 subsets of size two or more, wild intervals, both phenotypes | pushed about 21:00 EDT | COMPLETE, 80 s |

2026-09-15 about 21:10 EDT. snap-r15-composition reproduces tab:fixedk exactly, including median 1.012 and share 0.38 at two benchmarks, share 0.98 at eight, and the full-battery 1.244 with 1.143 to 1.338 and 1.078 with 0.993 to 1.157. The new material is the pooled distribution, so the appendix now gives margin median 1.136 with quartiles 1.018 and 1.390, range 0.927 to 1.786, accuracy median 1.038 with quartiles 1.001 and 1.229, range 0.315 to 5.439, and pooled exclusion shares 0.658 and 0.305. The duplicate paragraph I first wrote was removed, since the per-size analysis already existed. Build exit 0, main text 9 pages.

| snap-r7-crossfit-null | four cross-fitted weightings against independence, equicorrelated and matched covariance, 300 replicates each | pushed about 19:55 EDT | COMPLETE, 2,520 s |
| snap-r12-predict | held-out prediction of the observed ratio on PolyPythias, 84 fitting triples per configuration scored on the six disjoint runs | pushed 2026-09-15 about 21:22 EDT | RUNNING |

2026-09-15 about 21:25 EDT. snap-r7-crossfit-null settles how to read the cross-fitted proxy values. Under the matched covariance with no competence variable, margin means are shipped 1.0973, shrunk 0.7450, factor 0.9796 and clip-drop 1.0289, and accuracy means are 1.1089, 1.0485, 1.0660 and 1.0724. The observed cross-fitted values lie inside the central 90 percent for shipped, shrunk and factor on both scales, so they carry no evidence of a competence mechanism beyond covariance removal. Clip-drop is the exception, at 0.8890 against a fifth percentile of 0.9076 on margins and 0.9936 against 1.0027 on accuracy. Under the equicorrelated structure the factor weighting returns 0.8524, below its matched value, so these proxies can overshoot. Written into app:proxy.

| snap-r12-predict | PolyPythias held-out prediction, 84 triples per configuration | pushed about 21:22 EDT | COMPLETE, 16 s |
| snap-r12-predict-pooled | same design with the target pooled across configurations | pushed 2026-09-15 about 21:37 EDT | RUNNING |

2026-09-15 about 21:37 EDT. snap-r12-predict is a negative result and the paper now says so. Predicting the ratio measured on six held-out PolyPythias runs, the mean absolute log error is 0.3392 for independence, 0.5362 for the plug-in and 0.5125 for the decomposition on three-size margins, and 0.3697, 0.4471 and 0.3843 on accuracy. The five-size set repeats the ordering. The decomposition beats the plug-in in 0.809 and 0.754 of margin splits, so the item-noise term helps as intended, but neither fitted prediction beats the constant. The target is noisy, with the margin ratio running from 0.7013 to 1.8508 between its fifth and ninety-fifth percentiles, so snap-r12-predict-pooled repeats the test with the target pooled over configurations. Appendix and main-text limitation updated, build exit 0, main text 9 pages.

| snap-r6-variants | six interval constructions, four hard cells, 4,000 replicates | pushed about 20:08 EDT | COMPLETE, 1,960 s |
| snap-r12-predict-pooled | pooled-target version of the held-out prediction | pushed about 21:37 EDT | COMPLETE, 15 s |
| snap-r6-variants-b | same six constructions on the other eight cells, cell index offset by forty | pushed 2026-09-15 about 21:50 EDT | RUNNING |

2026-09-15 about 21:50 EDT. snap-r6-variants reproduces the shipped wild interval exactly, with a largest endpoint difference of 0.0 in all four cells, and finds one construction that reaches nominal coverage. The wild bootstrap-t with leave-one-cluster-out residual scaling covers 0.9473, 0.9575, 0.9525 and 0.9553, against 0.9405, 0.9395, 0.9455 and 0.9460 for the shipped Rademacher version, at median widths 4 to 12 percent wider. Mammen weights are worse everywhere, Webb weights match Rademacher, and the G/(G-1) cluster-t correction covers 0.9400, 0.9270, 0.9453 and 0.9475. That construction was chosen after seeing these cells, so snap-r6-variants-b now runs the other eight before the paper says anything about it.

2026-09-15 about 21:52 EDT. snap-r12-predict-pooled reverses the negative result, so target noise drove it. With the target pooled across configurations, its standard deviation across splits falls to 0.1403 on three-size margins, and the decomposition beats independence at 0.2754 against 0.3195 there, 0.3667 against 0.5226 on three-size accuracy, 0.2088 against 0.2175 on five-size margins, and 0.2507 against 0.3641 on five-size accuracy, winning in 0.60, 0.73, 0.63 and 0.71 of splits. The decomposition beats the plug-in on every set. Both versions are now in the appendix and the main-text limitation names both directions.

| snap-r6-shared-fit | off-diagonal seed covariance regressed on the item-noise outer product, 999-draw bootstrap, 500 matched nulls, 500 contaminated replicates | pushed about 20:47 EDT | COMPLETE |

2026-09-15 about 22:05 EDT. snap-r6-shared-fit is a diagnostic with no power, and the appendix now says exactly that. The observed margin slope is 1.6746 with a recipe bootstrap interval of 0.5337 to 3.0677, and the accuracy slope is 0.0327 with an interval of -0.0057 to 0.0731. Matched simulations carrying no shared effect already give a mean margin slope of 1.6823, and adding the shared effect at the share that reproduces the observed estimate moves that mean only to 1.8079 against a replicate spread of 1.2899 to 2.3709. A seed covariance that follows the item-noise scales is indistinguishable from contamination that follows them, so the cross-half assumption is reported as untested rather than as satisfied.

2026-09-15 about 22:10 EDT. Correction to the cross-half sweep paragraph. It said that noise shared across benchmarks within one half would enter the numerator alone, which is wrong, because a scalar half effect adds the same quantity to every entry of the cross-half covariance and the diagonal of that matrix is what the denominator reads. With ten equally weighted benchmarks the numerator absorbs the whole shared variance and the denominator absorbs a tenth of it, which is why the channel inflates rather than why it is invisible. The sentence now states the mechanism in those terms. Build exit 0, main text 9 pages, short-sentence check empty.

| snap-r6-itemshare | permutation of run-specific item deviations inside each group, 200 permutations, plus 200 fresh item half splits | pushed 2026-09-15 about 22:55 EDT | RUNNING |
| snap-r6-observed-intervals | six interval constructions applied to the 125 shipped configurations, 9,999 draws over twenty seeds, full battery and BoolQ removed | pushed about 22:55 EDT | RUNNING |
| snap-r6-precision-c | Rademacher against leave-one-cluster-out scaling at 10,000 replicates on the four hard cells | pushed about 22:55 EDT | RUNNING |

2026-09-15 about 22:55 EDT. snap-r6-itemshare is the test the failed regression diagnostic could not be. Permuting each run's item deviations inside a group leaves every group mean and therefore every trait score untouched, and it leaves any component shared by all items of a group untouched, so a genuine run-level shift survives while the association between particular items and particular runs is destroyed. An observed estimate above that permutation distribution carries item-level sharing, and an observed estimate inside it bounds the passage channel directly rather than by assumption.

| snap-r6-observed-intervals | eight interval constructions on the 125 shipped configurations, 9,999 draws over twenty seeds, full battery and BoolQ removed | pushed about 22:55 EDT | COMPLETE, 43 s |

2026-09-15 about 23:05 EDT. snap-r6-observed-intervals answers the question a reviewer asks after the coverage table, which is how much the reported interval moves under a different construction. On the full battery every one of the eight excludes one on margins and includes one on accuracy, and with BoolQ removed every one sits above one on both scales. The margin interval runs from 1.141 to 1.339 under Rademacher weights and from 1.130 to 1.349 under leave-one-cluster-out scaling, and the restricted inversion is the widest at 1.074 to 1.329. Endpoint spread across twenty bootstrap seeds never exceeds 0.0023, and the shipped 4,999-draw interval sits within 0.003 of the 9,999-draw Rademacher mean in all four batteries. Written into the coverage appendix, build exit 0, main text 9 pages.

| snap-r6-matched-variants | leave-one-cluster-out scaling and the G/(G-1) cluster-t added to both fitted scenarios, 2,000 replicates, seed 20260923 | pushed 2026-09-15 about 23:20 EDT | RUNNING |

2026-09-15 about 23:20 EDT. The variants work so far lives entirely in exchangeable populations, so a construction chosen there might not hold where the covariance is fitted to the data. snap-r6-matched-variants scores the same two extra constructions in the size and size-recipe scenarios, on fresh datasets, and version 1 of the kernel was replaced before it ran because it still computed the Mammen and Webb intervals that nothing summarises.

| snap-r6-itemshare | 200 within-group permutations of run-specific item deviations, plus 200 fresh item half splits | pushed about 22:55 EDT | COMPLETE, 588 s |

2026-09-15 about 23:35 EDT. snap-r6-itemshare bounds the channel the regression diagnostic could not reach. Under 200 permutations the margin estimate has mean 1.2428 with standard deviation 0.0008, and 0.075 of permutations reach or exceed the observed 1.2439, while on accuracy the mean is 1.0764 and 0.34 reach or exceed the observed value. Item-level sharing therefore accounts for about 0.0011 of the margin estimate and 0.0020 of the accuracy estimate, against excesses of 0.244 and 0.078, so a shared passage or a shared source document can't explain the inflation. Re-drawing the half split 200 times gives a real spread of 0.0018 on margins against 0.0009 under permutation, and 0.0083 against 0.0055 on accuracy, so item structure widens the spread without moving the centre. The shipped split returns 1.244 against a re-split median of 1.246, and 1.078 against 1.083, so the reported numbers sit on the low side of the split distribution. The whole-benchmark form of the violation survives the permutation untouched and stays untested, and the appendix says so. Written into the coverage appendix and into the BoolQ paragraph of the main text, build exit 0, main text 9 pages.

2026-09-15 about 23:50 EDT. Independent Opus audit of every passage added since the previous audit returned ten defects, nine of which I verified against the source JSON and fixed. Four were rounding, where 1.049 should be 1.048 for the shrunk cross-fitted accuracy mean, 0.513 should be 0.512 for the three-size margin decomposition, 0.320 should be 0.319 for pooled three-size independence, and the main-text inversion range 0.932 to 0.957 was the 2,000-replicate figure sitting next to a 10,000-replicate sentence, now 0.935 to 0.956. Two were quantifier errors, where the cross-half sweep range 0.997 to 1.001 covers margins alone and the main text said predictions rather than the decomposition alone, since the pooled plug-in loses to independence on five-size margins. One was a percentile read from the wrong side, because 0.401 of replicates reach or exceed the observed influence share, which puts the observed value at the 60th percentile and not the 40th. One described the matched-b generator as scaling by a recipe's share of the denominator sum when the code uses that recipe's denominator sum over the mean recipe, which differs by a factor of 25. One claimed the item-noise term matters more than the covariance term, which fails on pooled three-size margins where the covariance gain is 0.040 against 0.016. The tenth defect is unresolved and needs compute, because Appendix A reports 0.543 for the largest recipe's share of squared margin influence while snap-r6-matched-a recomputed 0.520 from cluster_influence.

| snap-r6-influence | recomputation of recipe influence concentration under six definitions, plus the removal change for every recipe | staged, waiting for a free slot | PENDING |

| snap-r6-variants-b | six constructions on the remaining eight cells, 4,000 replicates, cell index offset by forty | pushed about 21:50 EDT | COMPLETE, 3,722 s |

2026-09-16 about 00:10 EDT. snap-r6-variants-b confirms the leave-one-cluster-out construction on the eight cells it never saw, so it now enters the paper. Across all twelve populations it covers 0.9425 to 0.9577 against 0.9315 to 0.9510 for the shipped Rademacher version, it improves every cell by between 0.0068 and 0.0180, and it reaches 0.95 in eight cells against one. Median widths run 4 to 12 percent wider. Mammen weights lose in every cell, Webb weights sit within 0.0023 of Rademacher, the corrected cluster-robust t covers 0.9230 to 0.9497, and the residual-scaled version matches or beats the centred inversion in all twelve by up to 0.0115. The kernel reproduces the shipped wild interval to 2.2e-16 in every cell it checks. The appendix states that we chose the construction after seeing the first four cells, so the other eight confirm it on the same generator family rather than test it independently, and the paper keeps reporting the shipped interval.

| snap-r6-influence | recipe influence concentration under five definitions, plus the removal change for every recipe | pushed 2026-09-16 about 00:15 EDT | COMPLETE |

2026-09-16 about 00:25 EDT. snap-r6-influence resolves the tenth audit defect against Appendix A. The largest recipe's share of squared margin influence is 0.5199 under the theta linearisation, under the delta-method transfer to Lambda, and under the unscaled residual, so the figure doesn't depend on which of those the reader assumes. Appendix A's 0.543 doesn't reproduce under any of them, and its participation ratios of 2.88 and 8.68 come out at 3.081 and 9.016. Its removal figure does reproduce, since dropping the top-influence recipe moves the margin estimate by 0.0368 and no recipe moves it further. Appendix A now carries the recomputed values and names them as recomputed. Concentration on the cluster numerators alone gives 0.337 and on the denominators alone 0.426, which are different quantities and are recorded here so a later reader doesn't confuse them with the influence share.

| snap-r6-influence-b | recipe influence concentration under plain, CR1, CR2, CR3 and jackknife weighting | pushed 2026-09-16 about 00:35 EDT | COMPLETE |

2026-09-16 about 00:45 EDT. snap-r6-influence-b explains where the record's 0.543 came from. Leverage corrections raise the largest recipe's share, since the plain and CR1 weightings both give 0.5199 with ratios 3.081 and 9.016, CR2 gives 0.5291 with 2.998 and 8.787, and CR3 gives 0.5379 with 2.922 and 8.560. The jackknife that drops each recipe and recomputes theta returns the CR3 values to every printed digit, which is the internal check that the correction is doing what it claims. The record's 0.543, 2.88 and 8.68 therefore sit closest to the CR3 family and don't match the plain linearisation, but nothing reproduces them exactly, with gaps of 0.005 in the share and 0.04 and 0.12 in the ratios. Appendix A now names the plain linearisation, gives 0.520 with 3.08 and 9.02, and adds the leverage-corrected 0.538 with 2.92 and 8.56 so a reader can see that the figure moves with the correction.

2026-09-16 about 00:45 EDT. The abstract, the contribution list and the limitation paragraph all quoted the wild coverage range as 0.928 to 0.954, which is the 2,000-replicate figure, while every cell now rests on 10,000 replicates. All three now read 0.933 to 0.953. The appendix keeps 0.928 to 0.954 where it describes the 2,000-replicate run and prints that run's table, so the two ranges stay attached to their own runs.

| snap-r6-record-check | recomputation of the seed-label permutation experiment, the numerator design effects and the linearised standard errors | pushed 2026-09-16 about 00:55 EDT | COMPLETE, 159 s |

2026-09-16 about 01:05 EDT. snap-r6-record-check tested the three figures the paper still credited to the original analysis record, and two of them reproduce while one does not. The numerator design effects come out at 2.0565 and 1.2562 against the recorded 2.06 and 1.26, which is exact. The linearised standard errors come out at 0.0450 and 0.0937 on margins and 0.0377 and 0.0518 on accuracy, so three of the four printed values were right and the margin configuration figure had been rounded down from 0.0937 to 0.093, which also carried the ratio to 0.24 when the recomputed ratio is 0.23. The seed-label permutation is the one that moves. At 2,000 replicates the wild interval covers 0.9435 on margins and 0.9485 on accuracy with a Monte Carlo standard error near 0.005, against the recorded 0.925 and 0.954 from 1,000 permutations, so the margin figure sits about two standard errors above the record rather than clearly contradicting it. The paper now reports the recomputation, keeps the recorded values beside it in the appendix, and Appendix A no longer claims a measurable calibration error, because 0.944 lies about one standard error below nominal. The tail imbalance survives, with 0.039 of intervals below the null against 0.018 above.

| snap-r6-record-check-b | Frobenius distances, interval half-widths, cluster-robust permutation comparator, jackknife standard errors, battery correlation | pushed about 01:15 EDT | COMPLETE, 68 s |

2026-09-16 about 01:25 EDT. snap-r6-record-check-b clears the record on almost everything it touched, which isolates the permutation experiment as the single place the record and a recomputation disagree. The Frobenius distances come out at 5.5889 and 6.4949 against the recorded 5.59 and 6.49, with eighteen and 34 undefined ordered off-diagonal entries and diagonal substitutions contributing exactly 1.0 and 2.0, so every part of that sentence holds. The wild interval half-widths come out at 0.0974 and 0.0820 against the recorded 0.0974 and 0.082. The delete-one-recipe jackknife standard errors come out at 0.0489 and 0.0391 against the recorded 0.049 and 0.039. The cluster-robust comparator under permutation gives 0.9005 and 0.9375 against the recorded 0.881 and 0.941, which repeats the pattern from the wild comparator, where the margin figure sits about two standard errors above the record and accuracy agrees. No margin permutation reaches the observed estimate in either run, and the accuracy upper tail is 0.0195 against the recorded 0.021. The appendix now prints both comparator runs. The battery check didn't test what it was meant to, because I computed the mean seed correlation of 0.0608 and 0.0181 while the paper's 0.73 is an aggregate reliability, so that one figure stays unverified.

| snap-r6-record-check-c | battery rho_g, per-trait reliabilities, per-trait information ratios | pushed about 01:30 EDT | COMPLETE |

2026-09-16 about 01:40 EDT. snap-r6-record-check-c closes the provenance audit, and every figure it tested reproduces. The accuracy battery rho_g comes out at 0.7337 against the recorded 0.73, computed as Lambda squared over Lambda squared plus vbar with vbar 0.4222 at Lambda squared 1.1629. The per-trait information ratios give a median of 1.6082, a minimum of 1.5711 and a maximum of 1.7316 against the recorded 1.61, 1.571 and 1.732, so the summary the pipeline released without the underlying ratios is now an observation rather than a report. Accuracy carries two traits with negative reliability and two more below 0.10, exactly as recorded. Across the three record-check runs, eleven of twelve figures reproduce, and the seed-label permutation coverage is the only one that moves, by about two standard errors on margins in both the wild and the cluster-robust comparator.

| snap-r6-precision-c | Rademacher against leave-one-cluster-out scaling, four hard cells, 10,000 replicates, seed 20260922 | pushed about 22:55 EDT | COMPLETE, 2,522 s |

2026-09-16 about 01:55 EDT. snap-r6-precision-c confirms the construction and breaks one claim. The residual-scaled version beats the shipped one in all four cells again, by 0.0064 to 0.0179, covering 0.9464, 0.9529, 0.9572 and 0.9567 against 0.9365, 0.9350, 0.9508 and 0.9472, and the reimplementation still reproduces the shipped interval to 2.2e-16. The broken claim concerns the margin-like cell, where this run returns wild coverage of 0.9508 against 0.9404 from snap-r6-precision. Both rest on 10,000 replicates with a Monte Carlo standard error near 0.0022, so the difference of 0.0104 is about three standard errors of its difference, and a homogeneity test over the three runs of that cell returns about 0.005. The generators are byte-identical apart from the appended constructions, the median widths agree to four decimals at 0.2417 against 0.2422, and the undefined share is zero in both, so this is dataset variation rather than an implementation difference. The appendix no longer says the margin-like shortfall sits four standard errors below nominal. It now reports both runs, pools them at 0.946, and points the undercoverage claim at the cross-half and dominant-recipe cells, which repeat within 0.004.

### 2026-09-15 22:10 EDT — second Opus audit over tonight's twelve changed passages
Six defects reported, all six verified against the source JSON and fixed in `deliverables/appendices_bcd.tex`.
1. Line 426 claimed no observed-interval endpoint "moved by more than 0.0023 across those seeds". `r6_observed_intervals.json` records only per-seed standard deviations, the largest being 0.0022474 at `without_boolq/accuracy/wild_cr3/hi_sd`, and no range was ever computed. Rewritten as a standard-deviation bound.
2. Line 428 twice called the first four variant cells "the four hardest". At 10,000 replicates `margin_like` covers 0.9404 and `accuracy_like` 0.9449, which rank sixth and eighth of twelve, so two of the four were chosen for being calibrated to the observed estimates rather than for being hard. Both mentions now say so, matching line 424.
3. Line 428 said the residual-scaled construction "was the only one that reached nominal coverage" in those four. `r6_variants.json` gives `wild_cr3` 0.94725 in the cross-half cell, below nominal, and `centred` 0.9565 under one dominant recipe, above it. Now reported as three of four for `wild_cr3` and at most one for every other construction.
4. Line 430 attributed inflation to "noise shared across benchmarks within one half". `snap-r6-sharednoise.py` builds `fb = sgn * fa`, so a half effect confined to one half contributes zero to every cross-half entry and the channel requires repetition in the opposite half. Corrected, and it now matches the paragraph that follows.
5. Lines 426 and 428 made a blanket claim that no conclusion depends on the construction, while line 465 rests a remark on the reported upper endpoint leaving 0.011 of separation from the planned 1.349. `full/margin/wild_cr3/hi` is 1.3489775, which erases that margin. The blanket claim is narrowed to the include-one and exclude-one conclusions, and line 428 now names the exception.
6. Line 54 gave an unlabelled "battery-level $\rho_g$ recomputes to 0.734" inside a paragraph about accuracy traits whose table carries both scales. `r6_record_check_c.json` gives 0.7336647 for accuracy and 0.9613054 for margins. Both are now reported with their scale.
Build exit 0 twice, `main_text_pages` 9, `SHORT SENTENCES []`, snapshot 6 checksums OK.

| kernel | question | status |
|---|---|---|
| snap-r6-precision-d | margin-like cell at 40,000 replicates, seed 20260925, offset 200, scoring `wild_rademacher` and `wild_cr3`, to break the 0.9404 against 0.9508 tie between the two 10,000-replicate runs | pushed 2026-09-15 22:12 EDT |

### 2026-09-15 21:55 EDT — snap-r6-perm-seeds: the record's permutation coverage reproduces
Ten repeats of the seed-label permutation at the record's own 1,000 permutations, each on a separate stream, scoring the wild interval and the cluster-robust $t$.
Margin wild coverage runs 0.915 to 0.942 with mean 0.9313 and SD 0.0094, and the recorded 0.925 falls inside.
Accuracy wild runs 0.925 to 0.959 with mean 0.9439, recorded 0.954 inside.
Cluster-robust margin runs 0.851 to 0.906 with mean 0.8857, recorded 0.881 inside; accuracy runs 0.915 to 0.947 with mean 0.9313, recorded 0.941 inside.
All four recorded figures land inside their range, so the earlier two-standard-error gap against our 2,000-permutation run was seed noise at the record's replicate count, not a procedural difference. The twelfth and last figure credited to the original record therefore reproduces.
The ten runs put margin coverage 0.019 below nominal, which matches the 0.933 to 0.953 the twelve-population simulation gives, so the permutation on the real data and the simulation now agree on the size of the shortfall.
Paper updated in three places: `main.tex` line 92, `appendix_a.tex` line 90, `appendices_bcd.tex` line 465. The claim that our recomputation sits above the record, and the appendix A claim that the record reported a larger shortfall than we found, are both removed. Build exit 0 twice, `main_text_pages` 9, `SHORT SENTENCES []`, snapshot 6 checksums OK.

| kernel | question | status |
|---|---|---|
| snap-r6-calibrated | Beran prepivoting of the shipped wild interval on the observed data, 1,999 outer and 1,999 inner draws, seed 20260926, for the full battery and the BoolQ-removed battery on both scales, to report what the interval becomes once its own measured miscalibration is corrected | pushed 2026-09-15 22:22 EDT |

### 2026-09-15 22:02 EDT — snap-r6-calibrated: prepivoting moves no conclusion
Beran prepivoting of the shipped wild interval on the observed data, 1,999 outer resamples each carrying a 1,999-draw inner bootstrap, seed 20260926, with all 1,999 prepivoted values defined in every battery.
Full margin calibrates to 1.1509 to 1.3278 against the shipped 1.1429 to 1.3377, which is 0.018 narrower and still excludes one.
Full accuracy calibrates to 0.9939 to 1.1611 and still includes one. BoolQ-removed margin calibrates to 1.6894 to 1.8776, and BoolQ-removed accuracy widens to 1.3963 to 1.7044 and stays above one.
Implied coverage of the nominal levels runs 0.925, 0.937, 0.946 and 0.977 across the four batteries, so the full-battery margin interval reads as conservative under prepivoting while the simulation and the permutation both put the same interval below nominal.
That disagreement is expected and is recorded as such. Prepivoting estimates coverage inside the wild resampling model the interval already assumes, so it cannot see a violation the bootstrap shares with the estimator, and the paper continues to weigh the simulated coverage.
Added as a new paragraph in `deliverables/appendices_bcd.tex` after the constructions paragraph. Build exit 0 twice, `main_text_pages` 9, `SHORT SENTENCES []`, snapshot 6 checksums OK.

| kernel | question | status |
|---|---|---|
| snap-r6-prepivot-sim | does Beran prepivoting repair the coverage shortfall in simulation, scored on the four cells that carry it with 1,000 replicates, seed 20260927, offset 300, against the shipped 4,999-draw wild interval and an uncalibrated 399-draw one, to test the appendix's claim that prepivoting is blind to a violation the bootstrap shares with the estimator | pushed 2026-09-15 22:06 EDT |

### 2026-09-15 22:08 EDT — snap-r6-itemshare-b: the item-sharing tail share at 2,000 permutations
Within-group permutation of run-specific item deviations, 2,000 permutations, seed 20260924, with the inner split loop removed so the tail share carries the precision.
Margin permutation mean 1.24280 with SD 0.00088, and 0.087 of permutations reach or exceed the observed 1.24395, against 0.075 at 200 permutations. The Monte Carlo standard error on that share is 0.006, so the earlier figure sat inside noise and the direction doesn't change.
Accuracy permutation mean 1.07633 with SD 0.00534, and 0.352 reach or exceed the observed 1.07837, against 0.34 at 200.
The observed excess over the permutation mean stays at 0.0012 on margins and 0.0020 on accuracy, against excesses over one of 0.244 and 0.078, so item-level sharing still accounts for about 0.001 and 0.002 of them.
Updated `main.tex` line 140 and `appendices_bcd.tex` line 436 to the 2,000-permutation figures, and rewrote the split-spread sentence so the real and permuted standard deviations can't be confused now that the permutation SD also rounds to 0.0009. Build exit 0 twice, `main_text_pages` 9, `SHORT SENTENCES []`, snapshot 6 checksums OK.

| kernel | question | status |
|---|---|---|
| snap-r6-variants-c | the shipped Rademacher interval against the residual-scaled one on all twelve cells at 10,000 replicates, seed 20260928, offset 400, so the paper quotes one precision on both sides of that comparison instead of 4,000 against 10,000 | pushed 2026-09-15 22:12 EDT |

### 2026-09-15 22:37 EDT — snap-r6-precision-d settles the margin-like cell
40,000 replicates on the one cell whose two 10,000-replicate runs disagreed, seed 20260925, offset 200, with the reimplemented wild interval matching the shipped estimator to 0.0 on the first replicate and no undefined replicate.
Wild Rademacher covers 0.946575 with a Monte Carlo standard error of 0.0011, and the residual-scaled version covers 0.955275, an advantage of 0.0087 with median widths of 0.2414 against 0.2523.
The earlier 0.9404 sits 2.8 standard errors below this value and 0.9508 sits 1.9 above, so both were tails of the same distribution and the cell is dataset variation rather than an implementation difference.
Pooling all three runs over 60,000 replicates gives 0.9463 with a standard error of 0.00093, which agrees with the two-run pooled 0.946 the appendix already reported.
`appendices_bcd.tex` now reports the third run and the pooled figure, and it no longer describes the cell as unresolved. Build exit 0 twice, `main_text_pages` 9, `SHORT SENTENCES []`, snapshot 6 checksums OK.

| kernel | question | status |
|---|---|---|
| snap-r6-leave-one-out | do the three reported conclusions survive every deletion the design admits, recomputing the shipped wild interval after dropping each of the 25 recipes, each of the five size bands, each of the ten benchmarks, and all 300 recipe pairs for the full-battery margin | pushed 2026-09-15 22:45 EDT |

### 2026-09-15 22:41 EDT — snap-r6-leave-one-out: the margin conclusion holds, the accuracy one is fragile upward
Shipped wild interval at 4,999 draws recomputed after every deletion the design admits, which is each of the 25 recipes, each of the five size bands, each of the ten benchmarks, and all 300 recipe pairs for the full-battery margin. Wall time 37 s, every interval defined.
Margin, full battery: excludes one in all 25 single-recipe deletions, all 300 pairs, and all ten benchmark deletions, with the pair estimates spanning 1.1773 to 1.2629 and the nearest endpoint 0.0369 above one.
The single exception is the 530M band, whose removal gives 1.1782 with interval 0.9930 to 1.3382, so the lower endpoint falls 0.0070 below one on the remaining 100 configurations.
Accuracy, full battery: the interval stops including one under recipes 3, 4, 18, 20 and 21 (`dclm-baseline`, `dclm-baseline-25p-dolma1.7-75p`, `dolma1.7-no-reddit`, `falcon-with-cc`, `falcon-with-cc-top-10p`), under removal of the 750M band, and under removal of WinoGrande and of BoolQ.
All eight exceptions put the whole interval above one, seven with lower endpoints between 1.0022 and 1.0248 and the eighth being the BoolQ removal already reported at 1.4119, so every deletion that moves the accuracy conclusion moves it towards more inflation.
Both batteries without BoolQ stay above one under every recipe and size deletion.
Added a paragraph to `appendices_bcd.tex` after the subset enumeration and one sentence to `main.tex` line 30. Build exit 0 twice, `main_text_pages` 9, `SHORT SENTENCES []`, snapshot 6 checksums OK.

| kernel | question | status |
|---|---|---|
| snap-r6-twoway | is recipe clustering wide enough, given that the margin conclusion depends on the 530M band, computing the Cameron Gelbach Miller two-way variance over recipe and size band beside each one-way version on all four batteries | pushed 2026-09-15 22:52 EDT |

### 2026-09-15 22:45 EDT — snap-r6-twoway: size-band clustering holds, the two-way correction doesn't estimate
Standard errors of the squared ratio on the full battery: recipe 0.11198, size band 0.17522, intersection 0.23308.
Clustering on the five size bands alone, with four degrees of freedom and a critical value of 2.776, gives a margin interval of 1.0300 to 1.4262 that still excludes one, and an accuracy interval of 0.9607 to 1.1844 that still includes one, so both full-battery conclusions survive clustering on the dimension the leave-one-out flagged.
The Cameron Gelbach Miller two-way variance is negative on the full-battery margin, because the intersection component exceeds the sum of the recipe and size components, so no two-way interval exists there.
On full-battery accuracy the same subtraction returns a two-way standard error of 0.04004 against 0.08131 from recipes, a ratio of 0.492, whose interval of 1.0255 to 1.1287 would exclude one. Reporting that as a finding would be cherry-picking an estimator that just failed on the neighbouring cell, so the paper says we don't believe it and doesn't report it as a result.
The estimator behaves without BoolQ, widening the recipe standard error by 1.294 on margins and 1.403 on accuracy, with intervals of 1.6518 to 1.9101 and 1.2755 to 1.7963 that both stay above one.
Added a paragraph to `appendices_bcd.tex` after the leave-one-out paragraph. Build exit 0 twice, `main_text_pages` 9, `SHORT SENTENCES []`, snapshot 6 checksums OK.

| kernel | question | status |
|---|---|---|
| snap-r6-size-shared | how far a recipe-clustered interval undercovers when the shared run effect follows the size band instead, sweeping that effect's share over 0, 0.25, 0.5 and 0.75 with 2,000 replicates, seed 20260929, offset 500, scoring the wild interval and the cluster-robust t on each clustering | pushed 2026-09-15 22:58 EDT |

### 2026-09-15 22:48 EDT — snap-r6-matched-variants: the residual-scaled construction holds in the fitted scenarios
Both fitted scenarios rerun with 2,000 replicates, seed 20260923, adding `wild_cr3` and `cluster_t_cr1` to the four procedures already scored. Wall time 6,165 s.
Scenario one (per-band PSD covariance): margin wild 0.9460, cr3 0.9595, cluster-robust t 0.9345, its CR1 correction 0.9390, centred 0.9515, restricted 0.9685. Accuracy 0.9445, 0.9545, 0.9390, 0.9415, 0.9470, 0.9630.
Scenario two (also scaling recipe deviations): margin wild 0.9420, cr3 0.9640, cluster-robust t 0.9035, CR1 0.9090, centred 0.9655, restricted 0.9780. Accuracy 0.9420, 0.9580, 0.9200, 0.9260, 0.9565, 0.9670.
Every figure the appendix already reported for these scenarios reproduces inside two Monte Carlo standard errors, with the largest gap being the centred inversion at 0.9655 against the recorded 0.977.
The residual-scaled construction beats the shipped one by 0.0135 and 0.0220 on margins, so the alternative the generic generator preferred also wins under a description fitted to these data.
The G/(G-1) correction doesn't rescue the cluster-robust t, reaching 0.9390 and 0.9090 on margins against the uncorrected 0.9345 and 0.9035.
Three sentences added to the fitted-scenarios paragraph in `appendices_bcd.tex`. Build exit 0 twice, `main_text_pages` 9, `SHORT SENTENCES []`, snapshot 6 checksums OK.

| kernel | question | status |
|---|---|---|
| snap-r6-deletion-null | is five flips out of 25 recipe deletions more than chance, simulating a true ratio of exactly one and the observed 1.078 with 2,000 replicates each, seed 20260930, offset 600, running the same deletion sweep with recipe clustering held fixed, to check the claim just added to `main.tex` line 30 | pushed 2026-09-15 23:02 EDT |

### 2026-09-15 22:55 EDT — snap-r6-prepivot-sim confirms prepivoting is blind to this shortfall
Beran prepivoting run inside the four simulated cells that carry the coverage shortfall, 1,000 replicates each, 399 outer and 399 inner draws, seed 20260927, offset 300. The reimplemented wild interval matched the shipped estimator to 0.0 in every cell and no replicate was undefined. Wall time 3,144 s.
Prepivoted coverage is 0.924, 0.933, 0.931 and 0.944 for the cross-half, dominant-recipe, margin-like and accuracy-like cells.
The shipped interval on the same replicates covers 0.938, 0.931, 0.936 and 0.953, and an uncalibrated interval at the same 399 draws covers 0.936, 0.928, 0.938 and 0.949, so the draw count isn't responsible.
The calibration therefore tracks the interval it corrects and repairs none of the shortfall, reaching nominal in no cell. The appendix's explanation for why prepivoting called the observed full-battery margin interval conservative is now OBSERVED rather than asserted.
Four sentences added to the prepivoting paragraph in `appendices_bcd.tex`. Build exit 0 twice, `main_text_pages` 9, `SHORT SENTENCES []`, snapshot 6 checksums OK.

### 2026-09-15 22:56 EDT — snap-r6-size-shared: recipe clustering fails if the run effect follows the size band
Size-band shared run effect added to the margin-like population, built like the existing recipe-shared effect so the true ratio stays at 1.2446, swept over shares 0, 0.25, 0.5 and 0.75 with 2,000 replicates each, seed 20260929, offset 500. The reimplementation matched the shipped estimator to 1.1e-16 or exactly. Wall time 466 s.
Recipe-clustered wild coverage: 0.9445, 0.8095, 0.5785, 0.3475. Its median width shrinks from 0.2424 to 0.1888 across the sweep, because the recipe clusters never see the added variance.
Size-clustered wild coverage: 0.9525, 0.9120, 0.7560, 0.5115, with undefined shares rising to 0.034, 0.178 and 0.417. The size-clustered cluster-robust t holds better at 0.9490, 0.9370, 0.9040 and 0.7575.
This matters because the release shares one seed label across every recipe inside a size band, using seeds 2, 14 and 15 below 1B and 2, 4 and 5 at 1B, so the violation is physically plausible rather than hypothetical.
The saving result is that the size-clustered interval on the observed data, from snap-r6-twoway, gives 1.0300 to 1.4262 on margins and 0.9607 to 1.1844 on accuracy, so both reported conclusions survive the clustering that is robust to this violation even though the reported widths do not.
Written to `appendices_bcd.tex` as a new paragraph after the two-way paragraph, and to the `main.tex` limitation paragraph. Build exit 0 twice, `main_text_pages` 9, `SHORT SENTENCES []`, snapshot 6 checksums OK.

| kernel | question | status |
|---|---|---|
| snap-r6-band-share | is there a run effect shared across the 25 recipes inside a size band, testing the weighted cross-half covariance of batch-aligned band-mean deviations against 2,000 within-configuration run permutations, seed 20260931, which is the exact null because it preserves every configuration's own deviations and destroys only the alignment across recipes | pushed 2026-09-15 23:08 EDT |

| kernel | question | status |
|---|---|---|
| snap-r6-recipe-share | is the sharing the shipped clustering assumes present at all, running the band-share statistic with the grouping swapped to recipes, seed 20260932, so the paper can say whether recipe clustering corrects something real or is a precaution | pushed 2026-09-15 23:11 EDT |

### 2026-09-15 23:01 EDT — snap-r6-band-share and snap-r6-recipe-share: the dangerous violation is ruled out
Both runs align each configuration's runs by batch slot, average the run deviations over a group, and take the weighted cross-half covariance of the group means, against 2,000 within-configuration run permutations. That null preserves every configuration's own deviations and destroys only the alignment across the group, so it is exact for this question. Wall times 26 s and 22 s.
The permutation mean matches the independence prediction `total / n^2` to within 1.2 percent in all eight cells, which validates reading the statistic as a share of the seed covariance.
Band sharing, 25 configurations per group: the observed statistic sits inside the null everywhere, with tails 0.283, 0.744, 0.132 and 0.365. The implied share is 0.008 on full-battery margins with a standard error of 0.018 and a 95 percent upper bound of 0.044, and 0.020 without BoolQ with an upper bound of 0.053.
Recipe sharing, 5 configurations per group: also inside the null everywhere, with tails 0.556, 0.116, 0.158 and 0.086, but the standard error is 0.067 on full-battery margins, so it bounds recipe sharing only below 0.114.
This closes the limitation opened by `snap-r6-size-shared`, whose sweep showed coverage falling to 0.810 at a band-shared share of 0.25. The data bounds that share at 0.044, which is more than five times below the smallest share simulated. The clustering the paper uses is a precaution these data can't confirm, and the violation that would break it is ruled out.
Written to the size-band paragraph in `appendices_bcd.tex` and to the `main.tex` limitation. Build exit 0 twice, `main_text_pages` 9, `SHORT SENTENCES []`, snapshot 6 checksums OK.

| kernel | question | status |
|---|---|---|
| snap-r6-size-shared-low | what the recipe-clustered interval covers inside the band-shared region the data allows, at shares 0.02, 0.05 and 0.10 with 4,000 replicates each, seed 20260933, offset 700, so the paper can quote coverage at the bound instead of only excluding the damaging region | pushed 2026-09-15 23:16 EDT |
| snap-r6-joint-share | what the recipe-clustered interval covers when recipe sharing sits at its 0.114 bound and band sharing at its 0.044 bound together, plus each channel alone and twice the joint corner, 4,000 replicates per cell, seed 20260934, offset 800 | pushed 2026-09-15 23:30 EDT |
| snap-r6-size-shared-low | COMPLETE 2026-09-15 23:09 EDT. Recipe-clustered wild coverage 0.9455, 0.9405 and 0.9285 at band-shared shares of 0.02, 0.05 and 0.10, MC SE about 0.0037, cluster-robust t within 0.001 of the wild figure at each share, shipped-estimator check exactly 0.0 in all three cells. Written into the band paragraph of appendices_bcd.tex, replacing the refusal to quote coverage inside the bound, and into the main-text limitation as 0.941 at the 0.044 bound. | done |
| snap-r6-joint-share | COMPLETE 2026-09-15 23:23 EDT. Recipe-clustered wild coverage 0.9493 at recipe share 0.114 alone, 0.9367 at band share 0.044 alone, 0.9490 at both together with median width 0.247, and 0.9273 at twice both. Cluster-robust t within 0.001 of wild everywhere, shipped-estimator check exactly 0.0 in all four cells. The two channels partly cancel because recipe sharing widens the clusters the interval sums over. Written into the band paragraph of appendices_bcd.tex; main.tex now quotes the exact-bound 0.937 instead of the 0.05-cell 0.941. | done |
| snap-r6-weights | whether either conclusion depends on the flat one-tenth benchmark weighting, by rebuilding T and U under arbitrary weights from the shipped contrast projections and drawing 2,000 flat-Dirichlet and 2,000 near-flat weightings per battery, each with its own recipe-clustered wild interval at 4,999 draws, seed 20260935 | pushed 2026-09-15 23:38 EDT |
| audit correction | 2026-09-15 23:50 EDT. An independent numeric audit of main.tex found that the criterion sentence at line 199 said both variance estimates are positive for 1,445 margin and 1,222 accuracy pairs, when those counts are the pairs whose paired difference carries a positive variance. Verified against snap-r8-prediction C08 result.json, where both_positive is 1,428 and 1,010. The sentence now states the real criterion, and the stricter both-positive subset of 1,379 and 919 pairs is reported at medians 1.027 and 1.079. The main text also now carries the 530M deletion interval 0.993 to 1.338, which was only in the appendix. Four sentences elsewhere were compressed to hold nine pages. | done |
| snap-r6-twoway-boot | whether a two-way wild bootstrap, drawing one Rademacher per clustering dimension and multiplying, repairs the negative CGM variance on the observed cells and repairs coverage where band sharing breaks the recipe-clustered interval, 9,999 draws observed and two simulated cells at 1,000 replicates, seed 20260936, offset 900 | pushed 2026-09-15 23:45 EDT |
| snap-r6-weights | COMPLETE 2026-09-15 23:29 EDT. Flat-weight rebuild matches the shipped T and U to 1.08e-19. Over 2,000 flat-Dirichlet weightings, 0.817 of margin intervals exclude one with the estimate from 0.976 to 1.991 and a 5th percentile of 0.999; over 2,000 near-flat weightings all margin intervals exclude one with a lowest estimate of 1.059. Accuracy excludes one in 0.386 and 0.402 of weightings while the flat weighting gives 0.993 to 1.157 and does not, so the shipped weighting is the conservative one there. Written into the composition appendix and summarised in main.tex line 142. | done |
| snap-r6-twoway-boot | COMPLETE 2026-09-15 23:31 EDT. The two-way wild bootstrap at 9,999 draws returns 1.0514 to 1.4105 on full-battery margins, the cell where the analytic CGM variance went negative, and it still excludes one. On full-battery accuracy it returns 0.9732 to 1.1748, wider than the recipe interval rather than narrower, which confirms the analytic accuracy figure was an artefact. It repairs no coverage, since at band share 0.25 it covers 0.824 against 0.839 for the recipe-clustered interval and at the joint corner 0.932 against 0.936, both over 1,000 replicates. Written into the two-way paragraph of appendices_bcd.tex. | done |
| mistake | 2026-09-15 23:57 EDT. I wrote a new cross-half kernel into research/kaggle/snap-r6-crosshalf without checking that the name was taken, which overwrote the version 1 script and pushed a duplicate as version 2 of the same Kaggle kernel. The question had already been answered at 20:22 EDT, so the duplicate adds nothing. Version 1's design, log and results all survive in research/outputs/snap-r6-crosshalf, and Kaggle holds version 1 in its own version history, but the local script is gone and the CLI cannot pull a specific version. Recorded in research/kaggle/snap-r6-crosshalf/SOURCE_LOST.txt. The duplicate now lives in research/kaggle/snap-r6-crosshalf-b and its output will not be downloaded over version 1's. No paper text changed. | recorded |
| snap-r6-leave-one-run | whether either conclusion rests on one of the three runs per configuration, by re-estimating both scales on each of the three surviving run pairs with a recipe-clustered wild interval at 4,999 draws, and reporting the width so the loss of one contrast direction is visible. The seed-label deletion is guarded, because pop.batch indexes run slots rather than seed identities | pushed 2026-09-16 00:08 EDT |
| audit correction | 2026-09-16 00:15 EDT. An independent numeric audit of the appendices found six last-place errors in hand-computed derived quantities, all recomputed from snap-r11-removal lambda values with the Lambda-scale wild standard errors 0.045010 and 0.037702 from snap-r19-size-intervals. The accuracy delete-one-trait jackknife scale is 0.455372 not 0.456, the accuracy unscaled dispersion ratio is 4.2438 not 4.25, the nine-removal margin scale is 0.138511 not 0.138, its ratio is 3.0773 not 3.07, and the endpoints without the BoolQ removal are 0.958496 and 0.917767 rather than 0.959 and 0.917. The band-share independence claim moved from 1.2 to 1.3 percent, since the largest deviation is 1.215 percent on without-BoolQ margins. Every figure was recomputed here before the edit rather than taken on the auditor's word, and the three companion figures the auditor called correct do reproduce. | done |
| snap-r6-deletion-null | COMPLETE 2026-09-15 23:46 EDT. Under a true-ratio-one null with accuracy-like noise, 2,000 replicates, conditioning on the 1,900 whose full interval includes one, at least one recipe deletion flips in 0.1158 of replicates and five or more flip in 0.0205. A second null calibrated to the observed 1.078 gives 0.1235 and 0.0317. The observed five recipe flips therefore sit in the upper few percent of chance, so the claim that deletions move the accuracy conclusion towards more inflation survives and now carries its calibration. Written into the leave-one-out paragraph of appendices_bcd.tex and summarised in main.tex line 30. | done |
| snap-r6-crosshalf version 2 | COMPLETE 2026-09-15 23:44 EDT, duplicate of version 1, output kept in research/outputs/snap-r6-crosshalf-b and not used in the paper | superseded |
| snap-r6-deletion-null-margin | whether one band deletion out of five losing the margin exclusion is ordinary under the null, by simulating a margin-like population at rho 0.061 and noise 0.73 with 2,000 replicates, conditioning on replicates whose full interval excludes one, and counting deletions that lose the exclusion for both the 25 recipes and the five bands, seed 20260938 | pushed 2026-09-16 00:31 EDT |
| snap-r6-power | how large a replicate study would need to be for the accuracy conclusion to resolve, by simulating the fitted accuracy cell over 25, 50, 100 and 200 recipes at five bands each, with three runs and with six, 1,000 replicates per cell, plus a margin-like cell at the shipped design as a machinery check, seed 20260939, offset 1100 | pushed 2026-09-16 00:36 EDT |
| snap-r6-variants-c | COMPLETE 2026-09-15 23:55 EDT. All twelve populations at 10,000 replicates, seed 20260928, offset 400. The shipped Rademacher wild interval covers 0.9292 to 0.9504, the residual-scaled version covers 0.9401 to 0.9594, the gain runs from 0.0073 to 0.0143 in every cell, the residual-scaled version reaches 0.95 in ten cells against one, and its median width runs 4.4 to 12.7 percent wider. The appendix comparison now quotes both constructions at 10,000 instead of mixing 4,000 against 10,000. | done |
| audit correction | 2026-09-16 00:45 EDT. main.tex said simulated coverage of the cluster interval stays between 0.933 and 0.953, which matched neither the coverage table at 2,000 replicates, whose wild column runs 0.928 to 0.954, nor any 10,000-replicate run. Replaced with 0.929 to 0.950 across twelve populations at 10,000 replicates each, which is the highest-precision measurement of the full set and is stated with its replicate count. | done |
| monitoring | 2026-09-16 00:16 EDT. Replaced the retry-capable poller with a token-free monitor at scratchpad/monitor_kernel.sh, which polls status every 60 s, downloads output on either outcome, never re-pushes, and appends one row per terminal state to research/kernel_status.tsv with kernel, status, start, end, minutes, output files and the last done or error line. The old poller was launched for snap-r6-leave-one-run with a 300-retry budget and re-pushed the crashing kernel 34 times before I killed every poll_kernel.sh process. | done |
| snap-r6-leave-one-run | FAILED then fixed. ValueError: gainA must be (N, R) = (125, 2), got (125, 3) at population.py:120, because drop_run sliced the phenotype arrays but passed the unsliced gainA and gainB. Version 35 slices all three. Base estimates printed before the crash and match the shipped values exactly, at 1.2439499 with 1.1429 to 1.3377 on margins and 1.0783734 with 0.9926 to 1.1567 on accuracy. | rerunning |
| snap-r6-deletion-null-margin | COMPLETE 2026-09-16 00:15 EDT. Under a margin-like null, 2,000 replicates, conditioning on the 0.9725 whose full interval excludes one, at least one of the five band deletions loses the exclusion in 0.1620 of replicates and at least one of the 25 recipe deletions in 0.0452. The observed 530M failure is therefore ordinary rather than surprising, which the appendix now says. | done |
| snap-r6-power | COMPLETE 2026-09-16 00:15 EDT. Power to exclude one at the fitted accuracy cell is 0.111 at 25 recipes and three runs, 0.165, 0.306 and 0.620 at 50, 100 and 200 recipes, and 0.196, 0.398, 0.704 and 0.947 at the same four with six runs. The margin cell at the shipped design reaches 0.970. Coverage of the truth stays between 0.939 and 0.957 in all nine cells. Written into the leave-one-out appendix and summarised in main.tex line 30. | done |
| snap-r6-leave-one-run | COMPLETE 2026-09-16 00:17 EDT after the gain-slicing fix. Dropping each run in turn leaves margin estimates of 1.2556, 1.2416 and 1.2346, all excluding one, and accuracy estimates of 1.0838, 1.1144 and 1.0301, none excluding one. The margin width grows from 0.1948 to 0.2005, 0.3102 and 0.3559, which is the cost of losing one contrast direction. The seed-label deletion was skipped by its own guard, because pop.batch indexes run slots rather than seed identities. Written into the leave-one-out appendix. | done |
| snap-r9-decision | whether correcting a decision rule for covariance helps once the correction carries the noise of a three-run design, by estimating Lambda from the shipped design in each replicate and applying an uncorrected independence rule, a plug-in rule and an oracle rule to a fresh comparison of two model averages, at true gaps of zero, half and one aggregate standard deviation, for one and three runs per model, 4,000 replicates per cell, seed 20260940 | pushed 2026-09-16 00:47 EDT |
| snap-r6-deletion-null-a / -b / -margin-b | precision repeats of the three deletion null calibrations at 8,000 replicates each with fresh seeds 20260941, 20260942 and 20260943, which halves the Monte Carlo standard error on the tail shares the paper now quotes. Generator, sweep, clustering and conditioning are unchanged, so the runs can be pooled with the first pass if they agree. A build slip cut the write_text call off all three output lines and was caught before the push. | pushed 2026-09-16 00:52 EDT |
| snap-r9-decision | COMPLETE 2026-09-16 00:34 EDT. On a margin-like population the uncorrected independence rule declares a difference in 0.1133 of replicates at a true gap of zero, against a nominal 0.05, while the plug-in rule reaches 0.0508 and the oracle 0.0503; at three runs per model they give 0.1145, 0.0527 and 0.0512. The estimate's own standard deviation is 0.0592 and costs under 0.001 of size. On an accuracy-like population, where the estimate's standard deviation is 0.0988, the three rules give 0.0673, 0.0520 and 0.0470 at one run and 0.0643, 0.0532 and 0.0500 at three. Written into the flip-probability appendix and summarised in the practice recommendation of main.tex. | done |
| snap-r9-runs | how many replicate runs a comparison needs and whether extra runs repair the size error, by sweeping one to ten runs per model at true gaps of zero, a quarter, a half and one aggregate standard deviation on both populations, 4,000 replicates per combination, naming the smallest run count whose corrected rule reaches four fifths, seed 20260944, offset 1400 | pushed 2026-09-16 00:59 EDT |

### 2026-09-16 10:34 to 11:50 EDT — the overnight batch, read and written up
The session went idle at roughly 01:00 EDT because a turn only advances when the harness invokes the model. The five kernels kept running unattended and their monitors recorded every outcome, but the shell monitors died with the session, so no results were written into the paper until this turn.

| kernel | outcome | disposition |
|---|---|---|
| snap-r6-deletion-null-a | COMPLETE 01:36 EDT, 75 min, 8,000 replicates, seed 20260941 | pooled |
| snap-r6-deletion-null-margin-b | COMPLETE 02:12 EDT, 111 min, 8,000 replicates, seed 20260943 | pooled |
| snap-r6-deletion-null-b | COMPLETE 02:17 EDT, 116 min, 8,000 replicates, seed 20260942 | pooled |
| snap-r9-runs | COMPLETE 00:37 EDT, 1 min, 4,000 replicates per combination, seed 20260944 | written |
| snap-r7-pilot | CANCEL 04:52 EDT after 275 min, no JSON written | partially salvaged from the log |

Pooling the three precision repeats with the first pass. All nine tail shares reproduce across the two passes, with the largest standardised difference at 2.25 and the rest under 1.8, so the runs pool. The true-one accuracy null now rests on 9,477 kept replicates, giving 0.105 for at least one recipe flip and 0.0176 for five or more, against 0.1158 and 0.0205 from the first pass alone. The null calibrated to the observed 1.078 rests on 8,964 kept replicates at 0.127 and 0.032. The margin null rests on 9,697 kept replicates at 0.164 for at least one band deletion losing the exclusion and 0.041 for at least one recipe deletion. Written into the leave-one-out paragraph of `appendices_bcd.tex`, and `main.tex` line 30 moved from 0.021 to 0.018.

snap-r9-runs. Sweeping one to ten runs per model, the uncorrected independence rule declares at a zero margin gap in 0.112 to 0.122 of replicates at every run count, and the plug-in rule stays between 0.047 and 0.056, with the accuracy population at 0.061 to 0.078 against 0.047 to 0.063. Neither sequence trends with the run budget, which confirms the analytic argument that both standard errors fall as the square root of the run count so their ratio is invariant. Power at a one standard deviation gap runs from 0.100 at one run to 0.614 at ten on margins and 0.111 to 0.615 on accuracy, and no swept run count reaches four fifths at any nonzero gap. Written into the flip-probability appendix.

snap-r7-pilot. The kernel printed real results before it stalled, and the log survives even though no JSON was written. At dclm-baseline 150M the branch named default reproduced the released seed-2 margins at a correlation of 0.99999998 with a median absolute difference of 1.6e-05, small-aux-2 matched seed-14 at 0.99999998, and small-aux-3 matched seed-15 at 0.99999998, while every mismatched pair sat between 0.836 and 0.874. At 1B the default branch matched seed-2 at 0.99999996. The stall came four hours into a 1B auxiliary branch download with no output. Not yet written into the paper, because `snap-r7-verify` is testing whether the same reproduction holds across recipes.

### 2026-09-16 11:20 EDT — a presentation defect found and fixed, not a numeric error
`main.tex` line 92 says a 10,000-replicate repeat of all twelve populations puts wild coverage between 0.933 and 0.953, and line 211 said the same quantity stays between 0.929 and 0.950. Both figures are correct and they describe different runs, the first from `snap-r6-precision` plus `snap-r6-precision-b` at seed 20260917 whose twelve cells span 0.9334 to 0.9527, and the second from `snap-r6-variants-c` at seed 20260928 whose twelve cells span 0.9292 to 0.9504. The 2026-09-16 00:45 EDT audit correction assumed line 92 was stale when it was not, and so left the paper quoting two ranges for one described quantity. Both sentences now name their sweep, line 211 reports the union of the two at 0.929 to 0.953, and the appendix carries a matching cross-reference. No number changed.

| kernel | question | status |
|---|---|---|
| snap-r9-runs-b | where the corrected rule actually reaches four fifths, extending the sweep to 12, 16, 20, 24, 32, 40, 60 and 80 runs per model at the same four gaps on both populations, 8,000 replicates per combination, beside the closed-form oracle run count, seed 20260945, offset 1600 | pushed 2026-09-16 11:45 EDT |
| snap-r9-transfer | what the plug-in correction costs when it is estimated on one battery and carried to a comparison with a different true ratio, sweeping the target from 1.00 to 1.60 against a correction fixed at the margin design, with matched and oracle rules beside it, 8,000 replicates per target, seed 20260946, offset 1800 | pushed 2026-09-16 11:45 EDT |
| snap-r6-joint-share-c | the two coverage numbers the main text quotes at the band bound and the joint corner, repeated at 12,000 replicates with a fresh seed, which halves the Monte Carlo standard error to about 0.0021, seed 20260947, offset 2100 | pushed 2026-09-16 11:45 EDT |
| snap-r6-twoway-cov | whether the two-way bootstrap really covers worse than recipe clustering, at 8,000 replicates and 2,999 draws against the earlier 1,000 and 999, reporting the paired per-replicate difference rather than two separate coverages, seed 20260948, offset 2200 | pushed 2026-09-16 11:45 EDT |
| snap-r7-verify | whether the released per-item scores reproduce from the public checkpoints across recipes rather than only dclm-baseline, rescoring the first 150 ARC-Easy documents at 150M for three seed branches of the first six recipes present, with the scoring and checkpoint mapping spliced verbatim from the pilot, a per-branch JSON flush, a per-repository exception guard and a wall-clock budget | pushed 2026-09-16 11:45 EDT |

### 2026-09-16 11:50 to 12:30 EDT — four of the five replacements landed and are written in

| kernel | result | disposition |
|---|---|---|
| snap-r9-runs-b | COMPLETE 10:45 EDT. Across 12, 16, 20, 24, 32, 40, 60 and 80 runs per model the uncorrected rule declares at a zero margin gap in 0.108 to 0.119 of replicates and the corrected rule in 0.047 to 0.053, so the size error is invariant to the run budget out to 80 runs. Four fifths at a one standard deviation gap arrives at 16 runs on margins, matching the closed form exactly, and at 20 on accuracy where the oracle also misses four fifths at 16 by 0.002. A half standard deviation gap needs 63 runs by the closed form, and 80 reaches 0.881. A quarter needs 252. | written into the flip-probability appendix |
| snap-r9-transfer | COMPLETE 10:47 EDT. The correction estimated on the margin design, true ratio 1.245, holds nominal size only where the target agrees, at 0.053. Below it turns conservative, at 0.015 for a target of 1.00 and 0.028 at 1.10, and power at a one standard deviation gap falls to 0.043 against 0.114 for a matched rule. Above it under-corrects, at 0.080, 0.101 and 0.123 for targets of 1.40, 1.50 and 1.60 against 0.159, 0.188 and 0.216 uncorrected. The matched rule holds 0.048 to 0.053 throughout. | written into the appendix and into the main-text recommendation, which now says the ratio has to be measured locally |
| snap-r6-twoway-cov | COMPLETE 11:48 EDT, 204 s. At 8,000 replicates and 2,999 draws the two-way bootstrap covers 0.823 against 0.833 at a band-shared quarter and 0.932 against 0.940 at the joint corner. Scoring both intervals on the same replicate gives paired differences of $-0.010$ with standard error 0.0024 and $-0.008$ with standard error 0.0019, both more than four standard errors below zero. The four observed cells reproduce the earlier 9,999-draw figures to every reported digit. | written, and the appendix now states a measured deficit instead of an unresolved one |
| snap-r7-verify | COMPLETE 11:52 EDT, 373 s. Twelve of twelve loadable branches reproduce the released margins. The default branch matches released seed 2, small-aux-2 matches seed 14 and small-aux-3 matches seed 15, in c4, dclm-baseline and the quarter and half dclm-baseline blends with dolma 1.7. Eleven correlations are 0.99999997 or better with median absolute differences of 1.3e-05 to 1.9e-05, the twelfth is c4 under default at 0.99996, and the best wrong seed never exceeds 0.897. Two recipes did not resolve, and the error is a missing identifier rather than a refused request, so the count of 85 repositories returning 401 needs care. | written into the seed-label paragraph |

Page budget. The main-text recommendation grew by one sentence and pushed the manuscript to ten pages. Seven compressions recovered nothing, because each one reflowed inside its own paragraph, and the page came back only when the closing sentence of the recommendation was deleted. That sentence said recommending independence elsewhere would need replicate populations beyond DataDecide, which the ethics statement already says. `main_text_pages` 9, `SHORT SENTENCES []`, `prose_colons_semicolons_dashes` 0, build exit 0 twice, snapshot 6 checksums OK.

A grammar defect found in passing. The coverage reconciliation earlier today produced "across the two high-precision sweeps across twelve populations", with a doubled preposition. Fixed to "across two sweeps of twelve populations".

| kernel | question | status |
|---|---|---|
| snap-r7-verify-b | whether the seed mapping holds across the whole battery rather than a convenience sample, taking the remaining nineteen recipes at 150M with the scoring and checkpoint mapping unchanged, and recording exactly which repositories resolve | pushed 2026-09-16 12:15 EDT |
| snap-r9-runs-quarter | the quarter standard deviation gap, which the closed form puts at 252 runs per model and no sweep has reached, at 100, 140, 180, 220, 252, 300 and 360 runs on both populations with 8,000 replicates per combination, seed 20260949, offset 2400 | pushed 2026-09-16 12:15 EDT |
| snap-r9-transfer-b | the same transfer sweep with the accuracy design supplying the correction, whose true ratio is 1.078 and whose estimate is noisier, so the paper can say whether the shape of the transfer penalty depends on which design produced the number, seed 20260950, offset 2600 | pushed 2026-09-16 12:15 EDT |
| snap-r6-share-power | whether the permutation test that bounds band sharing can detect band sharing at all, sweeping true shares of 0, 0.02, 0.044, 0.10, 0.25 and 0.50 with 500 replicates and 999 permutations each, and reporting the rejection rate, the bias of the plug-in share estimate and the coverage of the interval the paper quotes. A test without power would make the 0.044 bound an artefact of the procedure rather than a fact about these runs, and nothing we have run so far measures that | pushed 2026-09-16 12:28 EDT |

### 2026-09-16 12:45 to 13:10 EDT — two more landed, five running again

| kernel | result | disposition |
|---|---|---|
| snap-r9-runs-quarter | COMPLETE 12:34 EDT. Four fifths at a quarter standard deviation gap arrives at exactly the 252 runs per model the closed form predicts on margins, and at 300 on accuracy where the oracle itself reaches only 0.7995 at 252. The uncorrected rule declares at a zero margin gap in 0.112 to 0.122 of replicates at all seven counts from 100 to 360, so the size error survives every run budget from one to 360. | written into the flip-probability appendix |
| snap-r9-transfer-b | COMPLETE 12:47 EDT. With the accuracy design as the source, true ratio 1.078 and estimate standard deviation 0.098, the transferred rule holds 0.056 at a target of 1.10 and recovers almost nothing above, at 0.168 for a target of 1.50 against 0.199 uncorrected and 0.054 matched. The penalty tracks the distance between the two ratios rather than the source design, and reading both sweeps together the transferred rule stays inside 0.04 to 0.06 only while the target sits within about 0.05 of the source. | written into the transfer passage |

A check that needed no new compute. The accuracy conclusion rests on a lower endpoint of 0.9933, and a reviewer will ask whether bootstrap noise decides its position relative to one. `snap-r6-observed-intervals` already carried the answer at twenty bootstrap seeds, where that endpoint has a standard deviation of 0.0015 and therefore sits more than four of its own bootstrap standard deviations below one. The appendix said only that no endpoint varies by more than 0.0023, which is true and does not answer the question a reader is actually asking, so it now states the accuracy endpoint directly. Before spending a kernel on the question I checked whether an existing output answered it, and it did.

| kernel | question | status |
|---|---|---|
| snap-r6-size-shared-b | the 0.810 and 0.579 coverage figures that carry the main-text limitation, repeated at 10,000 replicates against the original 2,000, which brings the Monte Carlo standard error from about 0.009 to about 0.004, seed 20260952, offset 3000. The three-quarter cell is dropped because 0.348 is far enough from nominal that precision changes no decision | pushed 2026-09-16 12:55 EDT |
| snap-r6-boolq-sweep | whether the BoolQ result is a smooth function of weight or an artefact of removal, sweeping the BoolQ weight from its flat 0.10 to 0 in sixteen steps with the other nine benchmarks renormalised equally, computing the estimate and a recipe-clustered wild interval at 4,999 draws at each step on both scales, and naming the weight at which the accuracy interval stops including one, seed 20260953. The paper currently reports the two endpoints of a dial it never turned | pushed 2026-09-16 13:05 EDT |

### 2026-09-16 13:30 EDT — five results read, four corrections to the manuscript, five kernels running again

Every number below was checked against the kernel's own JSON before it was allowed near a tex file, and two of the four corrections exist because that check failed something the manuscript already said.

| kernel | result | disposition |
|---|---|---|
| snap-r7-availability | COMPLETE 11:31 EDT. The census of all 125 repositories returns 80 HTTP 401s, five names carrying dolma1.6 with two plus signs that the hub rejects as identifiers, and 40 that resolve anonymously, and the split is identical in each of the five size bands at sixteen, one and eight. Every open repository carries default, small-aux-2 and small-aux-3, and the eight at 1B add large-aux-2 and large-aux-3. | main.tex and the seed-label appendix now carry the census. It replaced my own earlier sentence, which called two Falcon recipes a naming gap when the census shows both answer 401. The loader's message is misleading, because an anonymous client cannot tell a private repository from a missing one, so we record status codes now rather than message text. |
| snap-r7-verify plus snap-r7-verify-b | COMPLETE. Pooling both runs gives 24 branch cells over eight recipes at 150M, and the default branch matches released seed 2, small-aux-2 matches seed 14 and small-aux-3 matches seed 15 in every one. Twenty cells reach 0.99999997 or better with median absolute differences between 1.0e-05 and 2.0e-05, the weakest is 0.99993525, and the best wrong seed anywhere is 0.8966. | The appendix claimed the four weakest reach 0.99994, which the weakest cell misses. Corrected to 0.99993. I recomputed the whole claim from both JSONs rather than trusting the sentence I had already written. |
| snap-r6-joint-share-c | COMPLETE. At 12,000 replicates the band-shared corner covers 0.9444 and the joint corner covers 0.9428, which is half a Monte Carlo standard error apart. | The first pass at 4,000 replicates separated those two cells by 0.012 and the appendix read that gap as the two sharing channels partly cancelling. The gap is noise and the reading is now deleted. The paragraph reports both passes, the pooled 0.9425 and 0.9444, and says plainly that the earlier reading rested on a difference the first pass was too small to measure. main.tex moves from 0.937 to 0.943. |
| snap-r6-size-shared-b | COMPLETE. At 10,000 replicates with a second seed the recipe-clustered interval covers 0.9431 at a share of zero, 0.8227 at a quarter and 0.5805 at a half. | Pooled with the original 2,000 replicates this gives 0.943, 0.821 and 0.580, so every quotation of 0.810 and 0.579 in both files moves, and the cost of a quarter becomes twelve points of coverage rather than thirteen. The two passes agree to within 0.013 at every cell. |
| snap-r6-share-power | COMPLETE. The batch-slot permutation test rejects 0.044 of the time at one in twenty when the true band share is zero, and its plug-in estimate misses the truth by 0.0009 or less up to a quarter. Power runs 0.218 at 0.02, 0.490 at 0.044, 0.838 at 0.10 and 0.992 at a quarter. The quoted standard error is the null standard deviation, so it reads 0.0216 where the estimate's actual spread is 0.0386 at a share of 0.044 and 0.0942 at a quarter. | Written into the band-share appendix. The bound is trustworthy at the observed 0.008, where the interval covers 0.962, and anti-conservative out at the shares it excludes, so the paper now calls it a limit on what these data resolve rather than a confidence statement. Power at the bound itself is only about half, and the appendix says so. |
| snap-r6-boolq-sweep | COMPLETE 11:31 EDT, version 2 after the three-tuple unpacking error in version 1. Lowering BoolQ's weight from the flat tenth to a twentieth raises margin inflation from 1.2439 to 1.5030 with every interval excluding one, and accuracy from 1.0784 to 1.2311 with the interval ceasing to include one at a weight of 0.0867. | Written into the removal appendix. The accuracy conclusion is smooth in the estimate and sharp in the weighting, since a battery that discounts BoolQ by a seventh reports dependence on accuracy too. |

| kernel | question | status |
|---|---|---|
| snap-r6-corner-remedy | whether scaling the recipe residual by one minus its leverage recovers the coverage the two corner cells lose, at 12,000 replicates, seed 20260954, offset 3200 | running, poller attached |
| snap-r6-recipe-shared-cov | what the size-clustered fallback costs under the sharing channel this design actually bounds, sweeping the recipe share over 0, 0.114, 0.25 and 0.50 at 4,000 replicates, seed 20260955. The paper recommends that fallback and has never tested it against recipe sharing | pushed 2026-09-16 13:22 EDT, poller attached |
| snap-r6-battery-width | whether the interval still covers on batteries of 4, 6, 8, 12 and 20 benchmarks rather than the ten the release ships, at 4,000 replicates, seed 20260956. The recommendation is aimed at readers whose battery is not this one | pushed 2026-09-16 13:22 EDT, poller attached |
| snap-r6-run-count-cov | what a second run costs and a fourth buys, sweeping runs per configuration over 2, 3, 4, 6 and 10 at 4,000 replicates, seed 20260957. Every coverage number in the paper inherits the release's three runs | pushed 2026-09-16 13:22 EDT, poller attached |
| snap-r6-mirror-power | whether the mirror permutation test can see the recipe sharing it bounds at 0.114, sweeping that share over 0, 0.05, 0.114, 0.25 and 0.50, seed 20260958. The corner argument rests on that bound and its standard error is 0.067 against 0.018 for the band version | pushed 2026-09-16 13:22 EDT, poller attached |

The three coverage kernels are byte-identical to snap-r6-size-shared apart from the docstring, the cell list, the seed and the output filename, and snap-r6-mirror-power is snap-r6-share-power with the tiled band effect replaced by a repeated recipe effect and the grouping index changed from modulo to integer division. Splicing rather than retyping is deliberate, because the one function I reconstructed from memory this week was materially wrong.

### 2026-09-16 14:40 EDT — the five-kernel batch landed, all five COMPLETE, all five written in

The polling agents were replaced with detached shell monitors at `research/monitor_kernel.sh`, because agents cost tokens for work that is a status loop. Each monitor polls once a minute, downloads on any terminal state, and appends one row to `research/kernel_status.tsv`.

| kernel | result | disposition |
|---|---|---|
| snap-r6-corner-remedy | COMPLETE in 40 minutes. The cr3 correction, which divides each recipe residual by one minus its leverage, lifts the recipe-clustered interval from 0.942 to 0.9499 at the band corner and from 0.9455 to 0.9543 at the joint corner over 12,000 replicates, widening the median from 0.241 to 0.252. | Written into the corner paragraph. The main text keeps the uncorrected interval because that is what the shipped estimator computes, and the appendix now prices the remedy at four percent of width. |
| snap-r6-mirror-power | COMPLETE in 37 minutes. The mirror permutation test rejects 0.054 at a true recipe share of zero, its estimate is unbiased to within 0.0046 everywhere, and its power runs 0.248 at 0.05, 0.628 at its own 0.114 bound and 0.982 at a quarter. Its quoted standard error of 0.0529 matches the estimate's actual spread of 0.0539 at the null. | Written into the mirror-test passage. The recipe bound the corner argument rests on is a measurement, since a quarter would be caught 98 times in 100. |
| snap-r6-recipe-shared-cov | COMPLETE in 15 minutes. Under recipe sharing the size-clustered wild interval covers 0.953, 0.955, 0.944 and 0.898 at shares 0, 0.114, 0.25 and 0.50, and its cluster-robust counterpart covers 0.947, 0.944, 0.929 and 0.869. The recipe-clustered interval holds 0.942 to 0.947 throughout and the reimplementation reproduces the shipped interval exactly in all four cells. | Written into the fallback passage. The interval the limitation section recommends costs nothing in coverage at the bound this design measures, and it costs width, at a median 0.357 against 0.246. |
| snap-r6-battery-width | COMPLETE in 25 minutes after a version 1 failure. Across batteries of 4, 6, 8, 10, 12 and 20 benchmarks the true ratio climbs from 1.088 to 1.469 and the recipe-clustered interval covers between 0.941 and 0.951, with no undefined replicate in any cell. | Written into the same passage. The recommendation now carries evidence outside the ten benchmarks the release ships. |
| snap-r6-run-count-cov | COMPLETE in 20 minutes after a version 1 failure. From two runs per configuration to ten the recipe-clustered interval covers 0.942 to 0.952 while the median width falls from 0.343 to 0.114, and the size-clustered fallback drops to 0.915 at two runs. | Written in. A reader with two runs should keep the recipe clustering, which the appendix now says. |

Both version 1 failures were the same defect, `KeyError: 'size_shared'` at the report line, because the driver I spliced recorded that one cell key by name and my new cell lists vary other parameters. The line now records whatever the cell varies. Splicing a verified driver still beat retyping it, and the cost was one field.

### 2026-09-16 15:35 EDT — the robustness batch, eight kernels, all COMPLETE, all written in

A shell queue at `research/queue_runner.sh` now keeps five kernels running without an agent. It counts live monitors as occupied slots, pushes the next name from `research/queue.txt` when a slot frees, and idles rather than exits when the queue drains.

| kernel | result | disposition |
|---|---|---|
| snap-r6-observed-truth | COMPLETE. Recovering the ten by ten seed covariance through the shipped estimator alone, from ten single-benchmark restrictions and forty five pairs, reproduces the reported ratios at 1.2439 on margins and 1.0784 on accuracy. The positive semidefinite projection clips one small negative eigenvalue on margins and three on accuracy, giving 1.2438 and 1.0778. Simulating against those measured matrices leaves coverage at 0.947 and 0.945. | The strongest result in the batch, because every other coverage number in the paper assumes compound symmetry and this one shows the assumption was harmless. The independent reconstruction matching the shipped estimate to four decimals is also a check on the estimator itself. |
| snap-r6-detect-power | COMPLETE. At the accuracy battery's noise the interval excludes one in 0.018 of replicates at a true ratio of one, 0.063 at 1.05, 0.135 at 1.10, 0.281 at 1.15, 0.668 at 1.25 and 0.968 at 1.40. | Written into the appendix and into the main-text limitation. The accuracy null result now carries its power, and the paper says the interval bounds a large ratio rather than showing independence. |
| snap-r6-cluster-count | COMPLETE. Sweeping the recipe count over 5, 10, 15, 25, 40 and 60 gives wild coverage of 0.749, 0.946, 0.942, 0.942, 0.946 and 0.946. The exclusion rate at 1.2446 climbs from 0.54 at ten recipes to 0.97 at 25. | The shipped 25 clusters sit above the cliff rather than on it. At five clusters the wild interval does worse than the plain cluster-robust one, 0.749 against 0.910, which inverts the usual advice and the appendix says so. |
| snap-r6-heavy-tails | COMPLETE. A multivariate t leaves coverage at 0.944 with ten degrees of freedom and 0.940 with five, and a t with three takes it to 0.895. Recipe variance scales spread four-fold and sixteen-fold leave 0.938 and 0.946. | Written in. Normality is not needed, and only a tail with no finite kurtosis breaks the interval. |
| snap-r6-crosshalf-bias | COMPLETE. A cross-half error correlation of 0.02 and 0.05 leaves coverage at 0.946 and 0.943, 0.10 costs a point at 0.928, 0.20 costs two at 0.920, and 0.40 costs seven and a half at 0.864. | Written in. It prices the one assumption the estimator rests on, and the passage-level check places the real leak far below the first step. |
| snap-r6-unequal-noise | COMPLETE. Reading the per-benchmark item counts from the release rather than retyping them, and setting each benchmark's noise to the inverse square root of its count, leaves coverage at 0.945 against 0.946 for equal noise, and doubling the spread leaves 0.948. | Written in. I had first hardcoded ten counts from memory and replaced them with `pop.n_items` before pushing, which is the same class of error as the `load_olmo_as_llama` reconstruction. |
| snap-r6-webb-weights | COMPLETE. At 25 recipes Rademacher, Mammen and Webb weights cover 0.948, 0.939 and 0.948, separating only at five clusters where all three have already failed. | Written in. The weight choice does not matter at this design, and Mammen's 0.873 in the failed corner is a curiosity rather than a recommendation. |
| snap-r6-single-size | COMPLETE. One band of 25 configurations at three runs covers 0.930 while excluding one in 0.313 of replicates at a true 1.2446, rising to 0.739 at six runs and 0.957 at ten, with the median width falling from 0.543 to 0.253. | Written in. It turns the vague statement about weaker within-size evidence into a number, since ten runs inside one band would have settled the 1B question. |

Running now with monitors attached, and queued behind them: snap-r6-structured-truth, snap-r7-verify-1b, snap-r6-perm-exact, snap-r6-detect-power-margin, snap-r6-single-size-acc.

### 2026-09-16 18:35 EDT — the second robustness batch, six landed and written in, one failed and was repaired

The queue runner kept five slots busy without an agent for the whole batch, and the only intervention it needed was a top-up of `research/queue.txt`, since the runner fills slots on its own but cannot invent the next question.

| kernel | result | disposition |
|---|---|---|
| snap-r6-structured-truth | COMPLETE in 20 minutes. Five covariance shapes, each normalised to unit average variance, give recipe-clustered coverage of 0.9357 flat, 0.9430 dominant benchmark, 0.9455 negative links, 0.9463 two blocks and 0.9440 dominant plus negative blocks. Two shapes have a true ratio below one, at 0.9927 and 0.9900, and the interval excludes one in 0.011 and 0.009 of replicates there. | Written in. Compound symmetry is not doing the work, and the two sub-unit shapes show the procedure does not manufacture dependence where there is none. The flat cell sits about 2.7 Monte Carlo standard errors below the others, which is why snap-r6-structured-precise now repeats that comparison at 12,000 replicates rather than leaving a reading on a gap this size. |
| snap-r6-perm-exact | COMPLETE in 16 minutes. The band permutation test rejects at 0.034, 0.046, 0.044, 0.058 and 0.050 under normal errors, a t with five, a t with three, a sixteen-fold recipe variance spread and the t and spread together, with interval coverage between 0.946 and 0.972 and estimator bias never past 0.0017. | Written in. The bound the corner argument rests on keeps its nominal size when the exchangeability the permutation assumes is stressed, so it is not a normal-theory artefact. |
| snap-r6-single-size-acc | COMPLETE in 10 minutes. Inside one size band on the accuracy scale the interval covers 0.707 at three runs, 0.937 at six, 0.947 at ten and 0.948 at forty, with a quarter of replicates undefined at three runs and none past ten. The exclusion rate at a true 1.078 climbs from 0.029 to 0.317. | The reason the per-size table now carries a caveat. At three runs an accuracy interval inside one band is not an interval, because the estimate is undefined in 27 percent of replicates and the rest cover 0.707. |
| snap-r7-verify-1b | COMPLETE in 43 minutes. Fifteen 1B cells across eight recipes score the default branch closest to seed 2, large-aux-2 to seed 4 and large-aux-3 to seed 5, without an exception. The weakest correct correlation is 0.99986 and the strongest wrong one is 0.9098. Nine of the seventeen constructed repositories did not resolve. | Written in. The seed naming the paper assumes now holds at the size band where the interval includes one, which was the obvious place for a reviewer to ask whether the naming had drifted. |
| snap-r6-detect-power-margin | COMPLETE in 35 minutes. At the margin battery's own item noise the interval excludes one in 0.014 of replicates at a true ratio of one, 0.104 at 1.05, 0.332 at 1.10, 0.652 at 1.15 and 0.970 at 1.25. | Written in beside the accuracy power curve. The margin battery is about two and a half times the more sensitive, which is why one scale returns a conclusion and the other returns a bound. |
| snap-r6-estimator-bias | COMPLETE in 51 minutes. Across true ratios from 1.00 to 1.40 the mean estimate sits between 0.0005 above and 0.0028 below the truth on margins and between 0.0032 and 0.0067 below it on accuracy, against estimate standard deviations of 0.055 and 0.098, with no undefined replicate in any of the twelve cells. | Written in. The estimator is close enough to unbiased that the coverage results are about the interval rather than about a shifted point estimate, and the accuracy scale's larger downward pull is consistent with its larger sampling spread. |

snap-r6-logscale-repair failed at version 1 with `KeyError: 'wild_size'`, because I replaced the method dictionary but left the trailing guard that adds a size-clustered interval when the band index has more than one level, and a single-size cell still produces five distinct band labels through the modulo. The guard is gone and version 2 is running.

Running now with monitors attached: snap-r6-logscale-repair, snap-r6-two-runs-only, snap-r6-missing-configs, snap-r6-noise-level and snap-r6-size-count. Queued behind them, in order: snap-r6-logscale-full, snap-r6-alpha-levels, snap-r6-boot-reps, snap-r6-missing-acc, snap-r6-structured-precise and snap-r7-availability-recheck. The last of those repeats the availability census the main text quotes, because that count describes a remote service rather than a shipped file, and a number that can drift should be measured twice before a reviewer measures it for us.

### 2026-09-16 18:35 EDT — the plan's compute is finished, and the package that was missing now exists

An audit of the two planning documents against the repository found every compute gate from R0 through
R15 satisfied by a result file or a dated ledger row, with only R16 and R17 open, and neither of those
needs Kaggle. Both are now closed. Figure C is drawn by `research/figures/make_prediction_figure.py`
from the frozen snap-r8-paired-log result and sits in the paired-uncertainty appendix. The scientific
package is assembled under `research/`, with `manifest.csv` carrying all 375 runs with a SHA-256 each,
`items.csv` carrying all 37,682 items with their BoolQ passage groups, `results/` carrying 806 frozen
simulation cells and an index of 604 result files, `protocol.md` carrying the estimand decisions and the
amendment history, `README.md` carrying both data contracts read off the fields actually on disk, and
`tests/` carrying two suites that both pass.

The tests paid for themselves during the first run. One caught that the results index was harvesting
JSON out of the vendored package checkout inside each kernel's working directory, which would have
credited a cancelled kernel with results it never produced. The other caught that a kernel which failed
and was repushed carries both an ERROR row and a COMPLETE row, so the rule has to read the last state
rather than any state.

| kernel | result | disposition |
|---|---|---|
| snap-r6-logscale-repair | COMPLETE. Inside one size band the log-scale interval covers 0.902 against 0.930 for the plain one on margins, and on accuracy at three runs it is undefined in 0.090 of replicates and misses in 0.151, against 0.284 undefined and 0.017 missed. | The consequential result of the batch. The paper reports a log-scale interval at 1B, so the appendix now reads those endpoints as closer to a ninety percent statement and names the leverage correction as the better repair. |
| snap-r6-logscale-full | COMPLETE. At the full 125-cell design the two constructions agree within 0.004 at every cell, including the corners, and the leverage correction beats both at 0.954. | Written in beside it. The transform is a device for keeping endpoints positive in a thin cell rather than a better interval. |
| snap-r6-size-count | COMPLETE. Holding the cell count near 125, the recipe-clustered interval covers 0.938 to 0.948 across three, five, eight and twelve bands, while the size-clustered fallback covers 0.900 with three bands. | Written in. The fallback needs at least the five bands this release ships. |
| snap-r6-noise-level | COMPLETE. The interval covers 0.944 to 0.955 from item noise 0.25 through 2.00 and collapses at 3.00 to 0.668 with 0.312 undefined. | Written in. The failure point sits near four times the margin battery's noise, far above the accuracy battery's 1.45. |
| snap-r6-missing-configs and snap-r6-missing-acc | COMPLETE. Random drops of a tenth through a half leave margin coverage at 0.940 to 0.950 and accuracy coverage at 0.931 to 0.954, and six whole recipes removed leave 0.945 and 0.947. | Written in. Cluster balance costs nothing here, and the loss lands on width and power instead. |
| snap-r6-two-runs-only | COMPLETE. Across ten to twenty five recipes by two to four runs, coverage stays between 0.936 and 0.949 while the exclusion rate runs 0.266 at the cheapest corner and 0.997 at twenty five by four. | Written in. A reader planning their own measurement should buy recipes before runs. |
| snap-r6-boot-reps | COMPLETE. Bootstrap draws of 999, 2,499, 4,999 and 19,999 give coverage inside 0.002 of each other. | Written in. A replicator can use the cheapest count. |
| snap-r6-alpha-levels | COMPLETE. Nominal levels of one in ten, one in twenty and one in a hundred return 0.896, 0.951 and 0.990, and a band-shared quarter breaks all three together. | Written in. The interval is calibrated rather than tuned at one level. |
| snap-r6-truth-sweep | COMPLETE. Coverage runs 0.934 to 0.949 across true ratios from 1.00 to 1.60, and at a true ratio of exactly one the interval excludes one in 0.018 of replicates. | Written in. That 0.018 is the false positive rate a reader should assume on a battery with no dependence. |
| snap-r6-structured-precise | COMPLETE. Flat covers 0.9471 and dominant covers 0.9429 at 12,000 replicates, reversing the 4,000-replicate ordering. | The correction is in the appendix. This is the second 4,000-replicate gap in this project that failed to survive a larger run. |
| snap-r6-band-shared-acc and snap-r6-recipe-shared-acc | COMPLETE. On accuracy the recipe-clustered interval holds 0.936 to 0.950 under both sharing channels up to a quarter, while the size-clustered wild interval sits near 0.83 at every share including zero and its cluster-robust counterpart holds 0.935. | Written in. The fallback recommendation now names the cluster-robust version on the accuracy scale, because five clusters carrying that item noise are too few for the bootstrap. |
| snap-r7-availability-recheck | COMPLETE in one minute. The census returns the same 80 refusals, 40 open repositories and 5 invalid names seven hours later. | Written in. The provenance count the main text quotes is now a two-reading measurement. |

snap-r6-weights failed at `TypeError: Object of type ndarray is not JSON serializable` after 810 seconds,
because the report echoes each cell's varied parameters and the item-count weighting arrives as a numpy
array. The science had finished by then. The weights are now a list and the dump carries a default, and
version 2 is queued.

## 2026-09-16, 18:20 to 19:00 EDT, weighting and the audit pass

| kernel | outcome | what it changed |
|---|---|---|
| snap-r6-weights (v2) | COMPLETE. Flat, item-count, four-fold ramp and concentrated weightings cover 0.941 to 0.951, while the true ratio falls from 1.2446 to 1.0975 and the exclusion rate from 0.962 to 0.509. | Written in. The weighting moves the quantity being estimated rather than the honesty of the interval around it. |
| snap-r6-ragged-runs | RUNNING. Per-configuration run counts, with the shortfall random, lumpy by recipe, or mixed across two, three and four seeds. | The release loses seeds unevenly and every earlier sweep assumed it did not. |
| snap-r6-perm-power-acc | QUEUED. The permutation band-sharing test measured at the accuracy battery's noise of 1.45 rather than the margin battery's 0.73. | The paper quotes that bound on all four batteries and has only measured the test's power on one scale. |
| snap-r6-band-noise | QUEUED. Item noise that varies by size band over two-fold, four-fold and sixteen-fold ramps, plus one loud band and an alternating pattern. | Heteroscedasticity aligned with a variable the recipe clustering never models. |

An independent verification pass read every simulation number written into the appendix today back to
`research/results/simulation_cells.csv` and the kernel JSON, and it found five that did not match. The
noise sweep's upper endpoint is 0.954 rather than 0.955. The log-scale cells agree within 0.006 rather
than 0.004, because `recipe_0.114` and `band_0.25` separate by 0.0045 and 0.0058. The permutation
exactness run under seed 20260969 used 500 replicates with 999 permutations rather than 4,000 replicates.
The median widths of 0.357 and 0.246 belong to seed 20260955 on the margin scale rather than to the two
accuracy sweeps they had been placed under. The 1B branch verification covers five of the eight recipes
that resolve at 1B rather than all of them, since the run attempted three names that do not exist and
never attempted falcon, fineweb-edu or fineweb-pro. Each of the five was re-verified against the source
file before the appendix was edited, and all five are now corrected.

| snap-r6-corner-acc | COMPLETE at 4,000 replicates under seed 20260988. On the accuracy scale the recipe-clustered interval covers 0.950 with no sharing, 0.942 at the band bound, 0.951 at the recipe bound, 0.946 at the joint corner and 0.940 at twice both bounds. | Written in. The corner costs the accuracy interval the same half point to a point it costs the margin one, so the limitation applies to both reported scales. Its design note promised a leverage-corrected column that the run did not compute, which is why snap-r6-cr3-acc is queued. |

The verification agent also checked the provenance census and found it exact, since the two runs return
the same 80 refusals, 40 open repositories and 5 invalid names over the identical 125 repositories with
no per-repository difference. It flagged the gap between them as six and a half hours rather than the
seven the appendix claimed, which the run times of 11:31 and 18:01 confirm, and the appendix now says six
and a half.

| snap-r6-cluster-count-acc | COMPLETE at 4,000 replicates under seed 20260987. At the accuracy battery's item noise the wild interval covers 0.360 at five recipes with 0.606 undefined, 0.869 at ten, 0.943 at fifteen, 0.960 at 25, and 0.949 and 0.950 at forty and sixty. | Written in. The cluster floor is scale-dependent, since ten recipes suffice on margins and fall short on accuracy, and the size-clustered fallback recovers from 0.850 to 0.942 as the grid grows while holding five clusters throughout. |
| snap-r6-cheap-acc | QUEUED. The ten to 25 recipe by two to four run grid priced at the accuracy battery's noise, with twenty recipes added because the floor sits between ten and fifteen. | The appendix tells a reader to buy recipes before runs, and that advice was measured only on margins. |
